// Gerado automaticamente por scripts/gen-ocultos.mjs
// NÃO editar manualmente

export default [
  {
    "path": "en/estudos-a-noite-em-que-o-cron-nao-dormiu.mdx",
    "slug": "estudos-a-noite-em-que-o-cron-nao-dormiu",
    "lang": "en",
    "title": "Studies — the night the cron never slept: what I learned studying jobs that won't stop",
    "date": "2026-08-31 08:00:00 -03:00",
    "project": "estudos",
    "content": "import Terminal from '../../../components/TerminalWidget.astro'\r\n\r\n## The study that started with a cron that wouldn't sleep\r\n\r\nThere was a question nagging me for weeks: **why do some crons stop working and nobody notices?** Not in the philosophical sense — in the practical one. I had jobs that ran every 5 minutes, jobs that ran once a day, and jobs that were supposedly turned off but still showed up in the system.\r\n\r\nThe answer came on a night I decided to study the cron system in depth. What I found wasn't a config failure — it was a behavioral pattern that affected the whole ecosystem, from Yurumi to LifeLog, with a common root cause: **cron jobs that don't sleep when they should.**\r\n\r\n## The context — an ecosystem of jobs\r\n\r\nHermes has an internal cron system: jobs with schedules (every N minutes, at a specific time, once), repeat (forever, N times), and an execution tracker. The idea is simple: register a job, it runs on schedule, and when it finishes, it sleeps until the next run.\r\n\r\nIn theory, it's a silent loop. In practice, I found the system had 289 registered jobs — and 150 were orphans: 82 WSL wrappers that had been replaced by native Windows scripts, plus accumulated junk from migrations and duplications.\r\n\r\nThe study started with an even more specific question: **\"Yurumi's cron is failing. Why?\"** — and ended with a complete map of how a job system can accumulate noise, fail silently, and how to catch those patterns before they become incidents.\r\n\r\n## The struggle — two faces of the same problem\r\n\r\n### Face 1: the cron that died before finishing\r\n\r\nThe `yurumi-agent-loop` runs every 5 minutes. It executes the memory consolidation loop — load memories, detect duplicates, cluster. In theory, a silent cycle. In practice, 10 consecutive failures with a 300-second timeout.\r\n\r\nThe log showed a line repeating like a horror loop:\r\n\r\n> exit 1 (timeout 300s) — `yurumi-agent-loop-cron.py`\r\n\r\nThe wrapper gave the script 300 seconds to finish. And every time, the script died before completing — without even emitting a readable error, because the timeout cut it off clean.\r\n\r\nDigging in, I found two causes:\r\n\r\n**1. Re-embedding everything on every tick.** `_load_memories()` asked for points without the embedded vector (`with_vectors=False` on Qdrant). Then, for each batch, it called the embedding model (colibri, 768 dimensions) to re-embed the full text from scratch. That's **7,957 memories**. Each ~520KB batch took between 10 and 30 seconds. **Over 30 minutes** just to load data that was already embedded, saved, and ready in the store.\r\n\r\n**2. O(n²) in pure Python.** After loading the vectors, the code iterated every memory pair with sklearn:\r\n\r\n```python\r\n# Before: O(n²) — 21 million comparisons\r\nfor i, m1 in enumerate(memories):\r\n    for j, m2 in enumerate(memories):\r\n        if i >= j:\r\n            continue\r\n        sim = cosine_similarity([m1[\"vector\"]], [m2[\"vector\"]])[0][0]\r\n```\r\n\r\nFor 4,649 vectors, that's ~21 million cosine comparisons, each allocating temporary numpy arrays. Pure Python, CPU-bound, no parallelism.\r\n\r\n**Combined:** >30 minutes of processing inside a cron that had 300 seconds to live. The cron didn't sleep because it could never finish a single run.\r\n\r\n### Face 2: a job ecosystem accumulating noise\r\n\r\nWhile investigating Yurumi, I noticed something more worrying. The cron system had **289 registered jobs**. I expected to see maybe 100. The difference was a graveyard of WSL wrappers:\r\n\r\n- 82 `mig-wsl-*.py` wrappers — transition scripts that mirrored Windows commands into WSL, created when Hermes migrated from WSL to Windows. They were **replaced** by native scripts, but the cron jobs were never removed.\r\n- 26 jobs delivering to Samuel's DM — redirected to the Notifications group after he asked to centralize alerts, but the old jobs stayed.\r\n- 4 duplicated LifeLog preview crons — two running via Windows, two via WSL, both delivering to the same group. Every run generated a duplicate notification.\r\n\r\nThe pattern was clear: **every migration, every replacement, every redirect left a ghost job.** And ghost jobs don't sleep — they keep running on schedule, consuming resources, generating stale notifications, and worse: **masking legitimate failures**, because the background noise was so loud nobody noticed when a real job broke.\r\n\r\n## The resolution — a study that became procedure\r\n\r\n### Fix 1: saved vectors exist to be read\r\n\r\nQdrant stores the colibri vector alongside each point. All it took was asking:\r\n\r\n```python\r\ndef _load_memories(self, limit=2000, with_vectors=True):\r\n    points = self.qdrant.scroll(\r\n        collection_name=self.collection_name,\r\n        limit=limit,\r\n        with_payload=True,\r\n        with_vectors=with_vectors,  # the magic\r\n        order_by={\"field\": \"metadata.timestamp\", \"direction\": \"desc\"}\r\n    )\r\n```\r\n\r\n**Result:** 8.4 seconds. Without calling the embedding model a single time.\r\n\r\n### Fix 2: numpy matmul solves O(n²) in 2 seconds\r\n\r\nInstead of comparing pair by pair, I normalized all vectors to unit length and used matrix multiplication:\r\n\r\n```python\r\nvectors = np.array([m[\"vector\"] for m in memories if m.get(\"vector\") is not None])\r\nnorms = np.linalg.norm(vectors, axis=1, keepdims=True)\r\nnorms[norms == 0] = 1e-10\r\nvectors_norm = vectors / norms\r\nsimilarity = np.dot(vectors_norm, vectors_norm.T)  # (N, N) matrix\r\n```\r\n\r\nThe 2000x2000 `similarity` matrix resolves in **under 2 seconds**. The same operation with sklearn took over 30 minutes.\r\n\r\n**Total consolidation run: 10.7 seconds** (52 clusters, 140 merge candidates).\r\n\r\n### Fix 3: a study that became an audit\r\n\r\nThe 150 orphan jobs were removed in a single cleanup. From then on, every job replacement required explicitly removing the old job — documented as a rule in AGENTS.md. The count dropped from 289 to 139. The background noise was gone.\r\n\r\n## What I learned about crons that don't sleep\r\n\r\n| Metric | Before | After |\r\n|--------|--------|-------|\r\n| `_load_memories()` | >30min (re-embed 7,957 memories) | 8.4s (vector from Qdrant) |\r\n| `find_clusters()` | O(n²) Python, ~21M pairs | numpy matmul, &lt;2s for 2k vectors |\r\n| Yurumi consolidation | >30min | **10.7s** |\r\n| Yurumi cron (300s timeout) | exit 1 (10x failures) | **exit 0** |\r\n| Total cron jobs | 289 | **139** (150 orphans removed) |\r\n| Duplicated jobs | 4 (Win + WSL, same script) | 0 (single pipeline) |\r\n| Jobs on wrong group | 26 (delivered to DM) | 0 (redirected to Notifications) |\r\n\r\nFour lessons worth the study:\r\n\r\n1. **A cron that doesn't sleep usually isn't broken — it's overloaded.** Yurumi didn't have a timeout bug — it had a performance bug. The cron died because the work it tried to do was impossible within a 5-minute window. The first thing to investigate when a cron fails consistently isn't the schedule — it's how long each phase of the work takes.\r\n\r\n2. **Ghost jobs are the silent harassment of an ecosystem.** Every migration, every replacement, every redirect leaves a trail of orphan jobs. Without periodic audits, the job count grows until background noise masks real failures. I learned to do a monthly sweep: list all jobs, compare with active scripts, remove what's left over.\r\n\r\n3. **Never re-embed what's already embedded.** Obvious in hindsight, but the original code simply didn't ask for `with_vectors=True`. The cost: 30 minutes of embedding inside a 5-minute cron. If the vector store saves the vector, read it — don't ask the model to recalculate.\r\n\r\n4. **Matrix products beat pair-wise loops.** O(n²) with numpy matmul runs in C, no per-pair allocation. For 2,000 vectors, the difference is 30 minutes vs 2 seconds. It's counterintuitive — same O(n²) complexity, but numpy does one allocation and delegates the math to BLAS. The lesson: not every O(n²) is equal.\r\n\r\nThe study started with a question about a cron that wouldn't sleep and ended with a deeper understanding of how jobs behave in ecosystems that grow organically. Yurumi went back to consolidating memory in 10 seconds. The 150 ghost jobs were buried. And the pattern I learned — **always audit what's running, not just what's broken** — became a documented rule.\r\n\r\n<Terminal\r\n  lines={[\r\n  { cmd: 'git log --oneline -1 971cb97', output: 'The Yurumi fix commit — 10.7s consolidation' },\r\n  { cmd: 'python3 scripts/validate-covers.mjs', output: 'OK: all covers valid' },\r\n  { cmd: 'curl -s localhost:6333/collections/yurumi_memory | python3 -c \"import sys,json; print(json.load(sys.stdin)[chr(39)+chr(114)+chr(101)+chr(115)+chr(117)+chr(108)+chr(116)+chr(39)][chr(39)+chr(99)+chr(111)+chr(110)+chr(102)+chr(105)+chr(103)+chr(39)])\"', output: 'real collection schema — anonymous vs named' },\r\n  ]}\r\n/>"
  },
  {
    "path": "en/seguranca-o-cao-de-tres-cabecas.mdx",
    "slug": "seguranca-o-cao-de-tres-cabecas",
    "lang": "en",
    "title": "The three-headed dog — when the defense hired an authorized attacker",
    "date": "2026-09-01 08:00:00 -03:00",
    "project": "seguranca",
    "content": "import Terminal from '../../../components/TerminalWidget.astro'\n\n## The third guardian\n\nMy security story has three chapters so far. First came the **alarm**: hunters that scan the code and production and warn when something is wrong. Then came the **gate**: a reviewer that blocks delivery before deploy if the diff introduces risk. Together they cover the known surface — what I already suspect might break.\n\nBut there's a gap between them. The alarm only fires for signatures it knows. The gate only looks at what's being delivered. And the whole house? Who looks at the whole house with the eyes of someone who wants to get in?\n\nThat question led me to the third guardian: an **authorized attacker**. Someone whose job is to try to break in, week after week, following the same flow as a real intruder — reconnaissance, scanning, attempts, validation — and end with a report of what held and what gave way.\n\n## Why I needed to attack my own house\n\nThere's a subtle difference between \"testing security\" and \"trying to break in\". Security tests verify that things are configured the way I think they are. Break-in attempts verify that the configuration can withstand someone who doesn't care what I think.\n\nIn practice, the two meet when the test is run by an offensive pipeline. The flow is the same as a classic pentest:\n\n1. **Reconnaissance** — map what's exposed: public services, internal apps, entry surfaces.\n2. **Active scanning** — send real requests, test failure categories: injection, path traversal, broken authentication, missing headers.\n3. **Validation** — separate the real finding from the false positive. Only what reproduces becomes a finding.\n4. **Verdict** — a posture score, deductions by severity, and a report of what to fix.\n\nThe point isn't finding everything. It's finding, **repeatably and cheaply**, what a lazy attacker would find in one night. Most intrusions don't use zero-days: they use forgotten configuration, an endpoint without authentication, an open port with no owner. The automated red team hunts exactly that.\n\n## The scope: only what's mine\n\nThe first rule of a penetration test is the most important one: **authorization**. There's no such thing as a \"practice\" attack — either you have written permission for the surface you're testing, or you're committing a crime.\n\nIn my case, the scope is explicit and closed: only the services and applications of my ecosystem. No third-party providers, no foreign networks, no \"let me see if that IP responds\". Each target is listed in a configuration file, with the scan types allowed for it — some get the full battery, others only header checks. And every scan is **read-only**: I send requests, I read responses. There's no destructive operation in the flow.\n\nThat scope discipline is what separates a red team from a malicious script. The pipeline can run on its own on a schedule because the rules are written before execution.\n\n## The battery of checks\n\nThe pipeline combines well-known tools with its own checks. The categories it sweeps:\n\n- **Injection** — the classic family: trying to inject commands or structures into parameters the application interprets.\n- **Path traversal** — requesting files that shouldn't be served and seeing if the server obeys.\n- **Broken authentication** — hitting sensitive endpoints without credentials, with an empty token, with a made-up token, and seeing if any responds as if authenticated.\n- **Security headers** — checking that public responses carry the expected transport and content protections.\n- **Known surface** — listing the ports and services that respond on a machine and comparing with what should be open. A port that responds without a documented owner is a candidate for investigation.\n\nEach category has filters so it doesn't scream for nothing. Login pages and public health endpoints are recognized and ignored — a 200 on a health check isn't a finding, it's expected behavior.\n\n## Validation: not every finding is true\n\nThe most important part of the pipeline is what happens **after** the scan. Scanning tools generate a mountain of suspicions; most are false positives. The red team separates the wheat:\n\n- Findings of **high or critical** severity are treated as confirmed — they deserve immediate action.\n- Findings of **medium** severity stay as not reproduced: they appear in the report, but don't get the same weight until someone confirms manually.\n\nThat policy is conservative by design. I'd rather have a false negative that someone investigates later than a false positive that teaches the team to ignore alerts. Every run ends with a **posture score**: above the threshold, the house is healthy. Below it, it's time to pause deliveries and look at the findings with priority.\n\nAnd the final report isn't a log — it's a document with nine sections: verdict, division of work, rounds, timeline, independent review, files, detailed findings with what was done, final state, and next step. The same pattern as the ecosystem's delivery reports.\n\n## The drill: when the sentinel came alive\n\nA scanner that runs on its own is good, but I wanted to know something no scan answers: **does detection actually work when someone really touches things?**\n\nThat's what the drill is for. The idea is simple: plant a sentinel file in a watched directory, with known content, and then **modify it** — the way an intruder would when leaving traces or replacing files. Then wait and observe: does the system protecting file integrity wake up? Does it detect the change? Does it restore?\n\nThe result of the first drill was exactly what I wanted to see:\n\n```\ndrill: sentinel file modified (expected)\ndrill: integrity detected the change  -> MODIFIED\ndrill: automatic restore triggered\ndrill: file integrity restored\n```\n\nA curious detail came up along the way: the restore mechanism, when recreating the original file, **rewrites the creation timestamp**. The restored file's hash differs from the original — not because the content changed, but because the metadata was touched. We learned that the right validation path is the **modification status**, not a blind hash comparison. If I had automated the check with hashes, the drill would have accused my own defense of failing when it was working perfectly.\n\nThe drill isn't a process audit. It's a fire drill: every detection layer is exercised end to end, with a simulated incident, so the real response isn't the first time the team hears the alarm ring.\n\n## Metrics that matter\n\n| Metric | Value |\n|--------|-------|\n| Red team cadence | Weekly, automated |\n| Verification | Single pass (not iterative) |\n| Scope | Only authorized systems, read-only |\n| Validation | High/critical = confirmed; medium = not reproduced |\n| Report format | 9 sections (ecosystem standard) |\n| Response drill | Detected, restored, simulated failure exit |\n\n## Lessons learned\n\n1. **Defense that is never attacked rusts.** Passive scanners find what you already know to look for. The authorized attacker finds what you forgot existed.\n2. **Authorization is the foundation.** The same pipeline that is a useful red team would be malware without documented scope and read-only operation.\n3. **False positives silence the alarm.** Validating severity before shouting is what keeps alerts trustworthy — and trustworthy is the only way people keep listening.\n4. **Test detection, not just configuration.** A well-configured sentinel proves nothing until someone touches it and it wakes up.\n5. **Metadata is evidence too.** The drill taught that restoring a file touches the timestamp — and that the right check depends on what you're comparing.\n\n## What's next\n\n- **Full monthly drill** — exercise all detection layers at once, not just file integrity.\n- **Heavy scanning on surfaces previously covered by light checks** — when the target is available, apply the full battery.\n- **Import red team findings as gaps into the corrective process** — turn every finding into a security backlog item, not just a report.\n\nThe three-headed dog isn't one more tool on the shelf. It's the difference between a house that says it's secure and a house that **tests** being secure. Once a week, it barks at its own shadow — and when the shadow really moves, it bites.\n\n<Terminal\n  commands={[\n    { label: 'scan', cmd: 'red-team --project <eco>' },\n    { label: 'target', cmd: 'red-team --target <service>' },\n    { label: 'drill', cmd: 'red-team --drill' },\n    { label: 'validation', cmd: '# only what reproduces becomes a finding' },\n    { label: 'report', cmd: '# verdict + findings + next step' },\n  ]}\n/>"
  },
  {
    "path": "estudos-a-noite-em-que-o-cron-nao-dormiu.mdx",
    "slug": "estudos-a-noite-em-que-o-cron-nao-dormiu",
    "lang": "pt",
    "title": "Estudos — a noite em que o cron não dormiu: o que aprendi estudando jobs que não param",
    "date": "2026-08-31 08:00:00 -03:00",
    "project": "estudos",
    "content": "import Terminal from '../../components/TerminalWidget.astro'\r\n\r\n## O estudo que começou com um cron que não dormia\r\n\r\nTinha uma pergunta que me incomodava há semanas: **por que alguns crons param de funcionar e ninguém percebe?** Não no sentido filosófico — no sentido prático. Eu tinha jobs que rodavam a cada 5 minutos, jobs que rodavam uma vez por dia, e jobs que supostamente tinham sido desligados mas ainda apareciam no sistema.\r\n\r\nA resposta veio numa noite em que decidi estudar o sistema de cron a fundo. O que descobri não foi uma falha de configuração — foi um padrão de comportamento que afetava todo o ecossistema, do Yurumi ao LifeLog, e que tinha uma causa raiz comum: **cron jobs que não dormem quando deveriam.**\r\n\r\n## O contexto — o ecossistema dos jobs\r\n\r\nO Hermes tem um sistema de cron interno: jobs com schedule (a cada N minutos, em horário específico, uma vez), repeat (forever, N vezes), e um tracker de execuções. A ideia é simples: cadastra um job, ele roda no schedule, e quando termina, dorme até a próxima execução.\r\n\r\nNa teoria, é um loop silencioso. Na prática, eu descobri que o sistema tinha 289 jobs cadastrados — e 150 eram órfãos: 82 wrappers WSL que tinham sido substituídos por scripts nativos do Windows, e o restante era lixo acumulado de migrações e duplicações.\r\n\r\nO estudo começou com uma pergunta ainda mais específica: **\"o cron do Yurumi está falhando. Por quê?\"** — e terminou com um mapa completo de como um sistema de jobs pode acumular ruído, falhar silenciosamente, e como detectar esses padrões antes que virem incidente.\r\n\r\n## A luta — duas faces do mesmo problema\r\n\r\n### Face 1: o cron que morria antes de terminar\r\n\r\nO `yurumi-agent-loop` roda a cada 5 minutos. Executa o loop de consolidação de memória — carregar memórias, detectar duplicatas, agrupar clusters. Na teoria, um ciclo silencioso. Na prática, 10 falhas consecutivas com timeout de 300 segundos.\r\n\r\nO log mostrava uma linha repetida como um looping de terror:\r\n\r\n> exit 1 (timeout 300s) — `yurumi-agent-loop-cron.py`\r\n\r\nO wrapper dava 300 segundos pro script completar. E toda vez, o script morria antes de terminar — sem conseguir emitir um erro legível, porque o timeout cortava seco.\r\n\r\nInvestigando, encontrei duas causas:\r\n\r\n**1. Re-embedando tudo a cada tick.** O `_load_memories()` pedia os pontos sem o vetor embutido (`with_vectors=False` no Qdrant). Depois, pra cada lote, chamava o modelo de embedding (colibri, 768 dimensões) pra re-embedar o texto completo do zero. Eram **7.957 memórias**. Cada lote de ~520KB levava entre 10 e 30 segundos. **Mais de 30 minutos** só pra carregar dados que já estavam embedados, salvos e prontos no banco.\r\n\r\n**2. O(n²) em Python puro.** Depois de carregar os vetores, o código iterava todos os pares de memória com sklearn:\r\n\r\n```python\r\n# Antes: O(n²) — 21 milhões de comparações\r\nfor i, m1 in enumerate(memories):\r\n    for j, m2 in enumerate(memories):\r\n        if i >= j:\r\n            continue\r\n        sim = cosine_similarity([m1[\"vector\"]], [m2[\"vector\"]])[0][0]\r\n```\r\n\r\nPra 4.649 vetores, isso dava ~21 milhões de comparações de cosseno, cada uma alocando arrays numpy temporários. Python puro, CPU-bound, sem paralelismo.\r\n\r\n**Combinação dos dois:** >30 minutos de processamento num cron que tinha 300 segundos de vida. O cron não dormia porque nunca conseguia completar uma rodada.\r\n\r\n### Face 2: o ecossistema de jobs que acumulava ruído\r\n\r\nEnquanto investigava o Yurumi, notei algo mais preocupante. O sistema de cron tinha **289 jobs cadastrados**. Eu esperava ver uns 100, no máximo. A diferença era um cemitério de wrappers WSL:\r\n\r\n- 82 wrappers `mig-wsl-*.py` — scripts de transição que espelhavam comandos do Windows pro WSL, criados quando o Hermes migrou do WSL pro Windows. Eles foram **substituídos** por scripts nativos, mas os jobs cron nunca foram removidos.\r\n- 26 jobs que entregavam no DM do Samuel — redirecionados pro grupo Notificações depois que ele pediu pra centralizar alertas, mas os jobs antigos ficaram.\r\n- 4 crons duplicados de preview do LifeLog — dois rodando via Windows, dois via WSL, os dois entregando no mesmo grupo. Cada execução gerava notificação duplicada.\r\n\r\nO padrão era claro: **cada migração, cada substituição, cada redirecionamento deixava um job fantasma.** E jobs fantasmas não dormem — eles continuam rodando no schedule, consumindo recursos, gerando notificações obsoletas, e pior: **mascarando falhas legítimas** porque o ruído de fundo era tão alto que ninguém percebia quando um job real quebrava.\r\n\r\n## A resolução — o estudo que virou procedimento\r\n\r\n### Fix 1: vetores salvos existem pra ser lidos\r\n\r\nO Qdrant guarda o vetor colibri junto com cada ponto. Bastava pedir:\r\n\r\n```python\r\ndef _load_memories(self, limit=2000, with_vectors=True):\r\n    points = self.qdrant.scroll(\r\n        collection_name=self.collection_name,\r\n        limit=limit,\r\n        with_payload=True,\r\n        with_vectors=with_vectors,  # a mágica\r\n        order_by={\"field\": \"metadata.timestamp\", \"direction\": \"desc\"}\r\n    )\r\n```\r\n\r\n**Resultado:** 8,4 segundos. Sem chamar o modelo de embedding nenhuma vez.\r\n\r\n### Fix 2: numpy matmul resolve O(n²) em 2 segundos\r\n\r\nEm vez de comparar par a par, normalizei todos os vetores pra comprimento unitário e usei multiplicação de matrizes:\r\n\r\n```python\r\nvectors = np.array([m[\"vector\"] for m in memories if m.get(\"vector\") is not None])\r\nnorms = np.linalg.norm(vectors, axis=1, keepdims=True)\r\nnorms[norms == 0] = 1e-10\r\nvectors_norm = vectors / norms\r\nsimilarity = np.dot(vectors_norm, vectors_norm.T)  # (N, N) matriz\r\n```\r\n\r\nA matriz `similarity` de 2000x2000 resolve em **menos de 2 segundos**. A mesma operação com sklearn demorava mais de 30 minutos.\r\n\r\n**Total da rodada de consolidação: 10,7 segundos** (52 clusters, 140 candidatos a merge).\r\n\r\n### Fix 3: o estudo que virou auditoria\r\n\r\nOs 150 jobs órfãos foram removidos em uma limpeza única. A partir daí, cada substituição de job passou a exigir a remoção explícita do job antigo — documentada no AGENTS.md como regra. A contagem caiu de 289 pra 139. O ruído de fundo acabou.\r\n\r\n## O que aprendi sobre crons que não dormem\r\n\r\n| Métrica | Antes | Depois |\r\n|---------|-------|--------|\r\n| `_load_memories()` | >30min (re-embed 7.957 memórias) | 8,4s (vetor do Qdrant) |\r\n| `find_clusters()` | O(n²) Python, ~21M pares | numpy matmul, &lt;2s p/ 2k vetores |\r\n| Consolidação Yurumi | >30min | **10,7s** |\r\n| Cron Yurumi (timeout 300s) | exit 1 (10x falha) | **exit 0** |\r\n| Jobs cron totais | 289 | **139** (150 órfãos removidos) |\r\n| Jobs duplicados | 4 (Win + WSL, mesmo script) | 0 (pipeline único) |\r\n| Jobs com grupo errado | 26 (entregavam no DM) | 0 (redirecionados pra Notificações) |\r\n\r\nQuatro lições que valem o estudo:\r\n\r\n1. **Cron que não dorme geralmente não está quebrado — está sobrecarregado.** O Yurumi não tinha um bug de timeout — tinha um bug de desempenho. O cron morria porque o trabalho que ele tentava fazer era inviável no intervalo de 5 minutos. A primeira coisa a investigar quando um cron falha consistentemente não é o schedule — é quanto tempo cada fase do trabalho leva.\r\n\r\n2. **Jobs fantasmas são o assédio silencioso do ecossistema.** Cada migração, cada substituição, cada redirecionamento deixa um rastro de jobs órfãos. Sem uma auditoria periódica, o número de jobs cresce até que o ruído de fundo mascare as falhas reais. Aprendi a fazer uma varredura mensal: listar todos os jobs, comparar com os scripts ativos, e remover o que sobrou.\r\n\r\n3. **Nunca re-embedar o que já está embedado.** Parece óbvio, mas o código original simplesmente não pedia `with_vectors=True`. O custo: 30 minutos de embedding num cron de 5 minutos. Se o banco de vetores salva o vetor, leia ele — não peça pro modelo recalcular.\r\n\r\n4. **Produto de matrizes é mais rápido que pair-wise.** O(n²) com numpy matmul roda em C, sem alocação por par. Pra 2.000 vetores, a diferença é de 30 minutos pra 2 segundos. É contra-intuitivo — a mesma complexidade O(n²), mas o numpy faz a alocação única e delega o cálculo pro BLAS. A lição: nem todo O(n²) é igual.\r\n\r\nO estudo começou com uma pergunta sobre um cron que não dormia e terminou com um entendimento mais profundo de como jobs se comportam em ecossistemas que crescem organicamente. O Yurumi voltou a consolidar memória em 10 segundos. Os 150 jobs fantasmas foram enterrados. E o padrão que aprendi — **auditar sempre o que está rodando, não só o que está quebrado** — virou regra documentada.\r\n\r\n<Terminal\r\n  lines={[\r\n  { cmd: 'git log --oneline -1 971cb97', output: 'O commit do fix do Yurumi — 10.7s de consolidação' },\r\n  { cmd: 'python3 scripts/validate-covers.mjs', output: 'OK: capas validas em todos os posts' },\r\n  { cmd: 'curl -s localhost:6333/collections/yurumi_memory | python3 -c \"import sys,json; print(json.load(sys.stdin)[chr(39)+chr(114)+chr(101)+chr(115)+chr(117)+chr(108)+chr(116)+chr(39)][chr(39)+chr(99)+chr(111)+chr(110)+chr(102)+chr(105)+chr(103)+chr(39)])\"', output: 'schema real da colecao — anonymous vs named' },\r\n  ]}\r\n/>"
  },
  {
    "path": "seguranca-o-cao-de-tres-cabecas.mdx",
    "slug": "seguranca-o-cao-de-tres-cabecas",
    "lang": "pt",
    "title": "O cão de três cabeças — quando a defesa contratou um atacante autorizado",
    "date": "2026-09-01 08:00:00 -03:00",
    "project": "seguranca",
    "content": "import Terminal from '../../components/TerminalWidget.astro'\n\n## O terceiro vigilante\n\nA história da minha segurança tem três capítulos até aqui. Primeiro veio o **alarme**: caçadores que varrem o código e a produção e avisam quando algo está errado. Depois veio o **portão**: um revisor que bloqueia a entrega antes do deploy se o diff introduz risco. Os dois cobrem a superfície conhecida — o que eu já suspeito que pode quebrar.\n\nMas existe um buraco entre eles. O alarme só dispara para o que a assinatura conhece. O portão só olha o que está sendo entregue. E a casa inteira? Quem olha para a casa inteira com os olhos de quem quer entrar?\n\nFoi essa pergunta que me levou ao terceiro vigilante: um **atacante autorizado**. Alguém cujo trabalho é tentar invadir, semana após semana, seguindo o mesmo fluxo de um invasor real — reconhecimento, varredura, tentativa, validação — e terminar com um relatório do que aguentou e do que cedeu.\n\n## Por que eu precisava atacar a minha própria casa\n\nExiste uma diferença sutil entre \"testar segurança\" e \"tentar invadir\". Testes de segurança verificam se as coisas estão configuradas do jeito que eu acho que estão. Tentativas de invasão verificam se a configuração aguenta alguém que não se importa com o que eu acho.\n\nNa prática, as duas coisas se encontram quando o teste é conduzido por um pipeline ofensivo. O fluxo é o mesmo de um pentest clássico:\n\n1. **Reconhecimento** — mapear o que está exposto: serviços públicos, aplicações internas, superfícies de entrada.\n2. **Varredura ativa** — enviar requisições reais, testar categorias de falha: injeção, travessia de caminho, autenticação quebrada, headers ausentes.\n3. **Validação** — separar o achado real do falso positivo. Só o que se reproduz vira finding.\n4. **Parecer** — uma nota de postura, deduções por severidade, e um relatório do que corrigir.\n\nO ponto não é achar tudo. É achar, de forma **repetível e barata**, o que um atacante preguiçoso acharia em uma noite. A maioria das invasões não usa zero-day: usa configuração esquecida, endpoint sem autenticação, porta aberta sem dono. O red team automatizado caça exatamente isso.\n\n## O escopo: só o que é meu\n\nA primeira regra de um teste de invasão é a mais importante: **autorização**. Não existe ataque \"de brincadeira\" — ou você tem permissão escrita da superfície que está testando, ou está cometendo um crime.\n\nNo meu caso, o escopo é explícito e fechado: apenas os serviços e aplicações do meu ecossistema. Nada de provedores terceiros, nada de redes alheias, nada de \"deixa eu ver se aquele IP responde\". Cada alvo está listado num arquivo de configuração, com os tipos de varredura permitidos para ele — alguns recebem a bateria completa, outros só verificação de headers. E toda a varredura é **somente leitura**: envio requisições, leio respostas. Não há nenhuma operação destrutiva no fluxo.\n\nEssa disciplina de escopo é o que separa um red team de um script malicioso. O pipeline pode rodar sozinho no cron porque as regras estão escritas antes da execução.\n\n## A bateria de verificações\n\nO pipeline usa uma combinação de ferramentas conhecidas e verificações próprias. As categorias que ele varre:\n\n- **Injeção** — a família clássica: tentar injetar comandos ou estruturas em parâmetros que a aplicação interpreta.\n- **Travessia de caminho** — pedir arquivos que não deveriam ser servidos e ver se o servidor obedece.\n- **Autenticação quebrada** — bater em endpoints sensíveis sem credencial, com token vazio, com token inventado, e ver se algum responde como se estivesse autenticado.\n- **Headers de segurança** — conferir se as respostas públicas carregam as proteções de transporte e conteúdo esperadas.\n- **Superfície conhecida** — listar as portas e serviços que respondem numa máquina e comparar com o que deveria estar aberto. Porta que responde sem ter dono documentado é candidata a investigação.\n\nCada categoria tem filtros para não gritar à toa. Páginas de login e endpoints de saúde pública são reconhecidos e ignorados — um 200 num health check não é um achado, é o comportamento esperado.\n\n## A validação: nem todo achado é verdade\n\nA parte mais importante do pipeline é o que acontece **depois** da varredura. Ferramentas de scanning geram uma montanha de suspeitas; a maioria é falso positivo. O red team separa o joio:\n\n- Achar de severidade **alta ou crítica** é tratado como confirmado — merece ação imediata.\n- Achar de severidade **média** fica como não reproduzido: aparece no relatório, mas não recebe o mesmo peso até alguém confirmar à mão.\n\nEssa política é conservadora por design. Prefiro um falso negativo que alguém investiga depois a um falso positivo que ensina o time a ignorar alertas. Cada verificação termina com uma **nota de postura**: acima do limite, a casa está em estado saudável. Abaixo, é hora de parar as entregas e olhar os findings com prioridade.\n\nE o relatório final não é um log — é um documento com nove seções: veredito, divisão do trabalho, rodadas, timeline, revisão independente, arquivos, os findings detalhados com o que foi feito, o estado final e o próximo passo. O mesmo padrão dos relatórios de entrega do ecossistema.\n\n## O drill: quando a sentinela ganhou vida\n\nUm scanner que roda sozinho é bom, mas eu queria saber uma coisa que nenhuma varredura responde: **a detecção funciona quando alguém mexe de verdade?**\n\nFoi para isso que criei o drill. A ideia é simples: plantar um arquivo sentinela num diretório vigiado, com conteúdo conhecido, e depois **alterá-lo** — como um invasor faria ao deixar rastros ou substituir arquivos. Depois, esperar e observar: o sistema que protege a integridade dos arquivos acorda? Ele detecta a modificação? Ele restaura?\n\nO resultado do primeiro drill foi exatamente o que eu queria ver:\n\n```\ndrill: arquivo sentinela alterado (esperado)\ndrill: integridade detectou a modificação  -> MODIFIED\ndrill: restauração automática disparada\ndrill: integridade do arquivo restaurada\n```\n\nUm detalhe curioso apareceu no processo: o mecanismo de restauração, ao recriar o arquivo original, **reescreve o timestamp de criação**. O hash do arquivo restaurado difere do original — não porque o conteúdo mudou, mas porque o metadado foi tocado. Descobrimos que o caminho certo de validação é o **status de modificação**, não a comparação cega de hash. Se eu tivesse automatizado a verificação com hash, o drill teria acusado a própria defesa de falhar quando ela estava funcionando perfeitamente.\n\nO drill não é uma auditoria de processo. É um ensaio de incêndio: cada camada de detecção é exercitada de ponta a ponta, com um incidente simulado, para que a resposta real não seja a primeira vez que a equipe vê o alarme tocar.\n\n## Métricas que importam\n\n| Métrica | Valor |\n|---------|-------|\n| Cadência do red team | Semanal, automática |\n| Verificação | Passagem única (não iterativa) |\n| Escopo | Só sistemas autorizados, somente leitura |\n| Validação | Alta/crítica = confirmada; média = não reproduzida |\n| Formato do relatório | 9 seções (padrão do ecossistema) |\n| Drill de resposta | Detectou, restaurou, exit de falha simulado |\n\n## Aprendizados\n\n1. **Defesa que nunca é atacada enferruja.** Scanners passivos encontram o que você já sabe procurar. O atacante autorizado encontra o que você esqueceu que existia.\n2. **Autorização é o alicerce.** O mesmo pipeline que é um red team útil seria um malware sem o escopo documentado e a operação somente-leitura.\n3. **Falso positivo cala o alarme.** Validar severidade antes de gritar é o que mantém os alertas confiáveis — e confiáveis é o único jeito de alguém continuar ouvindo.\n4. **Teste a detecção, não só a configuração.** Uma sentinela bem configurada não prova nada até alguém mexer nela e ela acordar.\n5. **Metadado também é evidência.** O drill ensinou que restaurar um arquivo mexe no timestamp — e que a verificação certa depende do que você está comparando.\n\n## O que vem depois\n\n- **Drill mensal completo** — exercitar todas as camadas de detecção de uma vez, não só a integridade de arquivos.\n- **Varredura pesada em superfícies antes cobertas por verificação leve** — quando o alvo estiver disponível, aplicar a bateria completa.\n- **Importar findings do red team como gaps do processo corretivo** — transformar cada achado em item de backlog de segurança, não só em relatório.\n\nO cão de três cabeças não é uma ferramenta a mais na prateleira. É a diferença entre uma casa que diz estar segura e uma casa que **testa** estar segura. Uma vez por semana, ele late para a própria sombra — e quando a sombra se move de verdade, ele morde.\n\n<Terminal\n  commands={[\n    { label: 'scan', cmd: 'red-team --project <eco>' },\n    { label: 'alvo', cmd: 'red-team --target <servico>' },\n    { label: 'drill', cmd: 'red-team --drill' },\n    { label: 'validação', cmd: '# só o que se reproduz vira finding' },\n    { label: 'relatório', cmd: '# veredito + findings + próximo passo' },\n  ]}\n/>"
  }
];
