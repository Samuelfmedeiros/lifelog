// Gerado automaticamente por scripts/gen-ocultos.mjs
// NÃO editar manualmente

export default [
  {
    "path": "descobertas-os-350-links-que-apontavam-para-o-nada.mdx",
    "slug": "descobertas-os-350-links-que-apontavam-para-o-nada",
    "lang": "pt",
    "title": "Os 350 links que apontavam para o nada — o dia em que o hreflang virou ruído",
    "date": "2026-09-15 12:00:00 -03:00",
    "project": "descobertas",
    "content": "import Terminal from '../../components/TerminalWidget.astro'\n\n## Primeiro: o que é hreflang\n\nImagine um livro que existe em dois idiomas. No começo de cada capítulo, uma nota diz: \"este mesmo capítulo, em inglês, está na página X\".\n\nhreflang é essa nota dentro do HTML da página. Uma linha no cabeçalho:\n\n```html\n<link rel=\"alternate\" hreflang=\"pt\" href=\"/post/exemplo/\" />\n<link rel=\"alternate\" hreflang=\"en\" href=\"/en/post/example/\" />\n<link rel=\"alternate\" hreflang=\"x-default\" href=\"/post/exemplo/\" />\n```\n\nEla conta ao Google: \"esta página também existe em outro idioma, naquele outro endereço\". A tag não traduz nada e não redireciona ninguém. É pura declaração — uma dica para o mecanismo de busca escolher qual versão mostrar para quem. Quem busca em inglês recebe o link em inglês, quem busca em português recebe o em português. Sem a dica, o Google entrega a versão que ele achar melhor, e o visitante cai no idioma errado.\n\nO `x-default` é a terceira opção: \"se a pessoa não fala nem português nem inglês, mande para esta aqui\" — no nosso caso, a versão portuguesa, que é a padrão.\n\n## A única regra que importa\n\nPara a dica funcionar, o outro lado precisa confirmá-la.\n\nSe a página em português declara \"meu irmão em inglês mora em `/en/post/example/`\", a página em `/en/post/example/` precisa declarar de volta \"meu irmão em português mora em `/post/exemplo/`\". Isso se chama reciprocidade, e a documentação oficial do Google é direta: par sem retorno, as duas tags são ignoradas.\n\nRepare no que isso significa na prática: hreflang quebrado não aparece no console do navegador, não deixa o build vermelho, não gera alerta de nenhum tipo. O único lugar onde o problema existe é do lado do Google — onde ninguém está olhando. Um site pode passar anos \"declarando\" versões traduzidas que o buscador joga fora silenciosamente.\n\nFoi exatamente o nosso caso.\n\n## O que estava acontecendo aqui\n\nO blog gera as tags de idioma automaticamente para toda página — cada uma das mais de mil páginas do site declara a versão portuguesa, a inglesa e a padrão. Na auditoria, somaram-se 350 links nesse estado: 169 indo de português para inglês, 166 no caminho contrário, 15 `x-default`.\n\nBoa parte deles apontava para endereço que nunca existiu. Exemplo concreto: o post `2026-07-28-semana-da-qualidade-portfolio` tem, em inglês, o nome traduzido — `2026-07-28-quality-week-portfolio`. Mas o gerador de tags não sabia disso. Ele montava o endereço inglês por regra: pega o caminho português, cola `/en` na frente. Resultado: um link para `/en/2026-07-28-semana-da-qualidade-portfolio/` — página que não existe em lugar nenhum. O Google lê, não acha o outro lado, descarta o par. Centenas de vezes.\n\nA regra que gerava esses links foi escrita antes de o blog ser bilíngue de verdade. Na época havia duas ou três páginas fixas, e \"cola /en na frente\" resolvia. O blog cresceu, passou a ter um arquivo por idioma, e a premissa continuou lá, valendo como lei, sem ninguém ter decidido isso em voz alta. E a realidade tem três dobras que a regra não previa:\n\n**Nome de arquivo traduzido.** Parte dos posts nasceu com slug idêntico nos dois idiomas; outra parte veio com o título traduzido no nome do arquivo inglês. A concatenação não tem como adivinhar em qual dos dois casos ela está.\n\n**Página que só existe em um idioma.** O painel de revisão dos rascunhos ocultos é puramente português — não existe (nem precisa existir) versão em inglês. Emitir alternate dali é inventar uma página.\n\n**Alias de rota.** As páginas fixas não espelham o nome: `/arquivo` mora em `/en/archive`, `/sobre` em `/en/about`. Colar o prefixo devolve `/en/arquivo`, que dá 404.\n\n## Como funciona o pareamento de verdade\n\nAqui está o ponto que quase todo mundo (eu incluso) imagina errado: os dois idiomas **não** se reconhecem pelo endereço.\n\nCada post tem, no cabeçalho do arquivo, uma data de publicação e um projeto. O sistema que casa os idiomas usa esse par — mesma data de publicação, mesmo projeto. Se o arquivo português e o inglês batem nos dois campos, eles são irmãos, não importa o nome que cada um tenha no disco.\n\nA cura foi levar essa verdade para dentro do gerador de tags. Em vez de o layout (que só enxerga o caminho da página) adivinhar o endereço do irmão, quem sabe quem é o irmão é a página que carrega a lista de posts — ela consulta o pareamento por data + projeto e entrega o caminho certo, pronto, para o layout. O layout parou de derivar e passou a receber.\n\nDois detalhes apareceram pelo caminho.\n\nO primeiro: o prop que transporta o irmão precisa de três valores, não dois. \"A página tem irmão, o caminho é este\" (um texto). \"Esta página não tem irmão, não emita alternate\" (nulo). E \"ninguém me informou nada\" (ausente — cai na regra antiga, que funciona, para as páginas fixas). O bug de antes morava justamente em tratar os dois últimos como a mesma coisa: sem separar \"sem irmão\" de \"não informei\", o site continuava inventando link.\n\nO segundo: ordem dos ramos. Os previews dos posts ocultos têm caminho próprio (`/ocultos/preview/pt/...`). Na primeira versão da correção, o caso geral rodava antes do caso de preview e montava um inglês a partir de um português que já era composto. Sobraram uns 37 links quebrados — todos no painel de revisão, justamente o lugar do site que nunca aparece no menu. Reordenar os casos fechou, e ficou a regra: um caso que não aparece na navegação continua aparecendo no build.\n\n## Dois achados que não eram hreflang\n\nA mesma varredura esbarrou em mais duas coisas.\n\nO manifesto do site (o arquivo que diz ao celular como instalar o blog como aplicativo) pedia dois ícones, e o iPhone pedia um terceiro. Os três caminhos davam 404 desde sempre. O motivo: o `.gitignore` tinha uma linha global `*.png`, colocada para nunca commitar print de teste sem querer. O efeito colateral é que era impossível commitar os ícones sem forçar — e ninguém nunca forçou. Os arquivos simplesmente nunca existiram no repositório. Corrigido com uma exceção para a pasta (`!public/icons/*.png`) e dois ícones desenhados de verdade (192×192 e 512×512).\n\nE o vigia dos links, uma vez escrito, falhou no próprio teste: o teste de regressão navegava para uma rota que nunca existiu no projeto e tratava o 404 como defeito do site. O vigia acusava o inocente e o culpado era o vigia. Cortado para o smoke honesto, a cobertura exaustiva ficou com o script que caminha pelo build e confere, um a um, cada alternate declarado contra cada arquivo gerado.\n\n## As duas checagens que ficaram\n\nPara o problema não voltar calado, duas checagens agora rodam no pipeline:\n\n1. **Sincronia PT/EN** — compara cada post português com os ingleses pelo par data + projeto. Post novo sem tradução reprova o build com aviso explícito, antes de qualquer tag ser emitida.\n2. **Validador de alternates** — roda depois do build, lê cada HTML gerado, extrai todo `link rel=alternate` e confirma que o destino existe. Qualquer caminho inventado vira exit 1 e o deploy para. Cento e quatorze linhas, zero complacência.\n\nDetalhe técnico que quase passou: comparar caminho de arquivo com link do HTML exige decodificar os escapes de porcentagem (`%C3%AD` vira o \"í\" correspondente) e normalizar os acentos — o mesmo \"í\" pode ter dois formatos de bytes. Sem isso, o validador acusava de quebrado um link perfeitamente válido. E falso positivo ensina a desligar o guardião mais rápido que qualquer bug real.\n\n## Métricas\n\n| Item | Antes | Depois |\n|------|-------|--------|\n| Links de alternate apontando para 404 | 350 | 0 |\n| Alternate declarados no build | ~3.5k | 3.876 |\n| Ícones do manifesto existentes no pacote | 0 de 2 | 2 de 2 |\n| Página de erro indexável | sim | não |\n| Guardião rodando no CI | não existia | pós-build, bloqueante |\n| Build de prova | -- | 1.293 páginas, testes unitários 89 em 89 |\n\n## Aprendizados\n\n**A explicação vem antes da cura.** Entender o que a tag faz do lado do Google — pedir confirmação de volta — foi o que mostrou por que o nosso caminho estava errado. Toda conversa com um sistema externo (buscador, e-mail, pagamento) começa melhor pelo \"como isso funciona lá do outro lado\" do que pelo \"por que meu código está errado\".\n\n**Invariância silenciosa é a mais cara.** \"Todo português tem inglês no mesmo path\" nunca foi decidido em voz alta. Premissa que ninguém escreveu não tem dono, e é ela que vira três linhas de concatenação num layout.\n\n**Declaração sem verificação é decoração.** hreflang é um pedido, não uma ordem. O verificador de verdade fica do lado de fora, no Google. Enquanto ninguém conferisse o caminho de volta, o verde do build não significava nada.\n\n**Ausência é um valor.** \"Sem irmão\" e \"não informei\" precisaram ser separados para o sistema parar de inventar. Metade dos bugs de integração que eu arrumo são duas intenções diferentes disputando um estado só.\n\n**O validador também precisa de teste.** Escrevi o guardião e o teste que falhava por culpa dele no mesmo dia. Confiar na ferramenta sem ler a acusação dela é só terceirizar o bug.\n\n## O que vem a seguir\n\nO que fica aberto é o outro lado da moeda: um post novo pode nascer com hreflang quebrado de um jeito que nem a derivação nem o nome divergente cobrem — tradução publicada com data diferente do original. O pareamento casa pelo par exato; se a data escorregar entre os dois arquivos, o irmão some sem ninguém avisar. A próxima checagem que faz sentido escrever não é \"esse link existe\", e sim \"esse post tem irmão\".\n\n<Terminal\n  commands={[\n    { cmd: '--- o que e hreflang ---' },\n    { cmd: 'link no <head>: esta pagina tbm existe em outro idioma, ali' },\n    { cmd: 'regra: so vale se os dois lados se apontam (reciprocidade)' },\n    { cmd: 'sem retorno -> Google ignora as duas tags, e ninguem ve' },\n    { cmd: '--- o sintoma aqui ---' },\n    { cmd: '350 alternates: pt->en 169 | en->pt 166 | x-default 15' },\n    { cmd: 'gerador montava irmao = \"/en\" + caminho portugues' },\n    { cmd: '--- onde a premissa quebrava (3 dobras) ---' },\n    { cmd: '1. filename traduzido: semana-da-qualidade vs quality-week' },\n    { cmd: '2. pagina mono-idioma: painel de revisao nao tem ingles' },\n    { cmd: '3. alias: /arquivo mora em /en/archive, nao /en/arquivo' },\n    { cmd: '--- como pareia de verdade ---' },\n    { cmd: 'irmao = mesma pubDate + mesmo project (nao o endereco)' },\n    { cmd: 'quem sabe o irmao e a pagina; o layout so recebe' },\n    { cmd: 'prop com 3 valores: caminho | nulo (nao tem irmao) | ausente (deriva)' },\n    { cmd: '--- travas que ficaram ---' },\n    { cmd: 'check-lang-sync: post sem traducao reprova o build' },\n    { cmd: 'check-alternates: cada href do HTML vs arquivos do dist/' },\n    { cmd: '114 linhas, 3876 alternates, 0 quebrados, CI bloqueante' },\n  ]}\n/>"
  },
  {
    "path": "en/descobertas-os-350-links-que-apontavam-para-o-nada.mdx",
    "slug": "descobertas-os-350-links-que-apontavam-para-o-nada",
    "lang": "en",
    "title": "The 350 links pointing to nowhere — the day hreflang became noise",
    "date": "2026-09-15 12:00:00 -03:00",
    "project": "descobertas",
    "content": "import Terminal from '../../../components/TerminalWidget.astro'\n\n## First: what hreflang actually is\n\nPicture a book that exists in two languages. At the start of each chapter, a note says: \"this same chapter, in English, is on page X\".\n\nhreflang is that note inside the page's HTML. One line in the head:\n\n```html\n<link rel=\"alternate\" hreflang=\"pt\" href=\"/post/exemplo/\" />\n<link rel=\"alternate\" hreflang=\"en\" href=\"/en/post/example/\" />\n<link rel=\"alternate\" hreflang=\"x-default\" href=\"/post/exemplo/\" />\n```\n\nIt tells Google: \"this page also exists in another language, at that other address\". The tag doesn't translate anything and redirects no one. It's a pure declaration — a hint that helps the search engine pick which version to show whom. Someone searching in English gets the English link, someone searching in Portuguese gets the Portuguese one. Without the hint, Google serves whichever version it guesses, and the visitor lands in the wrong language.\n\n`x-default` is the third option: \"if the person speaks neither Portuguese nor English, send them here\" — in our case the Portuguese version, which is the default.\n\n## The one rule that matters\n\nFor the hint to work, the other side has to confirm it.\n\nIf the Portuguese page declares \"my English sibling lives at `/en/post/example/`\", then the page at `/en/post/example/` must declare back \"my Portuguese sibling lives at `/post/exemplo/`\". That's called reciprocity, and Google's official docs are blunt: a pair without a return path, both tags get ignored.\n\nNotice what that means in practice: a broken hreflang doesn't show up in the browser console, doesn't turn the build red, doesn't trigger any alert of any kind. The only place the problem exists is on Google's side — where nobody is looking. A site can spend years \"declaring\" translated versions that the search engine throws away silently.\n\nThat was exactly our case.\n\n## What was happening here\n\nThe blog generates the language tags automatically for every page — each of the site's thousand-plus pages declares the Portuguese version, the English one, and the default. The audit counted 350 links in that shape: 169 going Portuguese to English, 166 the other way, 15 `x-default`.\n\nA good share of them pointed at an address that never existed. Concrete example: the post `2026-07-28-semana-da-qualidade-portfolio` has an English file whose name is translated — `2026-07-28-quality-week-portfolio`. But the tag generator didn't know that. It built the English address by rule: take the Portuguese path, paste `/en` in front. Result: a link to `/en/2026-07-28-semana-da-qualidade-portfolio/` — a page that exists nowhere. Google reads it, finds no other side, discards the pair. Hundreds of times.\n\nThe rule generating those links was written before the blog was genuinely bilingual. Back then there were two or three fixed pages, and \"paste /en in front\" solved it. The blog grew, moved to one file per language, and the assumption stayed, enforced like a law nobody ever voted on. And reality has three folds the rule didn't foresee:\n\n**Translated filename.** Some posts were born with the same slug in both languages; others came with the title translated in the English filename. Concatenation has no way to guess which case it's in.\n\n**A page that exists in only one language.** The hidden-draft review panel is pure Portuguese — there is no English version (nor any need for one). Emitting an alternate from there is inventing a page.\n\n**Route aliases.** The fixed pages don't mirror their names: `/arquivo` lives at `/en/archive`, `/sobre` at `/en/about`. Pasting the prefix yields `/en/arquivo`, which is a 404.\n\n## How the pairing actually works\n\nHere's the point almost everyone (me included) gets wrong: the two languages **don't** recognize each other by address.\n\nEach post carries, in its frontmatter, a publication date and a project. The system that matches languages uses that pair — same pubDate, same project. If the Portuguese file and the English file match on both fields, they're siblings, no matter what each one is named on disk.\n\nThe cure was bringing that truth into the tag generator. Instead of the layout (which only sees the page's path) guessing the sibling's address, the one that knows who the sibling is turns out to be the page that loads the post collection — it queries the date + project pairing and hands the layout the right path, ready-made. The layout stopped deriving and started receiving.\n\nTwo details surfaced along the way.\n\nFirst: the prop carrying the sibling needs three values, not two. \"This page has a sibling, the path is this\" (a string). \"This page has no sibling, emit no alternate\" (null). And \"nobody told me anything\" (absent — falls back to the old rule, which works, for the fixed pages). The original bug lived precisely in treating the last two as the same thing: without separating \"no sibling\" from \"not provided\", the site keeps inventing links.\n\nSecond: branch order. Hidden-post previews have their own path shape (`/ocultos/preview/pt/...`). In the first version of the fix, the general case ran before the preview case and built an English URL out of an already-composed Portuguese one. About 37 broken links survived — all in the review panel, exactly the part of the site that never shows up in the menu. Reordering the cases closed it, and the rule stuck: a case that doesn't appear in navigation still appears in the build.\n\n## Two finds that weren't hreflang\n\nThe same sweep ran into two more things.\n\nThe site's manifest (the file that tells a phone how to install the blog as an app) asked for two icons, and the iPhone asked for a third. All three paths had 404'd since day one. The reason: the `.gitignore` had a global `*.png` line, placed there so test screenshots would never get committed. The side effect: it was impossible to commit the icons without forcing it — and nobody ever forced. The files simply never existed in the repository. Fixed with an exception for the folder (`!public/icons/*.png`) and two properly drawn icons (192×192 and 512×512).\n\nAnd the link watchdog, once written, failed its own test: the regression spec navigated to a route that never existed in the project and treated the 404 as a site defect. The watchdog accused the innocent, and the guilty one was the watchdog. Cut down to an honest smoke test, with the exhaustive coverage left to the script that walks the build and checks, one by one, every declared alternate against every generated file.\n\n## The two checks that stayed\n\nSo the problem can't come back quietly, two checks now run in the pipeline:\n\n1. **PT/EN sync** — compares each Portuguese post against the English ones by the date + project pair. A new post without a translation fails the build with an explicit warning, before any tag is emitted.\n2. **Alternate validator** — runs after the build, reads every generated HTML, extracts every `link rel=alternate`, and confirms the destination exists. Any invented path becomes exit 1 and the deploy stops. A hundred and fourteen lines, zero leniency.\n\nA technical detail that almost slipped: comparing a filesystem path against an HTML link requires decoding the percent-escapes (`%C3%AD` becomes the matching accented letter) and normalizing accents — the same \"í\" can exist in two byte shapes. Without that, the validator flagged perfectly valid links as broken. And a false positive teaches you to disable the guardian faster than any real bug would.\n\n## Metrics\n\n| Item | Before | After |\n|------|--------|-------|\n| Alternate links pointing to 404 | 350 | 0 |\n| Alternates declared in the build | ~3.5k | 3,876 |\n| Manifest icons present in the bundle | 0 of 2 | 2 of 2 |\n| Error page indexable | yes | no |\n| Guardian running in CI | didn't exist | post-build, blocking |\n| Proof build | -- | 1,293 pages, unit tests 89 of 89 |\n\n## Lessons\n\n**The explanation comes before the cure.** Understanding what the tag does on Google's side — asking for a confirmation back — is what showed why our path was wrong. Every conversation with an external system (search, email, payments) starts better with \"how does this work over there\" than with \"why is my code broken\".\n\n**Silent invariants are the most expensive.** \"Every Portuguese page has English at the same path\" was never decided out loud. An assumption nobody wrote down has no owner, and it's the one that becomes three lines of concatenation in a layout.\n\n**A declaration without verification is decoration.** hreflang is a request, not an order. The real verifier lives outside, at Google. Until someone confirmed the return path, a green build meant nothing.\n\n**Absence is a value.** \"No sibling\" and \"not provided\" had to be separated for the system to stop inventing. Half of the integration bugs I fix are two different intentions sharing one state.\n\n**The validator needs tests too.** I wrote the guardian and the spec that failed through its own fault on the same day. Trusting a tool without reading its accusation is just outsourcing the bug.\n\n## What comes next\n\nWhat stays open is the other side of the coin: a new post can be born with broken hreflang in a way that neither the derivation nor the divergent name covers — a translation published with a different date than the original. The pairing matches on the exact pair; if the date slips between the two files, the sibling vanishes with no warning. The next check that makes sense to write isn't \"does this link exist\" — it's \"does this post have a sibling\".\n\n<Terminal\n  commands={[\n    { cmd: '--- what hreflang is ---' },\n    { cmd: 'a <head> link: this page also exists in another language, there' },\n    { cmd: 'rule: only counts if both sides point at each other (reciprocity)' },\n    { cmd: 'no return path -> Google ignores both tags, and nobody sees it' },\n    { cmd: '--- the symptom here ---' },\n    { cmd: '350 alternates: pt->en 169 | en->pt 166 | x-default 15' },\n    { cmd: 'generator built sibling = \"/en\" + portuguese path' },\n    { cmd: '--- where the premise broke (3 folds) ---' },\n    { cmd: '1. translated filename: semana-da-qualidade vs quality-week' },\n    { cmd: '2. single-language page: review panel has no english' },\n    { cmd: '3. alias: /arquivo lives at /en/archive, not /en/arquivo' },\n    { cmd: '--- how pairing really works ---' },\n    { cmd: 'sibling = same pubDate + same project (not the address)' },\n    { cmd: 'the page knows the sibling; the layout just receives' },\n    { cmd: 'prop with 3 values: path | null (no sibling) | absent (derive)' },\n    { cmd: '--- locks that stayed ---' },\n    { cmd: 'check-lang-sync: post without translation fails the build' },\n    { cmd: 'check-alternates: every HTML href vs dist/ files' },\n    { cmd: '114 lines, 3876 alternates, 0 broken, blocking CI' },\n  ]}\n/>"
  }
];
