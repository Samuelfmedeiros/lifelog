// Gerado automaticamente por scripts/gen-ocultos.mjs
// NÃO editar manualmente

export default [
  {
    "path": "en/en-lifelog-o-ciclo-do-nao.mdx",
    "slug": "en-lifelog-o-ciclo-do-nao",
    "lang": "en",
    "title": "The no loop — when every reader rejection became a ticket in the pipeline",
    "date": "2026-09-06 08:00:00 -03:00",
    "project": "lifelog",
    "content": "import Terminal from '../../../components/TerminalWidget.astro'\n\n## The day silence ate a request\n\nThis blog publishes daily, and every post is born hidden. I read, decide what goes live, and when something doesn't sit right with me, the system logs my rejection into a file inside the repository. Simple, traceable, quiet. For months this worked — until I discovered that \"working\" and \"being read\" are different things.\n\nThe rejections file grew. Related issues stayed open. Nothing complained, because everything was \"recorded\". But a record without notification is a message in a bottle: the content is intact, the destination is what's missing. A rewrite request sat idle for days while the pipeline kept its rhythm, happy, unaware it owed an answer.\n\n## Why a file isn't enough\n\nA rejection had three possible destinations and the implementation got two right: a traceable comment in the repository and a physical copy under `docs/recusas/`. What was missing was the link that turns a record into a task — someone (or something) that looks at that file and says \"this needs a response\". The blog had the memory of the request, but no system with a mandate to act on it.\n\nThe fix wasn't creating yet another place to write things down. It was closing the loop: now, when a rejection is recorded, the matching pipeline gets notified and the post returns to the review queue — with the original reason attached, intact, in the same place it was born. The file stayed the source of truth; what changed was who reads it and when.\n\n```js\n// Before: rejection recorded, period.\nawait commitNotaFile(slug, nota);        // file in the repo\nawait createIssue(slug, nota);           // traceable issue\n// ...and the pipeline? The pipeline found out when (or if) someone remembered.\n\n// After: rejection recorded + automatic follow-up\nawait commitNotaFile(slug, nota);\nawait createIssue(slug, nota);\nawait notifyPipeline(slug, nota);        // the loop closes itself\n```\n\n## What the loop teaches about feedback\n\nA system that accepts rejection without reacting teaches the user to stop rejecting. Worse: it teaches that rejecting is pointless. The button existed, worked, saved everything — and still the loop was broken, because whoever receives the feedback was never told they had received it.\n\nThe fix reminded me of a rule I apply in code and forget in processes: **every input needs a named consumer**. Not \"someone will see it eventually\", but a specific component, with a specific trigger, reacting to a specific event. Without that, the input is just archiving with good posture.\n\n| | Rejection as a file | Rejection as a ticket |\n|---|---|---|\n| Record | traceable | traceable |\n| Who reads it | whoever remembers | the pipeline, always |\n| Response time | undefined | one cycle |\n| Silence possible | yes, and common | no |\n\n## The takeaway\n\nThe automation I'm proud of isn't the one that publishes posts by itself. It's the one that guarantees **none of my requests disappear**. If I reject a post, that \"no\" has a path: file, issue, notification, rewrite, re-delivery. The blog became a system that listens and answers — and if the loop ever breaks again, there is now a part of the process whose only job is to notice.\n\n<Terminal\n  commands={[\n    { label: 'origin', cmd: 'POST /api/recusar  {\"path\": \"<slug>\", \"nota\": \"...\"}' },\n    { label: 'record', cmd: 'traceable issue + docs/recusas/<slug>.md' },\n    { label: 'follow-up', cmd: 'pipeline notified -> rewrites -> re-delivers hidden' },\n    { label: 'closure', cmd: 'issue closed when the new version enters the flow' },\n  ]}\n/>"
  },
  {
    "path": "en/portfolio-o-rastro-que-sobrou.mdx",
    "slug": "portfolio-o-rastro-que-sobrou",
    "lang": "en",
    "title": "What the trail left behind — immutable cache, a Tuesday revert, and the return of V5",
    "date": "2026-09-11 12:00:00 -03:00",
    "project": "portfolio",
    "content": "import Terminal from '../../../components/TerminalWidget.astro'\n\n## Tuesday, a tracker that never ages\n\nToday's post is not about a bug. It's about what's left when three days of maintenance pass through the same repository — and what each change left behind.\n\nIt starts on Tuesday (09/10). The portfolio uses self-hosted analytics, and the integration was a single line in the layout: a `<script>` tag pointing to the tracker. It worked. Except when it didn't — I had updated the script on the server and visitors' browsers kept running the old version, sometimes for days.\n\nThe diagnosis fit in one header line:\n\n```http\ncache-control: public, max-age=31536000, immutable\n```\n\nOne year of caching. Immutable. On the **analytics script**.\n\nImmutable is the promise you make to the browser: \"this URL's content will never change\". It's perfect for bundles with a hash in the name — `/_next/static/chunk-abc123.js` gets a new name when its content changes, so you can freeze its cache without fear. The problem shows up when the same promise covers a file that **changes without changing its name**. The browser does the exact opposite of what you want: when you fix the tracker, it politely ignores the fix until the cache expires.\n\nThe fix was small to write and conceptual to understand:\n\n```tsx\n<script src=\"/analytics/script.js?v=2\" />\n```\n\nQuery string on the URL. The `?v=2` changes nothing for the server — but it changes everything for the cache. New URL, new cache entry. It's the same trick I had already learned the hard way with this blog's cover images: **replacing content at the same URL is invisible to anyone who already loaded it**. Immutable asset + new content = a fix only you can see.\n\n## Friday before dawn, the revert that came from outside\n\nFriday (09/05), 03:55 AM. A commit lands in the repository — from another machine. \"Revert Merge PR #72\": the resume tailor, the tool that analyzes a résumé against a job opening with AI, had been removed from the tree entirely. Route, components, lib — gone.\n\nThat same Friday I merged the revert, fixed a duplicated copyright in the footer that showed up along the way, and the day closed with three commits. The project moved on without V5. No drama: the revert existed because the feature had been deactivated by an earlier decision, and the merge just consolidated that state.\n\nBut every reverted feature leaves a trail: the tests that covered it, the patterns it established, the question \"what if we bring it back?\". The revert doesn't answer — it only postpones.\n\n## The return, now with an LLM in the middle of the path\n\nMonday (09/09) was a dependabot integration day — nine dev-dependency PRs, from vercel 58.9 to 59.11, vitest 4 to 5, framer-motion, mercadopago. The kind of day that doesn't become a post because \"updated dependencies\" has no conflict, only discipline.\n\nAnd then yesterday (09/10, late afternoon), V5 came back. Single commit, honest message:\n\n```bash\n$ git log -1 --format=%B\nfeat(resume-tailor): V5 reintroduced with LLM chain on the 9router\n(via Arachne) and fail-loud behavior\n\n- Route now uses the Arachne /api/llm/chat (9router behind it) with a\n  direct RESUME_LLM_URL fallback for dev; end of the hardcoded free\n  model race.\n- Unavailable LLM now responds 502 (before: placeholder PDF with\n  HTTP 200).\n- Anti-echo guard for the prompt template (looksLikePromptEcho) +\n  tests.\n- Reintroduces the V5 files removed by revert 4dd1a99.\n```\n\nThree design decisions are worth recording.\n\n**First: the LLM chain left the portfolio's code.** The previous version kept a list of free models with cascading fallbacks — each one could vanish, and the fallback list turned into a holiday wish list. Now the route talks to the LLM gateway that already runs in my ecosystem (which in turn orchestrates providers, rate limits and rotation), with a direct URL fallback only for the local environment. The portfolio no longer knows which models exist — and that's how it should be.\n\n**Second: fail loud.** The old version had a behavior I now consider a crime: when the LLM didn't respond, it generated a placeholder PDF and answered HTTP 200. The user got an empty document and thought that was it. Now: 502. Explicit error, no disguise.\n\n**Third: the anti-echo guard.** LLMs sometimes \"answer\" by repeating the prompt itself. Detectable — the output looks like the template, not like an analysis. A simple detector (with tests) rejects that echo before it becomes a PDF.\n\nThe revert and the reintroduction don't cancel each other out: the first one cleaned up, the second came back with a different architecture. If V5 had returned exactly the same, the revert would have been pointless.\n\n<Terminal\n  client:visible\n  title=\"samuel@dev:~/portfolio\"\n  commands={[\n    { cmd: 'git log --oneline -1' },\n    { cmd: '# feat(resume-tailor): V5 reintroduced with LLM chain on the 9router' },\n    { cmd: 'curl -s -X POST /api/resume-tailor -F cv=@resume.pdf -F job=@opening.txt' },\n    { cmd: '# HTTP 502 (llm down) — no placeholder PDF' },\n    { cmd: 'curl -s -X POST /api/resume-tailor -F cv=@resume.pdf -F job=@opening.txt' },\n    { cmd: '# HTTP 200 — PDF generated, anti-echo guard ok' },\n  ]}\n/>\n\n## What remains\n\nTuesday closed with a single commit — the tracker's cache-bust. A light day, the kind the end-of-day routine documents and nobody mentions again. But the three days tell the same story from different angles:\n\n| Day | Change | The trail |\n|-----|--------|-----------|\n| 09/05 | V5 revert + footer fix | Single copyright, open question |\n| 09/09 | 9 dependabot PRs | Dev deps updated, zero drama |\n| 09/10 | Cache-bust `?v=2` | Tracker updates when it should |\n| 09/10 | V5 reintroduced | LLM behind a gateway, honest failure |\n\nImmutable caching taught: **immutability is a promise about content, and it only holds when the name carries that promise too**. The revert taught: undoing is cheaper than living with the wrong thing. And the reintroduction taught: when a feature comes back, it should come back better — if it returns identical, the interval taught nothing.\n\nThe next blog post that cites a new asset will ship `?v=` in the URL from the very commit. I learned it the painful way twice — cover and tracker — so I never forget it again."
  },
  {
    "path": "lifelog-o-ciclo-do-nao.mdx",
    "slug": "lifelog-o-ciclo-do-nao",
    "lang": "pt",
    "title": "O ciclo do não — quando cada recusa do leitor virou um ticket no pipeline",
    "date": "2026-09-06 08:00:00 -03:00",
    "project": "lifelog",
    "content": "import Terminal from '../../components/TerminalWidget.astro'\n\n## O dia em que o silêncio comeu um pedido\n\nO blog publica todos os dias, e cada post nasce escondido. Eu leio, decido o que vai pro ar, e quando algo não me agrada, o sistema anota minha recusa num arquivo dentro do repositório. Simples, rastreável, quieto. Por meses isso funcionou — até descobrir que \"funcionar\" e \"ser lido\" são coisas diferentes.\n\nO arquivo de recusas crescia. Issues relacionadas ficavam abertas. Nada reclamava, porque tudo estava \"registrado\". Só que registro sem notificação é mensagem numa garrafa: o conteúdo está intacto, o destino é que não existe. Um pedido de reescrita ficou dias parado enquanto o pipeline seguia seu ritmo, feliz, sem saber que devia algo.\n\n## Por que um arquivo não basta\n\nA recusa tinha três destinos possíveis e a implementação acertou dois: um comentário rastreável no repositório e uma cópia física em `docs/recusas/`. O que faltava era o elo que transforma registro em tarefa — alguém (ou algo) que olhe aquele arquivo e diga \"isto precisa de resposta\". O blog tinha a memória do pedido, mas nenhum sistema com mandato de agir sobre ela.\n\nA correção não foi criar mais um lugar pra escrever. Foi fechar o ciclo: agora, quando uma recusa é registrada, o pipeline correspondente é notificado e o post volta pro fluxo de revisão — com o motivo original anexado, intacto, no mesmo lugar onde nasceu. O arquivo continuou sendo a fonte de verdade; o que mudou foi quem o lê e quando.\n\n```js\n// Antes: recusa registrada, e só.\nawait commitNotaFile(slug, nota);        // arquivo no repo\nawait createIssue(slug, nota);           // issue rastreável\n// ...e o pipeline? O pipeline descobria quando (ou se) alguém lembrava.\n\n// Depois: recusa registrada + cobrança automática\nawait commitNotaFile(slug, nota);\nawait createIssue(slug, nota);\nawait notifyPipeline(slug, nota);        // o ciclo fecha sozinho\n```\n\n## O que o ciclo ensina sobre feedback\n\nUm sistema que aceita rejeição sem reagir ensina o usuário a parar de rejeitar. Pior: ensina que recusar é inútil. O botão existia, funcionava, salvava tudo — e ainda assim o loop estava quebrado, porque quem recebe o feedback nunca foi avisado de que tinha recebido.\n\nO conserto me lembrou de uma regra que aplico no código e esqueço nos processos: **toda entrada precisa de um consumidor nomeado**. Não \"alguém vai ver eventualmente\", mas um componente específico, com gatilho específico, reagindo a um evento específico. Sem isso, a entrada é só arquivamento com passo firme.\n\n| | Recusa como arquivo | Recusa como ticket |\n|---|---|---|\n| Registro | rastreável | rastreável |\n| Quem lê | quem lembrar | o pipeline, sempre |\n| Tempo de resposta | indefinido | um ciclo |\n| Silêncio possível | sim, e comum | não |\n\n## O aprendizado\n\nA automação que me orgulho não é a que publica posts sozinha. É a que garante que **nenhum pedido meu desaparece**. Se eu recuso um post, aquele \"não\" tem um caminho: arquivo, issue, notificação, reescrita, re-entrega. O blog virou um sistema que ouve e responde — e se um dia o ciclo quebrar de novo, agora existe uma parte do processo cujo único trabalho é perceber isso.\n\n<Terminal\n  commands={[\n    { label: 'origem', cmd: 'POST /api/recusar  {\"path\": \"<slug>\", \"nota\": \"...\"}' },\n    { label: 'registro', cmd: 'issue rastreável + docs/recusas/<slug>.md' },\n    { label: 'cobrança', cmd: 'pipeline notificado -> reescreve -> re-entrega oculta' },\n    { label: 'fechamento', cmd: 'issue fechada quando a nova versão entra no fluxo' },\n  ]}\n/>"
  },
  {
    "path": "portfolio-o-rastro-que-sobrou.mdx",
    "slug": "portfolio-o-rastro-que-sobrou",
    "lang": "pt",
    "title": "O rastro que sobrou — cache imutável, revert de terça e a volta do V5",
    "date": "2026-09-11 12:00:00 -03:00",
    "project": "portfolio",
    "content": "import Terminal from '../../components/TerminalWidget.astro'\n\n## Terça-feira, um tracker que não envelhece\n\nO post de hoje não é sobre um bug. É sobre o que sobra quando três dias de manutenção passam pelo mesmo repositório — e o que cada mudança deixou pra trás.\n\nComeça na terça (10/09). O portfólio usa um analytics self-hosted, e a integração era uma linha no layout: um `<script>` apontando pro tracker. Funcionava. Exceto quando não funcionava — eu tinha atualizado o script lá no servidor e o navegador dos visitantes continuava rodando a versão antiga, às vezes por dias.\n\nO diagnóstico foi uma linha de header:\n\n```http\ncache-control: public, max-age=31536000, immutable\n```\n\nUm ano de cache. Imutável. No **script** de analytics.\n\nImmutable é a promessa que você dá ao navegador: \"este URL nunca vai mudar de conteúdo\". É perfeito pra bundles com hash no nome — `/_next/static/chunk-abc123.js` muda de nome quando muda de conteúdo, então pode congelar o cache sem medo. O problema é quando a mesma promessa cobre um arquivo que **muda sem trocar de nome**. O navegador faz o oposto do que você quer: quando você conserta o tracker, ele educadamente ignora a correção até o cache expirar.\n\nA correção foi pequena de escrever e conceitual de entender:\n\n```tsx\n<script src=\"/analytics/script.js?v=2\" />\n```\n\nQuery string no URL. O `?v=2` não muda nada pro servidor — mas muda tudo pro cache. URL novo, entrada de cache nova. É o mesmo truque que eu já tinha aprendido na pele com as capas do blog: **trocar conteúdo no mesmo URL é invisível pra quem já carregou**. Asset imutável + conteúdo novo = correção que só você vê.\n\n## Madrugada de sexta, o revert que veio de fora\n\nSexta-feira (05/09), 03:55. Um commit chega no repositório — de outra máquina. \"Revert Merge PR #72\": o resume tailor, a ferramenta que analisa um currículo contra uma vaga com IA, tinha sido removido inteiro da árvore. Route, componentes, lib — fora.\n\nNa sexta mesma eu fiz o merge daquele revert, consertei um copyright duplicado no rodapé que apareceu no caminho, e o dia fechou com três commits. O projeto seguiu sem o V5. Nenhum drama: o revert existia porque a feature tinha sido desativada por decisão anterior, e o merge só consolidou o estado.\n\nMas toda feature revertida deixa um rastro: os testes que cobriam ela, os padrões que ela estabeleceu, a pergunta \"e se a gente trouxer de volta?\". O revert não responde — só adia.\n\n## A volta, agora com um LLM no meio do caminho\n\nSegunda (09/09) foi dia de integração massiva de dependabot — nove PRs de dev-dependencies, do vercel 58.9 pro 59.11, vitest 4 pro 5, framer-motion, mercadopago. O tipo de dia que não vira post porque \"atualizou dependências\" não tem conflito, tem só disciplina.\n\nE então ontem (10/09, tarde), o V5 voltou. Commit único, mensagem sincera:\n\n```bash\n$ git log -1 --format=%B\nfeat(resume-tailor): V5 reintroduzido com cadeia LLM no 9router\n(via Arachne) e falha alta\n\n- Rota usa /api/llm/chat do Arachne (9router por tras) com fallback\n  direto em RESUME_LLM_URL para dev; fim da corrida de modelos free\n  hardcoded.\n- LLM indisponivel agora responde 502 (antes: PDF placeholder com\n  HTTP 200).\n- Guard anti-eco do template do prompt (looksLikePromptEcho) + testes.\n- Reintroduz os arquivos do V5 removidos pelo revert 4dd1a99.\n```\n\nTrês decisões de design valem o registro.\n\n**Primeira: a cadeia de LLM saiu do código do portfólio.** A versão anterior mantinha uma lista de modelos gratuitos com fallback em cascula — cada um podia sumir, e o fallback virava lista de Natal. Agora a rota fala com o gateway de LLM que já roda no meu ecossistema (que por sua vez orquestra provedores, rate limit e rotação), com um fallback de URL direto só pro ambiente local. O portfólio não sabe mais quais modelos existem — e é assim que deve ser.\n\n**Segunda: falhar alto.** A versão antiga tinha um comportamento que eu considero crime hoje: quando o LLM não respondia, ela gerava um PDF placeholder e respondia HTTP 200. O usuário recebia um documento vazio e achou que era aquilo. Agora: 502. Erro explícito, sem disfarce.\n\n**Terceira: o guard anti-eco.** LLM às vezes \"responde\" repetindo o próprio prompt. Detectável — o output parece com o template, não com uma análise. Um detector simples (com testes) rejeita esse eco antes de virar PDF.\n\nO revert e a reintrodução não se cancelam: o primeiro limpou, o segundo voltou com arquitetura diferente. Se o V5 tivesse voltado igualzinho, o revert não teria servido pra nada.\n\n<Terminal\n  client:visible\n  title=\"samuel@dev:~/portifolio\"\n  commands={[\n    { cmd: 'git log --oneline -1' },\n    { cmd: '# feat(resume-tailor): V5 reintroduzido com cadeia LLM no 9router' },\n    { cmd: 'curl -s -X POST /api/resume-tailor -F cv=@curriculo.pdf -F job=@vaga.txt' },\n    { cmd: '# HTTP 502 (llm down) — sem PDF placeholder' },\n    { cmd: 'curl -s -X POST /api/resume-tailor -F cv=@curriculo.pdf -F job=@vaga.txt' },\n    { cmd: '# HTTP 200 — PDF gerado, guard anti-eco ok' },\n  ]}\n/>\n\n## O que sobra\n\nTerça fechou com um commit só — o cache-bust do tracker. Dia leve, do tipo que a rotina de fim de dia documenta e ninguém mais menciona. Mas os três dias contam a mesma história de lados diferentes:\n\n| Dia | Mudança | O rastro |\n|-----|---------|----------|\n| 05/09 | Revert do V5 + fix footer | Copyright único, pergunta em aberto |\n| 09/09 | 9 PRs dependabot | Deps dev atualizadas, zero drama |\n| 10/09 | Cache-bust `?v=2` | Tracker atualiza quando deveria |\n| 10/09 | V5 reintroduzido | LLM atrás de gateway, falha honesta |\n\nCache imutável ensinou: **a imutabilidade é uma promessa sobre o conteúdo, e só vale quando o nome carrega essa promessa junto**. O revert ensinou: desfazer é mais barato que conviver com o errado. E a reintrodução ensinou: quando uma feature volta, ela deve voltar melhor — se for idêntica, o intervalo não ensinou nada.\n\nO próximo post do blog que citar um asset novo vai levar `?v=` no URL desde o commit. Aprendi na dor duas vezes — capa e tracker — pra nunca mais esquecer uma."
  }
];
