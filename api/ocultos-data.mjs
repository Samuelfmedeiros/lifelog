// Gerado automaticamente por scripts/gen-ocultos.mjs
// NÃO editar manualmente

export default [
  {
    "path": "dogwalk-o-botao-que-dizia-estar-online-e-nao-fazia-nada.mdx",
    "slug": "dogwalk-o-botao-que-dizia-estar-online-e-nao-fazia-nada",
    "lang": "pt",
    "title": "O botão que dizia estar online — e não fazia nada",
    "date": "2026-09-02 12:00:00 -03:00",
    "project": "dogwalk",
    "content": "import Terminal from '../../components/TerminalWidget.astro'\n\n## O botão perfeito\n\nO botão parecia funcionar. Tinha o texto certo, a cor certa, a animação certa. Clicava, mudava o visual, dava a sensação de controle. O walker \"Ficava Online\" e \"Ficava Offline\" — pelo menos na superfície.\n\nO problema é que o handler era uma função vazia.\n\n`onToggleStatus={() => {}}` — um callback que não chamava nada. O estado do walker estava fixo em `online` dentro do hook, então o clique era puramente cosmético: a UI simulava a mudança, o estado ignorava o clique.\n\nUm botão que mente é pior que um botão quebrado. O quebrado você conserta; o que mente destrói confiança aos poucos, porque quem usa começa a duvidar de todos os outros controles da tela.\n\n## Como o teste pegou\n\nO botão não foi achado por inspeção visual. Foi pego pelo loop de testes: o critério de aceite da tela do walker exigia que o toggle mudasse o estado online↔offline e o teste abriu o componente, clicou, e o estado não mudou. Um teste determinístico olhando para o que o botão *faz*, não para o que ele *mostra*.\n\nO achado virou o fix #1 de uma rodada que encontrou três problemas no total:\n\n<Terminal command=\"git log --oneline 508901fb^..508901fb\" output=\"fix #1: toggle Ficar Online/Offline liga no setWalkerStatus (estado real)\nfix #2: handleAcceptRequest agora tem try/catch (erro propagava sem feedback)\nfix #3: PricingSection era dead code — documentado como decisão\" />\n\n## A correção\n\nO handler passou a chamar o `setWalkerStatus` exposto pelo `useWalkerDashboard` — o mesmo estado que alimenta o painel inteiro. O clique deixou de ser maquiagem e passou a ser a fonte de verdade: cor do botão, dot de status e tudo que depende do estado reagiu ao clique real.\n\nNa sequência, o status online ganhou persistência (parte 3.1): não bastava o botão funcionar na sessão — o estado precisava sobreviver a reload e refletir o que o servidor sabe. Botão honesto e estado persistente andam juntos; o primeiro sem o segundo é só uma animação mais convincente.\n\n## O que fica\n\n- **Handler vazio é bug, não placeholder.** Um callback `() => {}` em produção não é \"a implementar depois\" — é um botão fantasma esperando alguém clicar.\n- **Teste de aceite olha comportamento, não visual.** O botão passava no olho e falhava na lógica; só o teste pegou.\n- **Estado duplicado cria mentira.** O estado fixo no hook e o visual animado eram duas versões da verdade. O fix unificou as duas no `setWalkerStatus`.\n\nO teste de loop rodou, achou, corrigiu e documentou. E o botão, finalmente, passou a fazer o que sempre disse que fazia."
  },
  {
    "path": "en/dogwalk-o-botao-que-dizia-estar-online-e-nao-fazia-nada.mdx",
    "slug": "dogwalk-o-botao-que-dizia-estar-online-e-nao-fazia-nada",
    "lang": "en",
    "title": "The button that said it was online — and did nothing",
    "date": "2026-09-02 12:00:00 -03:00",
    "project": "dogwalk",
    "content": "import Terminal from '../../../components/TerminalWidget.astro'\n\n## The perfect button\n\nThe button looked like it worked. Right text, right color, right animation. Click it, the visual changed, giving the feeling of control. The walker \"Went Online\" and \"Went Offline\" — at least on the surface.\n\nThe problem was that the handler was an empty function.\n\n`onToggleStatus={() => {}}` — a callback that called nothing. The walker state was hardcoded to `online` inside the hook, so the click was purely cosmetic: the UI simulated the change, the state ignored the click.\n\nA button that lies is worse than a broken button. You fix the broken one; the lying one destroys trust slowly, because whoever uses it starts doubting every other control on the screen.\n\n## How the test caught it\n\nThe button wasn't found by visual inspection. It was caught by the test loop: the walker screen's acceptance criteria required the toggle to actually change the online↔offline state, and the test opened the component, clicked, and the state didn't change. A deterministic test looking at what the button *does*, not what it *shows*.\n\nThe finding became fix #1 in a round that found three problems total:\n\n<Terminal command=\"git log --oneline 508901fb^..508901fb\" output=\"fix #1: toggle Go Online/Offline calls setWalkerStatus (real state)\nfix #2: handleAcceptRequest now has try/catch (error propagated without feedback)\nfix #3: PricingSection was dead code — documented as decision\" />\n\n## The fix\n\nThe handler started calling `setWalkerStatus` exposed by `useWalkerDashboard` — the same state that feeds the entire panel. The click stopped being makeup and became the source of truth: button color, status dot, and everything depending on the state reacted to the real click.\n\nAfter that, the online status gained persistence (part 3.1): it wasn't enough for the button to work in-session — the state needed to survive reload and reflect what the server knows. An honest button and persistent state go together; the first without the second is just a more convincing animation.\n\n## What stays\n\n- **Empty handler is a bug, not a placeholder.** A `() => {}` callback in production isn't \"to be implemented later\" — it's a ghost button waiting for someone to click.\n- **Acceptance tests look at behavior, not visuals.** The button passed the eye test and failed the logic test; only the test caught it.\n- **Duplicate state creates lies.** The hardcoded state in the hook and the animated visual were two versions of truth. The fix unified both through `setWalkerStatus`.\n\nThe test loop ran, found, fixed, and documented. And the button finally started doing what it always said it did."
  },
  {
    "path": "en/estudos-o-dia-em-que-a-loss-parou-de-aprender.mdx",
    "slug": "estudos-o-dia-em-que-a-loss-parou-de-aprender",
    "lang": "en",
    "title": "The day the loss stopped learning",
    "date": "2026-09-02 08:00:00 -03:00",
    "project": "estudos",
    "content": "import Terminal from '../../../components/TerminalWidget.astro'\n\n## The silent symptom\n\nThe training run started fine. Logs went up, batches passed, the GPU got warm. And the loss didn't go down.\n\nNot really down: it sat glued at 10.8125 and refused to move. The model generated text, but it was blank-model text — that statistical noise of something that hasn't learned anything yet. Three hundred steps later, nothing had changed.\n\nThe first suspect was the classic one: too low a learning rate. 1e-6 for pretraining a large model is overly conservative — I've fallen into that trap before, turning the LR down until training \"stabilized\", only to find that stability and stagnation are the same thing seen from different angles.\n\nBut the log had a clue the low LR couldn't explain.\n\n## The clue: a loss of 96 on the first step\n\nThe starting checkpoint wasn't just any model. The initial loss was 96 — not 10, not 11: 96. Numbers like that don't show up in a healthy model; they show up when the weights are exploded.\n\nI went to inspect the weight distribution of the `lm_head` — the layer that translates the internal representation into probabilities over the vocabulary. The standard deviation was 13.87. For an output head, that's alarming: healthy weights sit in the tenths range, not the tens.\n\nWhat happened became clear: those three hundred steps weren't learning. They were *normalizing*. The model spent the entire run trying to undo the checkpoint's damage — and when it finally reached 10.82, which is exactly the logarithm of the vocabulary size, it had become a blank model all over again.\n\n## The math that explains everything\n\nThe loss of a model that guesses uniformly over the vocabulary is `ln(vocab_size)`. With a vocabulary of 50,257 tokens, that gives:\n\n```python\nimport math\nmath.log(50257)  # 10.826...\n```\n\n10.8125 is the floor. It's the loss of something that has learned nothing. The model wasn't stuck at a bad plateau — it had *returned to zero* after spending the entire run fixing broken weights.\n\nAnd the worst part: at an LR of 1e-6, it was never going to leave. For pretraining at this scale, the right learning rate is on the order of 3e-4 — three hundred times larger.\n\n## The trap of the \"good\" checkpoint\n\nThe cruel detail: the checkpoint had gone through a head warmup phase and wore the name \"best\" in the file tree. A checkpoint named best, with exploded weights and a starting loss of 96. The naming lied, and trusting it cost three hundred steps of GPU.\n\nThe lesson is the question I should have asked before relaunching: what is the loss of the first batch? If the first value is already absurd, it's not a learning problem — it's a starting problem. No learning rate fixes exploded weights; the whole run becomes rehabilitation.\n\n<Terminal command=\"python3 -u train.py --resume step_0300 --lr 3e-4 --max-steps 500\" output=\"loss 10.82 -> 9.91 -> 8.47 ... finally learning\" />\n\n## What changed\n\nThe relaunch used the clean checkpoint as its base — the normalized weights became the real starting point — and the learning rate correct for the scale. The frozen run finally started actually learning.\n\nIt also surfaced the second bug, hidden in the resume logic: a step condition that only worked when training started from zero. With a resume at 300 and gradient accumulation of 16, the step counter entered a loop that never matched the expected multiple — GPU at 0%, frozen log, training stalled with no visible error. The fix swapped the count for the real accumulator.\n\nDiagnosing a run that won't learn is almost always the same story: either the starting point is rotten, or the rate is wrong, or the resume is lying. This time it was all three."
  },
  {
    "path": "en/portfolio-o-typewriter-que-apagava-o-proprio-texto.mdx",
    "slug": "portfolio-o-typewriter-que-apagava-o-proprio-texto",
    "lang": "en",
    "title": "The typewriter that erased its own text",
    "date": "2026-09-02 16:00:00 -03:00",
    "project": "portfolio",
    "content": "import Terminal from '../../../components/TerminalWidget.astro'\n\n## The effect nobody asked for\n\nThe portfolio hero had a typewriter — the effect where text types itself, letter by letter. Elegant, even. The problem: on some loads it put on a full show — typed the first sentence, erased everything, and started over.\n\nThat wasn't charm. It was a bug.\n\nThe sequence gave away the cause: the text was already on screen (static HTML), then React mounted the component on top, reset the state, and the typewriter restarted from the beginning. The user watched the text blink and rewind — exactly when LCP was being measured.\n\n## The culprit: deferred hydration in the wrong place\n\nThe component had been \"optimized\" with `requestIdleCallback`: it only hydrated when the browser was idle. The intent was good — taking work off the critical path. But the typewriter is precisely the component that can't be hydrated later, because its hydration is *destructive*: mounting on top resets the text.\n\nThe optimization improved one metric while hurting the experience.\n\n<Terminal command=\"git log --oneline -3\" output=\"520270c perf(typewriter): remove idle-hydration that erased and retyped text\n01c2704 perf(idle-hydration): pure rIC, readyState no longer fires (root cause)\n7edb55d perf(cockpit): idle-hydration takes GSAP+canvas off critical path\" />\n\n## The fix\n\nTwo separate decisions:\n\n1. **The typewriter went back to hydrating immediately.** Text already on screen must not be erased by hydration. The effect stayed server-rendered and stable, with JS only taking over without resetting state.\n\n2. **Idle-hydration stayed only where it makes sense:** in the background (CockpitBackground), where the delay is invisible. GSAP + canvas left the critical path without anyone noticing — there, deferral is pure gain.\n\nThe result: LCP dropped from ~3.6s to ~2.7s, and the hero text stopped \"blinking\" on load.\n\n## The lesson\n\n**Not every performance optimization is right for every component.** `requestIdleCallback` is great for work that can wait; it's terrible for work that destroys what's already rendered. The right question before deferring any hydration: *what visibly happens when this component mounts on top?* If the answer is \"it erases something\", don't defer — or your users will watch your hero self-destruct every time they open the page.\n\nThe metric genuinely improved, and not on the back of \"the page seemed faster but blinked\". A performance gain that degrades experience isn't an improvement — it's a trade."
  },
  {
    "path": "estudos-o-dia-em-que-a-loss-parou-de-aprender.mdx",
    "slug": "estudos-o-dia-em-que-a-loss-parou-de-aprender",
    "lang": "pt",
    "title": "O dia em que a loss parou de aprender",
    "date": "2026-09-02 08:00:00 -03:00",
    "project": "estudos",
    "content": "import Terminal from '../../components/TerminalWidget.astro'\n\n## O sintoma silencioso\n\nO treino começou bem. Os logs subiam, o batch passava, a GPU esquentava. E a loss não descia.\n\nNão descia de verdade: ficava colada em 10.8125, passando disso não ia. O modelo gerava texto, mas era texto de modelo branco — aquele ruído estatístico de quem ainda não aprendeu nada. Trezentos passos depois, nada tinha mudado.\n\nA primeira suspeita era clássica: taxa de aprendizado baixa demais. 1e-6 em um pré-treino de modelo grande é conservador demais — eu mesmo já tinha caído nessa armadilha antes, ajustando o learning rate pra baixo até o treino \"ficar estável\", só pra descobrir que estabilidade e estagnação são a mesma coisa vista de ângulos diferentes.\n\nMas o log tinha uma pista que o LR baixo não explicava.\n\n## A pista: um loss de 96 no primeiro step\n\nO checkpoint de partida não era um modelo qualquer. A loss inicial era 96 — não 10, não 11: 96. Números assim não aparecem em modelo saudável; aparecem quando os pesos estão explodidos.\n\nFui olhar a distribuição dos pesos do `lm_head` — a camada que traduz a representação interna em probabilidades sobre o vocabulário. O desvio padrão era 13.87. Para um head de saída, isso é um espanto: pesos saudáveis ficam na casa de décimos, não de dezenas.\n\nO que aconteceu ficou claro: os trezentos passos não estavam aprendendo. Estavam *normalizando*. O modelo passou o treino inteiro tentando desfazer o estrago do checkpoint — e quando finalmente chegou em 10.82, que é exatamente o logaritmo do tamanho do vocabulário, ele tinha virado um modelo branco de novo.\n\n## A matemática que explica tudo\n\nA loss de um modelo que chuta uniformemente sobre o vocabulário é `ln(vocab_size)`. Com um vocabulário de 50.257 tokens, isso dá:\n\n```python\nimport math\nmath.log(50257)  # 10.826...\n```\n\n10.8125 é o piso. É a loss de quem não aprendeu nada. O modelo não estava estagnado num patamar ruim — ele tinha *voltado ao zero* depois de gastar todo o treino consertando pesos quebrados.\n\nE o pior: com LR de 1e-6, ele nunca ia sair dali. Para um pré-treino dessa escala, o learning rate correto é da ordem de 3e-4 — trezentas vezes maior.\n\n## A armadilha do checkpoint \"bom\"\n\nO detalhe cruel: o checkpoint tinha passado por uma fase de warmup do head e aparecia com o nome \"best\" na árvore de arquivos. Um checkpoint com nome de melhor, pesos explodidos e loss inicial de 96. A nomenclatura mentia, e a confiança nela custou trezentos passos de GPU.\n\nA lição é a pergunta que eu deveria ter feito antes de relançar: qual é a loss do primeiro batch? Se o primeiro valor já é absurdo, não é problema de aprendizado — é problema de partida. Nenhuma taxa de aprendizado conserta pesos explodidos; o treino inteiro vira reabilitação.\n\n<Terminal command=\"python3 -u train.py --resume step_0300 --lr 3e-4 --max-steps 500\" output=\"loss 10.82 -> 9.91 -> 8.47 ... finalmente aprendendo\" />\n\n## O que mudou\n\nO relançamento usou o checkpoint limpo como base — os pesos normalizados viraram o ponto de partida real — e a taxa de aprendizado correta para a escala. O treino que estava congelado voltou a aprender de verdade.\n\nFicou registrado também o segundo bug, escondido na lógica de resume: uma condição de passo que só funcionava quando o treino começava do zero. Com resume em 300 e gradiente acumulado de 16, o contador de passos entrava num loop que nunca batia com o múltiplo esperado — GPU em 0%, log congelado, treino parado sem erro visível. A correção trocou a contagem pelo acumulador real.\n\nDiagnosticar um treino que não aprende é quase sempre a mesma história: ou o ponto de partida está podre, ou a taxa está errada, ou o resume está mentindo. Desta vez foram os três."
  },
  {
    "path": "portfolio-o-typewriter-que-apagava-o-proprio-texto.mdx",
    "slug": "portfolio-o-typewriter-que-apagava-o-proprio-texto",
    "lang": "pt",
    "title": "O typewriter que apagava o próprio texto",
    "date": "2026-09-02 16:00:00 -03:00",
    "project": "portfolio",
    "content": "import Terminal from '../../components/TerminalWidget.astro'\n\n## O efeito que ninguém pediu\n\nO hero do portfólio tinha um typewriter — aquele efeito de texto que digita sozinho, letra por letra. Elegante, até. O problema é que, em alguns loads, ele fazia um show completo: digitava a primeira frase, apagava tudo, e começava de novo.\n\nNão era charme. Era bug.\n\nA sequência denunciava a causa: o texto já estava na tela (HTML estático), aí o React montava o componente por cima, zerava o estado e o typewriter recomeçava do início. O usuário via o texto piscar e voltar pro começo — exatamente quando o LCP estava sendo medido.\n\n## O culpado: hidratação adiada no lugar errado\n\nO componente tinha sido \"otimizado\" com `requestIdleCallback`: só hidratava quando o navegador estava ocioso. A intenção era boa — tirar o trabalho do path crítico. Mas o typewriter é justamente o componente que não pode ser hidratado depois, porque a hidratação dele é *destrutiva*: montar por cima reseta o texto.\n\nA otimização melhorava a métrica de um lado e piorava a experiência do outro.\n\n<Terminal command=\"git log --oneline -3\" output=\"520270c perf(typewriter): remove idle-hydration que apagava texto e redigitava\n01c2704 perf(idle-hydration): rIC puro, readyState não dispara mais (root cause)\n7edb55d perf(cockpit): idle-hydration tira GSAP+canvas do path crítico\" />\n\n## A correção\n\nDuas decisões separadas:\n\n1. **Typewriter voltou a hidratar na hora.** Texto que já está na tela não pode ser apagado pela hidratação. O efeito ficou server-rendered estável e o JS só assume por cima sem resetar estado.\n\n2. **Idle-hydration ficou só onde faz sentido:** no background (CockpitBackground), onde o atraso é invisível. GSAP + canvas saíram do path crítico sem ninguém perceber — ali o adiamento é ganho puro.\n\nO resultado: LCP caiu de ~3.6s para ~2.7s, e o texto do hero parou de \"piscar\" no load.\n\n## A lição\n\n**Nem toda otimização de performance vale para todo componente.** `requestIdleCallback` é ótimo para trabalho que pode esperar; é péssimo para trabalho que destrói o que já foi renderizado. A pergunta certa antes de adiar qualquer hidratação: *o que acontece visualmente quando esse componente monta por cima?* Se a resposta for \"apaga algo\", não adie — ou o usuário vai assistir seu hero se auto-destruir toda vez que abrir a página.\n\nA métrica melhorou de verdade, e não na base do \"a página parecia mais rápida mas piscava\". Melhoria de performance que degrada a experiência não é melhoria — é troca."
  }
];
