// Gerado automaticamente por scripts/gen-ocultos.mjs
// NÃO editar manualmente

export default [
  {
    "path": "capivara-a-porta-de-mao-unica.mdx",
    "slug": "capivara-a-porta-de-mao-unica",
    "lang": "pt",
    "title": "A porta de mão única — o restart que mantinha o serviço mudo",
    "date": "2026-09-12 08:00:00 -03:00",
    "project": "capivara",
    "content": "import Terminal from '../../components/TerminalWidget.astro'\n\n## A falha bem-educada\n\nO chat do Capivara parou de responder com contexto. Não parou de responder.\n\nContinuava devolvendo 200, continuava escrevendo um texto coerente, continuava rápido. A diferença estava num campo do payload: `sources: []`. Zero fontes. A memória do ecossistema — aquela que dá substância pra resposta — tinha sumido sem fazer barulho.\n\nEsse é o tipo de falha que eu mais temo, porque ela não disputa atenção. Um 500 me acorda. Um 200 vazio me deixa dormir e acordar com o painel funcionando mal há três dias.\n\n## O diagnóstico de cinco segundos\n\nFui olhar o serviço de embeddings, o componente que transforma texto em vetor e sem o qual a busca semântica não existe. Porta fechada, conexão recusada.\n\nO diagnóstico de cinco segundos é óbvio: **\"tá morto, reinicia\"**. Reiniciei.\n\nA porta não abriu. Esperei um pouco, vi a mesma porta fechada, e concluí que o restart não tinha pegado — então reiniciei de novo. O que eu não sabia é que uma segunda sessão de agente, olhando a mesma porta fechada, estava fazendo exatamente a mesma coisa do outro lado.\n\nDuas sessões reiniciando o mesmo container, cada restart zerando o relógio do anterior, e as duas concluindo \"morto\" porque a porta não abria. O serviço nunca esteve morto. Ele estava sendo interrompido a cada tentativa de acordar.\n\n## Por que reiniciar era o problema\n\nO serviço de embeddings só abre a porta **depois** de terminar o benchmark de startup. Ele carrega o modelo, mede throughput real de inferência, e só então sobe o servidor que escuta conexão. Nessa ordem.\n\nNuma máquina ociosa isso leva alguns minutos. Naquela madrugada o host estava com um treino pesado disputando recurso, e o cold load tinha caído na faixa de dez a quarenta minutos. Medido depois, num trecho menos pior: treze minutos com dois núcleos e meio.\n\nOu seja — porta fechada com o container `Up` e CPU alta **é o estado normal de um processo trabalhando**. Cada restart não curava nada: matava o progresso do load e começava o relógio do zero. Quem estava \"curando\" era justamente o que mantinha o serviço mudo.\n\nDescobrir isso exigiu inverter a pergunta. Em vez de \"por que ele morreu?\", \"quem está reiniciando?\". E a resposta era eu.\n\n## A segunda porta de mão única\n\nDepois que o embedder finalmente ficou de pé sozinho, o chat continuou sem fontes. Aí estava o bug de verdade, e ele era do meu lado.\n\nO núcleo da memória tem um fallback sensato: se o embedder não responde, degrada pra uma busca por palavra-chave em SQLite. O problema é que essa degradação é um **latch de mão única** — uma flag que vira verdadeira e nunca volta a ser falsa.\n\nUma falha de trinta segundos degradava o processo inteiro. E como nada no caminho de leitura testava se o embedder tinha voltado, o único modo de recuperar a memória vetorial era reiniciar o backend do hub. Um evento transitório virava estado permanente, com uma porta de saída que só eu podia abrir, manualmente, no momento em que eu lembrasse que ela existia.\n\nOs dois defeitos eram o mesmo defeito com roupas diferentes: um sistema tratando **\"ainda não\"** como **\"nunca\"**.\n\n## O auto-heal\n\nO conserto foi no `brain.py`, na função que entrega o store pra todo mundo (`_get_store`). Em vez de aceitar a degradação como definitiva, o store degradado passa a ser revalidado de tempos em tempos com um probe barato — que pergunta ao embedder se ele está disponível, sem tocar no banco vetorial.\n\n```python\n_STORE_RETRY_SECS = float(os.getenv(\"BRAIN_STORE_RETRY_SECS\", \"120\"))\n\n\ndef _embedder_ok(store) -> bool:\n    \"\"\"Probe barato no embedder do store (nao toca no Qdrant).\"\"\"\n    emb = getattr(store, \"_embedder\", None)\n    if emb is None:\n        return False\n    try:\n        probe = getattr(emb, \"is_available\", None)\n        if callable(probe):\n            return bool(probe(timeout=2.0))\n        emb.embed([\"ping\"])\n        return True\n    except Exception as exc:  # qualquer falha = ainda degradado\n        log.info(\"brain: embedder ainda indisponivel (%s)\", type(exc).__name__)\n        return False\n```\n\n```python\ndef _get_store():\n    global _store, _store_degraded_since\n\n    with _store_lock:\n        if _store is not None and getattr(_store, \"_use_sqlite\", False):\n            if _store_degraded_since is None:\n                _store_degraded_since = time.time()\n            elif time.time() - _store_degraded_since >= _STORE_RETRY_SECS:\n                if _embedder_ok(_store):\n                    log.warning(\"brain: embedder recuperado — reconstruindo store (Qdrant)\")\n                    _store = None\n                else:\n                    _store_degraded_since = time.time()\n\n        if _store is None:\n            from core.store import MemoryStore\n\n            store = MemoryStore(backend=\"auto\", group=YURUMI_GROUP)\n            if getattr(store, \"_use_sqlite\", False):\n                _store_degraded_since = time.time()\n                log.warning(\"brain: store iniciou degradado (SQLite) — retry em %.0fs\", _STORE_RETRY_SECS)\n            else:\n                _store_degraded_since = None\n            _store = store\n        return _store\n```\n\nQuatro decisões dentro de dezesseis linhas, e cada uma existe porque a versão ingênua dela queimou:\n\n1. **Marca o instante da degradação** em vez de tentar recuperar na hora. O retry imediato só aumenta o thrash.\n2. **Não reconstrói antes do intervalo.** O embedder pode estar em cold load de meia hora — testar a cada requisição seria reinventar o loop que causou o incidente.\n3. **Embedder de volta descarta o store** (`_store = None`) e deixa a construção seguinte criar um limpo, voltando ao banco vetorial.\n4. **Embedder ainda fora re-arma o timer** — mantém o SQLite servindo e tenta de novo depois. O fallback continua útil; ele só perde o direito de ser eterno.\n\nO `with _store_lock` existe porque duas requisições simultâneas podem entrar no mesmo diagnóstico ao mesmo tempo. Sem lock, as duas reconstroem.\n\n## Os testes que fecham a porta\n\nOito casos em `tests/test_brain_store_heal.py`, e eles não usam embedder nem banco vetorial real — o store é um fake injetado, com um embedder falso que responde uma flag. Isso deixa o teste determinístico e rápido o suficiente pra rodar em CI.\n\n| Caso | O que garante |\n|---|---|\n| store saudável | nasce sem flag de degradação |\n| nasce degradado | registra o instante |\n| degradou em runtime | detecta e data do mesmo jeito |\n| antes do intervalo | não reconstrói (sem thrash) |\n| embedder voltou | reconstrói e limpa a flag |\n| embedder ainda fora | mantém SQLite e re-arma o timer |\n| sem embedder | probe retorna falso |\n| probe levanta exceção | exceção conta como indisponível, não como crash |\n\nOs dois últimos são os que eu quase pulei. São o custo de pensar \"e se o probe quebrar?\" em vez de \"e se o embedder estiver fora?\".\n\n## A sequência de diagnóstico que virou regra\n\nEscrevi no AGENTS.md do Capivara, em caixa alta, a regra que aquele incidente custou a madrugada inteira pra produzir:\n\n> **Nunca reiniciar o container do embedder para \"curá-lo\".**\n\nE embaixo dela, a ordem que eu queria ter seguido:\n\n1. Medir o embedder direto — uma chamada mínima de embedding, com timeout curto. Isso responde \"ele está processando?\" em vez de \"a porta abriu?\".\n2. Embedder saudável + resposta sem fontes = o problema é o **store degradado**. A cura é reiniciar o backend do hub (segundos), não o container (meia hora).\n3. Embedder mudo com uptime curto = **esperar**. Uptime menor que trinta minutos significa carregando, não travado.\n\nTambém plantei prevenção no outro ponto da falha: todo healer do ecossistema passou a ser *warming-aware* — mede o uptime antes de reiniciar e se recusa a matar um cold load em andamento. Reiniciar virou último recurso com critério, não reflexo.\n\n## O resto do mesmo dia\n\nO 11/09 não foi só o embedder, e os outros dois consertos são da mesma espécie:\n\n- **Sync do espelho de backup.** Duas execuções do mesmo sync (cron + manual) intercalaram escrita e geraram violação de chave única no destino. Virou `INSERT OR REPLACE` + lock de instância única com detecção de lock órfão — e, o mais importante, tabela que falha agora faz o processo inteiro sair com código de erro. Backup quente que anuncia \"sucesso\" mentindo é pior que backup que confessa.\n- **CI do runner próprio.** Um bootstrap de configuração lia um arquivo `.env` incondicionalmente no import. No runner auto-hospedado o arquivo não existe, e todo job que importava o módulo morria com `FileNotFoundError` havia dois dias. Ajuste de duas linhas, e a vergonha de dois dias de CI vermelho tratado como \"normal do runner\".\n\n## Métricas\n\n| Item | Antes | Depois |\n|---|---|---|\n| Fontes no `/api/chat/ask` | 0 | 5 |\n| Recuperação da memória vetorial | restart manual do backend | automática, em até ~2 min |\n| Testes do caminho de heal | 0 | 8 |\n| Suite do backend | — | 243 passed |\n| Cold load do embedder no incidente | interrompido a cada restart | ~13 min e converge |\n\nO número que importa não é o dos testes: é que, pela primeira vez, o serviço de memória se recupera sozinho de uma falha que nem é dele.\n\n## O que fica\n\n**Porta fechada não é diagnóstico — uptime é.** \"Sem resposta\" cobre desde processo morto até processo carregando modelo. Separar os dois é a diferença entre uma cura e uma sabotagem com boas intenções.\n\n**Fallback sem caminho de volta não é resiliência, é vazamento.** Todo degrade precisa de uma política de re-promote, nem que seja um probe barato a cada dois minutos. Um latch de mão única transforma qualquer transitório em permanente.\n\n**Um healer sem critério é um agressor com crachá.** Quanto mais automatizado o seu loop de cura, mais barulho ele faz quando está errado — e mais difícil fica enxergar que ele é a causa. Foram literalmente duas sessões inteligentes se ajudando uma na outra pra manter um serviço no chão.\n\n**Quando duas sessões trabalham no mesmo sistema, falta protocolo, não inteligência.** O reinício concorrente não foi burrice de ninguém: foram duas pessoas certas com a mesma heurística rápida e nenhuma visão do relógio uma da outra.\n\n<Terminal\n  title=\"investigacao-do-latch\"\n  commands={[\n    { cmd: 'git show --stat 9e7763e | head -12', output: 'fix(brain): auto-heal do store Yurumi — degradacao por falha transitoria do embedder nao era permanente\\n backend/brain.py                       |  58 ++++++++++++++\\n backend/tests/test_brain_store_heal.py | 122 ++++++++++++++++++' },\n    { cmd: 'grep -n \"BRAIN_STORE_RETRY_SECS\" backend/brain.py', output: '132:_STORE_RETRY_SECS = float(os.getenv(\"BRAIN_STORE_RETRY_SECS\", \"120\"))' },\n    { cmd: 'grep -c \"def test_\" backend/tests/test_brain_store_heal.py', output: '8' },\n    { cmd: 'python -m pytest backend/tests -q | tail -1', output: '243 passed' },\n  ]}\n/>"
  },
  {
    "path": "en/capivara-a-porta-de-mao-unica.mdx",
    "slug": "capivara-a-porta-de-mao-unica",
    "lang": "en",
    "title": "The one-way door — the restart that kept the service mute",
    "date": "2026-09-12 08:00:00 -03:00",
    "project": "capivara",
    "content": "import Terminal from '../../../components/TerminalWidget.astro'\n\n## The polite failure\n\nCapivara's chat stopped answering with context. It didn't stop answering.\n\nIt kept returning 200, kept writing a coherent sentence, kept being fast. The difference lived in one field of the payload: `sources: []`. Zero sources. The ecosystem memory — the part that gives the answer its substance — had vanished without making a sound.\n\nThis is the kind of failure I dread most, because it doesn't compete for attention. A 500 wakes me up. An empty 200 lets me sleep and wake up three days later with a dashboard that's been quietly broken the whole time.\n\n## The five-second diagnosis\n\nI went to look at the embedding service — the component that turns text into vectors and without which semantic search doesn't exist. Port closed, connection refused.\n\nThe five-second diagnosis is obvious: **\"it's dead, restart it\"**. I restarted it.\n\nThe port didn't open. I waited a bit, saw the same closed port, concluded the restart hadn't taken — so I restarted again. What I didn't know is that a second agent session, staring at the same closed port, was doing exactly the same thing on the other side.\n\nTwo sessions restarting the same container, each restart zeroing the previous one's clock, both concluding \"dead\" because the port wouldn't open. The service was never dead. It was being interrupted every single time it tried to wake up.\n\n## Why restarting was the problem\n\nThe embedding service only opens its port **after** finishing its startup benchmark. It loads the model, measures real inference throughput, and only then brings up the server that listens for connections. In that order.\n\nOn an idle machine that takes a few minutes. That night the host had a heavy training run competing for resources, and the cold load had slid into the ten-to-forty-minute range. Measured later, in a less hostile stretch: thirteen minutes on two and a half cores.\n\nIn other words — a closed port with the container `Up` and CPU pegged **is the normal state of a process doing its job**. Each restart cured nothing: it killed the load's progress and started the clock from zero. The thing \"healing\" was the very thing keeping the service mute.\n\nFinding that out required inverting the question. Instead of \"why did it die?\", asking \"who keeps restarting it?\". And the answer was me.\n\n## The second one-way door\n\nOnce the embedder finally stood up on its own, the chat still had no sources. That's where the real bug was, and it was on my side.\n\nThe memory core has a sensible fallback: if the embedder doesn't answer, degrade to keyword search in SQLite. The problem is that this degradation is a **one-way latch** — a flag that flips to true and never flips back.\n\nA thirty-second outage degraded the entire process. And because nothing in the read path ever tested whether the embedder had come back, the only way to restore vector memory was restarting the hub's backend. A transient event became permanent state, with an exit door only I could open, manually, at the exact moment I remembered it existed.\n\nBoth defects were the same defect in different clothes: a system treating **\"not yet\"** as **\"never\"**.\n\n## The auto-heal\n\nThe fix landed in `brain.py`, in the function that hands the store to everyone (`_get_store`). Instead of accepting degradation as final, a degraded store now gets revalidated every so often with a cheap probe — one that asks the embedder whether it's available without touching the vector store.\n\n```python\n_STORE_RETRY_SECS = float(os.getenv(\"BRAIN_STORE_RETRY_SECS\", \"120\"))\n\n\ndef _embedder_ok(store) -> bool:\n    \"\"\"Probe barato no embedder do store (nao toca no Qdrant).\"\"\"\n    emb = getattr(store, \"_embedder\", None)\n    if emb is None:\n        return False\n    try:\n        probe = getattr(emb, \"is_available\", None)\n        if callable(probe):\n            return bool(probe(timeout=2.0))\n        emb.embed([\"ping\"])\n        return True\n    except Exception as exc:  # qualquer falha = ainda degradado\n        log.info(\"brain: embedder ainda indisponivel (%s)\", type(exc).__name__)\n        return False\n```\n\n```python\ndef _get_store():\n    global _store, _store_degraded_since\n\n    with _store_lock:\n        if _store is not None and getattr(_store, \"_use_sqlite\", False):\n            if _store_degraded_since is None:\n                _store_degraded_since = time.time()\n            elif time.time() - _store_degraded_since >= _STORE_RETRY_SECS:\n                if _embedder_ok(_store):\n                    log.warning(\"brain: embedder recuperado — reconstruindo store (Qdrant)\")\n                    _store = None\n                else:\n                    _store_degraded_since = time.time()\n\n        if _store is None:\n            from core.store import MemoryStore\n\n            store = MemoryStore(backend=\"auto\", group=YURUMI_GROUP)\n            if getattr(store, \"_use_sqlite\", False):\n                _store_degraded_since = time.time()\n                log.warning(\"brain: store iniciou degradado (SQLite) — retry em %.0fs\", _STORE_RETRY_SECS)\n            else:\n                _store_degraded_since = None\n            _store = store\n        return _store\n```\n\n*(kept verbatim — the source comments are in Portuguese; I don't translate code, I translate the prose around it.)*\n\nFour decisions inside sixteen lines, and each one exists because its naive version burned:\n\n1. **Stamp the moment of degradation** instead of trying to recover right away. Immediate retry only adds thrash.\n2. **Don't rebuild before the interval.** The embedder may be mid thirty-minute cold load — probing on every request would have reinvented the loop that caused the incident.\n3. **Embedder back means dropping the store** (`_store = None`) and letting the next build create a clean one, returning to the vector store.\n4. **Embedder still out re-arms the timer** — SQLite keeps serving and we try again later. The fallback stays useful; it just loses the right to be forever.\n\nThe `with _store_lock` exists because two concurrent requests can enter the same diagnosis at the same time. Without the lock, both rebuild.\n\n## The tests that close the door\n\nEight cases in `tests/test_brain_store_heal.py`, and they use no real embedder and no real vector store — the store is an injected fake with a fake embedder that answers a flag. That keeps the tests deterministic and fast enough for CI.\n\n| Case | What it guarantees |\n|---|---|\n| healthy store | starts without the degradation flag |\n| born degraded | records the timestamp |\n| degraded at runtime | detected and stamped the same way |\n| before the interval | no rebuild (no thrash) |\n| embedder came back | rebuilds and clears the flag |\n| embedder still out | keeps SQLite and re-arms the timer |\n| no embedder at all | probe returns false |\n| probe raises | exception counts as unavailable, not as a crash |\n\nThose last two are the ones I almost skipped. They're the cost of asking \"what if the probe breaks?\" instead of only \"what if the embedder is down?\".\n\n## The diagnosis sequence that became a rule\n\nI wrote, in caps, in Capivara's AGENTS.md, the rule that incident spent the whole night producing:\n\n> **Never restart the embedder container to \"fix\" it.**\n\nAnd underneath it, the order I wish I'd followed:\n\n1. Measure the embedder directly — a minimal embedding call with a short timeout. That answers \"is it processing?\" instead of \"did the port open?\".\n2. Healthy embedder + answer without sources = the problem is the **degraded store**. The cure is restarting the hub backend (seconds), not the container (half an hour).\n3. Mute embedder with short uptime = **wait**. Uptime under thirty minutes means loading, not hung.\n\nI also planted prevention at the other failure point: every healer in the ecosystem became *warming-aware* — it measures uptime before restarting and refuses to kill a cold load in progress. Restarting became a last resort with criteria instead of a reflex.\n\n## The rest of the same day\n\nSeptember 11th wasn't only the embedder, and the other two fixes are the same species:\n\n- **Backup mirror sync.** Two runs of the same sync (cron + manual) interleaved writes and produced a unique-key violation at the destination. It became `INSERT OR REPLACE` plus a single-instance lock with orphan-lock detection — and, most importantly, a failed table now makes the whole process exit with an error code. A hot backup that lies about \"success\" is worse than one that confesses.\n- **Self-hosted runner CI.** A config bootstrap read an `.env` file unconditionally at import time. On the self-hosted runner that file doesn't exist, and every job importing the module died with `FileNotFoundError` — for two days. A two-line adjustment, and the embarrassment of two days of red CI written off as \"runner being flaky\".\n\n## Metrics\n\n| Item | Before | After |\n|---|---|---|\n| Sources in `/api/chat/ask` | 0 | 5 |\n| Vector memory recovery | manual backend restart | automatic, within ~2 min |\n| Tests on the heal path | 0 | 8 |\n| Backend suite | — | 243 passed |\n| Embedder cold load during the incident | killed on every restart | ~13 min and converges |\n\nThe number that matters isn't the test count: it's that, for the first time, the memory service recovers on its own from a failure that isn't even its own.\n\n## What stays\n\n**A closed port is not a diagnosis — uptime is.** \"No answer\" spans everything from a dead process to a process loading a model. Separating the two is the difference between a cure and sabotage with good intentions.\n\n**A fallback without a return path isn't resilience, it's a leak.** Every degrade needs a re-promote policy, even if it's just a cheap probe every two minutes. A one-way latch turns any transient into permanent.\n\n**A healer without criteria is an attacker with a badge.** The more automated your healing loop, the more damage it does when it's wrong — and the harder it is to see that it's the cause. This was literally two intelligent sessions helping each other keep a service on the floor.\n\n**When two sessions work on the same system, what's missing is protocol, not intelligence.** The concurrent restarts weren't anyone's stupidity: they were two correct actors with the same fast heuristic and no visibility into each other's clock.\n\n<Terminal\n  title=\"the-latch-investigation\"\n  commands={[\n    { cmd: 'git show --stat 9e7763e | head -12', output: 'fix(brain): auto-heal do store Yurumi — degradacao por falha transitoria do embedder nao era permanente\\n backend/brain.py                       |  58 ++++++++++++++\\n backend/tests/test_brain_store_heal.py | 122 ++++++++++++++++++' },\n    { cmd: 'grep -n \"BRAIN_STORE_RETRY_SECS\" backend/brain.py', output: '132:_STORE_RETRY_SECS = float(os.getenv(\"BRAIN_STORE_RETRY_SECS\", \"120\"))' },\n    { cmd: 'grep -c \"def test_\" backend/tests/test_brain_store_heal.py', output: '8' },\n    { cmd: 'python -m pytest backend/tests -q | tail -1', output: '243 passed' },\n  ]}\n/>"
  }
];
