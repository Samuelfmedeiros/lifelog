// Gerado automaticamente por scripts/gen-ocultos.mjs
// NÃO editar manualmente

export default [
  {
    "path": "descobertas-os-350-links-que-apontavam-para-o-nada.mdx",
    "slug": "descobertas-os-350-links-que-apontavam-para-o-nada",
    "lang": "pt",
    "title": "Os 350 links que apontavam para o nada — o dia em que o hreflang virou ruído",
    "date": "2026-09-15 12:00:00 -03:00",
    "project": "descobertas",
    "content": "import Terminal from '../../components/TerminalWidget.astro'\n\n## O cabeçalho perfeito que ninguém lia\n\nO site estava bem-comportado demais. Cada página gerada emitia três tags de idioma — a versão em português, a versão em inglês e um `x-default` — mais o canonical, o Open Graph, o RSS. O HTML parecia um manual de boas práticas.\n\nSó tinha um problema: boa parte desses links apontava para página que não existia.\n\nNão é um detalhe cosmético. O Google é explícito na documentação de hreflang: se duas páginas não apontam uma para a outra, as tags são ignoradas. Um `alternate` que devolve 404 não é \"link quebrado\" no sentido de erro de console que alguém vê — é sinal de ruído. O robô lê, não confirma o caminho de volta, e descarta o par inteiro. Ou seja: o trabalho de bilíngue que custou semanas estava, em parte, declarado de um jeito que o buscador jogava fora.\n\nA auditoria somou 350 links nesse estado: 169 indo de português para inglês, 166 no sentido inverso e 15 de `x-default`. Todos gerados, todos silenciosos, todos errados.\n\n## A premissa que ninguém escreveu\n\nO código culpado era curto o bastante para caber em três linhas. O layout base calculava o caminho canônico da página e derivava as contrapartes por concatenação de string:\n\n```astro\n// antes\nconst canonPathEn = canonPath === '/' ? '/en/'\n  : (canonPath.startsWith('/en/') ? canonPath : '/en' + canonPath);\nconst canonPathPt = canonPath === '/en/' ? '/'\n  : (canonPath.startsWith('/en/') ? canonPath.slice(3) || '/' : canonPath);\n```\n\nNinguém sentou e decidiu \"toda página em português tem irmã em inglês no mesmo caminho\". A premissa se formou sozinha, porque no começo era verdade: havia duas ou três páginas fixas, e o prefixo `/en` resolvia. O dia em que o blog virou bilíngue de verdade, com um arquivo por idioma, a regra silenciosa continuou de pé emitindo URL com confiança de lei.\n\nE a realidade tem três dobras que a concatenação não previa.\n\n**Nome de arquivo traduzido.** A maioria dos posts nasceu com o slug nos dois idiomas — mas uma parte veio com o título traduzido no filename do inglês. `2026-07-28-semana-da-qualidade-portfolio` de um lado, `2026-07-28-quality-week-portfolio` do outro. Concatenar `/en` na frente do caminho português gera um endereço que nunca existiu em nenhum universo.\n\n**Página que só existe em um idioma.** O painel de rascunhos ocultos é português puro — não tem rota em inglês correspondente. Emitir `alternate` dali é inventar uma página.\n\n**Alias de rota.** As páginas fixas não espelham o nome: `/arquivo` mora em `/en/archive`, `/sobre` em `/en/about`. O corte do prefixo devolve `/en/arquivo`, que é 404.\n\nA conta feita na época registrou 22 slugs traduzidos, de 11 posts, no meio disso. Refazendo a medição hoje, com o pareamento por data de publicação mais projeto — que é como o sistema casa os dois idiomas de verdade — a lista de órfãos (post português sem inglês) dá zero. Os 13 pares que ainda têm filename divergente e os 6 ambíguos são exatamente o que a concatenação nunca conseguiria adivinhar.\n\n## O prop que precisa de três estados, não dois\n\nA cura não era \"consertar o path\"; era admitir que o layout não sabe quem é o irmão de quem. Quem sabe é a página, que tem acesso à coleção de posts. Então o layout passou a receber a contraparte por prop.\n\nE aí apareceu o ponto que eu não tinha visto antes de escrever: um boolean não servia, e nem duas opções.\n\n```astro\n// o prop tem tres estados, e dois deles significam coisas opostas\nalternatePath?: string | null;\n\n// undefined  -> deriva do proprio path (home, arquivo, sobre: espelho funciona)\n// string     -> usa exatamente este caminho como irma\n// null       -> NAO existe irma; nao emite alternate nenhum\n```\n\n`undefined` e `null` pareciam a mesma coisa até alguém passar o primeiro post sem tradução. Com a checagem frouxa, \"sem contraparte\" e \"não informei\" caíam no mesmo ramo e o site voltava a inventar link. A diferença semântica virou parte da interface do componente: passar `null` é uma afirmação, não uma ausência.\n\nO resto da função é uma escada de casos — previews ocultos trocando só o segmento de idioma, painel ocultos devolvendo nulo, aliases de rota, e só no fim o caminho derivado.\n\n## A ordem do bloco, ou os 37 links que sobraram\n\nA primeira versão da correção passou no teste e deixou quebrado o que ninguém estava olhando.\n\nPorque o bloco de preview oculto vinha depois do resto da lógica, e o caminho de preview tem formato próprio — `/ocultos/preview/pt/...`. Quando a função caía no ramo geral antes de reconhecer preview, ela montava um inglês a partir de um português que já era composto. Sobraram cerca de 37 links quebrados nesse nicho, todos em páginas que não aparecem em lugar nenhum da navegação: o painel de revisão, justamente onde eu nunca ia olhar.\n\nReordenar — reconhecer preview antes de qualquer coisa — fechou. Lição que vale para qualquer validador que eu escreva depois: um caso que não aparece no menu continua aparecendo no build.\n\n## Dois ícones que nunca puderam existir\n\nA mesma varredura achou outro problema, esse constrangedor de tão simples. O `manifest.json` pedia dois ícones e o `apple-touch-icon` pedia um terceiro. Os três caminhos davam 404 em produção desde sempre.\n\nO motivo estava no `.gitignore`:\n\n```gitignore\n*.png\n```\n\nUma linha global, inocente, colocada ali para não commitar print de teste. O efeito colateral: era impossível commitar o diretório de ícones sem forçar. Ninguém nunca forçou. Os arquivos simplesmente nunca existiram no repositório — e o deploy servia um manifesto prometendo ícones que não estavam no pacote.\n\nA cura teve duas partes: uma exceção para o diretório (`!public/icons/*.png`) e os dois PNGs desenhados, 192 por 192 e 512 por 512, ocupando 878 e 2.828 bytes. Nada de emoji renderizado — a máquina não tem fonte de emoji e o resultado sai como quadrado branco, que é literalmente o tipo de coisa que passaria batida num manifesto que ninguém abre.\n\nO bug não era o 404. Era o `.gitignore` silencioso o suficiente para transformar \"nunca existiu\" em \"está tudo igual\".\n\n## Um vigia que reprovava por culpa própria\n\nPelo padrão que eu já tinha aprendido da pior forma, não bastava consertar: precisava travar. Então nasceu um guardião que roda depois do build, caminha pelo `dist/`, lê cada HTML e extrai dois tipos de coisa com uma expressão regular — todo `link rel=alternate hreflang` e todo assetreferenciado no cabeçalho. Se algum caminho não existe no diretório gerado, o script devolve exit 1 e o pipeline para. Cento e catorze linhas no total.\n\nEle entrou no workflow do deploy como passo próprio, depois do build, antes do push.\n\nE aí o teste de regressão que escreveu junto falhou — por erro dele. O spec navegava para `/posts/`, rota que nunca existiu neste projeto, e tratava o 404 como falha do site. O vigia acusava o inocente e o culpado era o vigia. Cortei o spec para o smoke honesto: hreflang apontando para páginas reais, ícones do manifesto presentes, `noindex` nas duas páginas de erro. A cobertura exaustiva ficou com o script do `dist/`, que é onde ela é barata.\n\nUm detalhe que quase passou despercebido: comparar caminho de arquivo com href exige decodificar por cento e normalizar Unicode. O link vem `%C3%AD` no HTML; o filesystem pode estar em NFC ou NFD. Sem `unquote` mais normalização, o verificador acusa de quebrado um link perfeitamente válido — falso positivo em slug com acento, e falso positivo ensina a desligar o guardião mais rápido que qualquer bug real.\n\n## Métricas\n\n| Item | Antes | Depois |\n|------|-------|--------|\n| Links de alternate apontando para 404 | 350 | 0 |\n| Alternate declarados no build | ~3.5k | 3.876 |\n| Ícones do manifesto existentes no pacote | 0 de 2 | 2 de 2 |\n| Página de erro indexável | sim | não |\n| Guardião rodando no CI | não existia | pós-build, bloqueante |\n| Build de prova | -- | 1.293 páginas, testes unitários 89 em 89 |\n\n## Aprendizados\n\n**Invariância silenciosa é a mais cara.** \"Todo português tem inglês no mesmo path\" nunca foi decidido em voz alta. Premissas que ninguém escreveu não têm dono, e é elas que viram três linhas de concatenação num layout.\n\n**Declaração sem verificação é decoração.** hreflang é um pedido, não uma ordem. O padrão exige bidirecionalidade, e o verificador de verdade é o lado de fora. Enquanto ninguém confirmasse o caminho de volta, o verde do build não significava nada.\n\n**Ausência é um valor.** `undefined` e `null` precisaram ser separados para o sistema conseguir dizer \"não informei\" e \"não existe\" sem confundir os dois. Metade dos bugs de integração que eu arrumo são duas intenções diferentes disputando um estado só.\n\n**Um ignore global é uma política sem revisão.** Uma linha de `.gitignore` pode proibir um recurso inteiro de nascer, e o erro só aparece do lado de fora do repositório.\n\n**O validador também precisa de teste.** Escrevi o guardião e o espec que falhava por conta própria no mesmo dia. Confiar na ferramenta sem ler a acusação dela é só terceirizar o bug.\n\n## O que vem a seguir\n\nO vigia já pegou uma variação ao ser plantado: a regra de pareamento por data de publicação mais projeto precisou valer também para os previews ocultos, e foi exatamente a ordem dos ramos que quebrou ali. É o tipo de caso que só aparece porque agora existe um lugar onde ele dói no build em vez de silenciosamente no índice.\n\nO que fica aberto é o outro lado da moeda: um post novo pode nascer com hreflang quebrado de um jeito que nem a derivação nem o nome divergente cobrem — tradução publicada com data diferente do original. O pareamento casa pelo par exato, e se a data escorregar entre os dois arquivos, o alternate vira null sem ninguém avisar. É a próxima checagem que faz sentido escrever: não \"esse link existe\", mas \"esse post tem irmão\".\n\n<Terminal\n  commands={[\n    { cmd: '--- sintoma: 350 hreflang ignorados ---' },\n    { cmd: 'pt->en: 169 | en->pt: 166 | x-default: 15' },\n    { cmd: 'Google: par nao-mutuo -> tag descartada' },\n    { cmd: '--- causa: premissa de path, nao decisao ---' },\n    { cmd: 'canonPathEn = \"/en\" + canonPath' },\n    { cmd: 'slug traduzido / pagina mono-idioma / alias de rota' },\n    { cmd: '--- cura: quem sabe o irmao e a pagina ---' },\n    { cmd: 'alternatePath?: string | null' },\n    { cmd: 'undefined -> deriva | string -> usa | null -> nao emite' },\n    { cmd: '--- ordem dos ramos: preview antes do geral ---' },\n    { cmd: '~37 links quebrados sobravam no painel de revisao' },\n    { cmd: '--- icons: nunca existiram no repo ---' },\n    { cmd: '.gitignore: *.png  ->  !public/icons/*.png' },\n    { cmd: 'lifelog-192.png 192x192 878B | lifelog-512.png 512x512 2828B' },\n    { cmd: '--- guardiao pos-build ---' },\n    { cmd: 'python3 scripts/check-alternates.py --dist dist' },\n    { cmd: 'walk dist/ + unquote + NFC -> exit 1 se faltar rota' },\n    { cmd: '114 linhas, CI verde, 3876 alternates, 0 quebrados' },\n  ]}\n/>"
  },
  {
    "path": "en/descobertas-os-350-links-que-apontavam-para-o-nada.mdx",
    "slug": "descobertas-os-350-links-que-apontavam-para-o-nada",
    "lang": "en",
    "title": "The 350 links pointing to nowhere — the day hreflang became noise",
    "date": "2026-09-15 12:00:00 -03:00",
    "project": "descobertas",
    "content": "import Terminal from '../../../components/TerminalWidget.astro'\n\n## The perfect header nobody read\n\nThe site was behaving too well. Every generated page emitted three language tags — the Portuguese version, the English version, and an `x-default` — plus the canonical, Open Graph, and RSS. The HTML looked like a best-practices manual.\n\nThere was just one problem: a good share of those links pointed at pages that did not exist.\n\nThat is no cosmetic detail. Google is explicit in the hreflang docs: if two pages don't both point to each other, the tags are ignored. An `alternate` that returns 404 isn't a \"broken link\" in the sense of a console error someone sees — it's noise. The crawler reads it, doesn't confirm the return path, and discards the whole pair. In other words: the bilingual work that took weeks was, in part, declared in a way the search engine threw away.\n\nThe audit counted 350 links in that state: 169 going Portuguese to English, 166 the other way, and 15 `x-default`. All generated, all silent, all wrong.\n\n## The premise nobody wrote down\n\nThe guilty code was short enough to fit in three lines. The base layout computed the page's canonical path and derived the counterparts by string concatenation:\n\n```astro\n// before\nconst canonPathEn = canonPath === '/' ? '/en/'\n  : (canonPath.startsWith('/en/') ? canonPath : '/en' + canonPath);\nconst canonPathPt = canonPath === '/en/' ? '/'\n  : (canonPath.startsWith('/en/') ? canonPath.slice(3) || '/' : canonPath);\n```\n\nNobody sat down and decided \"every Portuguese page has an English sibling at the same path.\" The premise formed on its own, because at first it was true: there were two or three fixed pages, and the `/en` prefix solved it. The day the blog became genuinely bilingual, one file per language, the silent rule kept emitting URLs with the confidence of law.\n\nAnd reality has three folds the concatenation didn't foresee.\n\n**Translated filename.** Most posts were born with the slug in both languages — but some came with the title translated in the English filename. `2026-07-28-semana-da-qualidade-portfolio` on one side, `2026-07-28-quality-week-portfolio` on the other. Concatenating `/en` in front of the Portuguese path yields an address that never existed in any universe.\n\n**A page that only exists in one language.** The hidden-draft panel is pure Portuguese — there's no matching English route. Emitting an `alternate` from there is inventing a page.\n\n**Route aliases.** The fixed pages don't mirror their names: `/arquivo` lives at `/en/archive`, `/sobre` at `/en/about`. Cutting the prefix yields `/en/arquivo`, which is a 404.\n\nThe count made at the time logged 22 translated slugs, from 11 posts, in the middle of this. Redoing the measurement today, with pairing by pubDate plus project — which is how the system actually matches the two languages — the orphan list (a Portuguese post without English) comes to zero. The 13 pairs that still have a divergent filename and the 6 ambiguous ones are precisely what concatenation could never guess.\n\n## The prop needs three states, not two\n\nThe cure wasn't \"fix the path\"; it was admitting the layout doesn't know who is whose sibling. The one that knows is the page, which has access to the post collection. So the layout started receiving the counterpart via prop.\n\nAnd here came the point I hadn't seen before writing it: a boolean wasn't enough, and neither were two options.\n\n```astro\n// the prop has three states, and two of them mean opposite things\nalternatePath?: string | null;\n\n// undefined  -> derive from the path itself (home, archive, about: mirror works)\n// string     -> use exactly this path as the sibling\n// null       -> NO sibling exists; emit no alternate at all\n```\n\n`undefined` and `null` looked like the same thing until someone passed the first untranslated post. With a loose check, \"no counterpart\" and \"I didn't specify\" fell into the same branch and the site went back to inventing links. The semantic difference became part of the component's interface: passing `null` is an assertion, not an absence.\n\nThe rest of the function is a ladder of cases — hidden previews swapping only the language segment, the hidden panel returning null, route aliases, and only at the end the derived path.\n\n## The order of the block, or the 37 links that were left\n\nThe first version of the fix passed the test and left broken the thing nobody was looking at.\n\nBecause the hidden-preview block came after the rest of the logic, and the preview path has its own shape — `/ocultos/preview/pt/...`. When the function fell into the general branch before recognizing the preview, it assembled an English from a Portuguese that was already composed. About 37 broken links were left in that niche, all on pages that appear nowhere in navigation: the review panel, exactly where I never went to look.\n\nReordering — recognize the preview before anything else — closed it. A lesson that holds for any validator I write later: a case that doesn't show up in the menu still shows up in the build.\n\n## Two icons that could never exist\n\nThe same sweep found another problem, embarrassing in how simple it was. The `manifest.json` asked for two icons and `apple-touch-icon` asked for a third. All three paths had been returning 404 in production since forever.\n\nThe reason was in the `.gitignore`:\n\n```gitignore\n*.png\n```\n\nA global line, innocent, placed there so I wouldn't commit test screenshots. The side effect: it was impossible to commit the icons directory without forcing it. Nobody ever forced it. The files simply never existed in the repository — and the deploy served a manifest promising icons that weren't in the package.\n\nThe cure had two parts: an exception for the directory (`!public/icons/*.png`) and the two PNGs drawn, 192 by 192 and 512 by 512, taking up 878 and 2,828 bytes. No rendered emoji — the machine has no emoji font and the result comes out as a white square, which is literally the kind of thing that would slip past a manifest nobody opens.\n\nThe bug wasn't the 404. It was the `.gitignore` being silent enough to turn \"never existed\" into \"nothing changed.\"\n\n## A watchdog that failed because of itself\n\nBy the pattern I had already learned the hard way, fixing wasn't enough: it had to be locked. So a guardian was born that runs after the build, walks the `dist/`, reads every HTML and extracts two kinds of things with a regex — every `link rel=alternate hreflang` and every asset referenced in the head. If any path doesn't exist in the generated directory, the script returns exit 1 and the pipeline stops. A hundred and fourteen lines in total.\n\nIt entered the deploy workflow as its own step, after the build, before the push.\n\nAnd then the regression spec written alongside it failed — by its own fault. The spec navigated to `/posts/`, a route that never existed in this project, and treated the 404 as a site failure. The watchdog accused the innocent and the guilty one was the watchdog. I cut the spec to the honest smoke: hreflang pointing to real pages, manifest icons present, `noindex` on the two error pages. The exhaustive coverage stayed with the `dist/` script, which is where it's cheap.\n\nA detail that almost slipped by: comparing a file path to an href requires percent-decoding and Unicode normalization. The link arrives as `%C3%AD` in the HTML; the filesystem may be in NFC or NFD. Without `unquote` plus normalization, the checker flags a perfectly valid link as broken — a false positive on an accented slug, and a false positive teaches you to switch off the guardian faster than any real bug.\n\n## Metrics\n\n| Item | Before | After |\n|------|--------|-------|\n| Alternate links pointing to 404 | 350 | 0 |\n| Alternates declared in the build | ~3.5k | 3,876 |\n| Manifest icons present in the package | 0 of 2 | 2 of 2 |\n| Error page indexable | yes | no |\n| Guardian running in CI | didn't exist | post-build, blocking |\n| Proof build | -- | 1,293 pages, unit tests 89 of 89 |\n\n## Lessons\n\n**A silent invariant is the expensive one.** \"Every Portuguese page has English at the same path\" was never decided out loud. Premises nobody wrote have no owner, and it's them that become three lines of concatenation in a layout.\n\n**A declaration without verification is decoration.** hreflang is a request, not an order. The standard requires bidirectionality, and the real validator is the outside. As long as nobody confirmed the return path, the build's green meant nothing.\n\n**Absence is a value.** `undefined` and `null` had to be separated so the system could say \"I didn't specify\" and \"it doesn't exist\" without confusing the two. Half the integration bugs I fix are two different intentions fighting over a single state.\n\n**A global ignore is a policy without review.** One `.gitignore` line can forbid an entire feature from being born, and the error only shows up outside the repository.\n\n**The validator needs tests too.** I wrote the guardian and the spec that failed on its own the same day. Trusting a tool without reading its accusation is just outsourcing the bug.\n\n## What comes next\n\nThe watchdog already caught a variation the moment it was planted: the pubDate-plus-project pairing rule had to hold for the hidden previews too, and it was exactly the order of the branches that broke there. It's the kind of case that only surfaces because now there's a place where it hurts in the build instead of silently in the index.\n\nWhat stays open is the other side of the coin: a new post can be born with broken hreflang in a way neither the derivation nor the divergent filename covers — a translation published with a different date from the original. The pairing matches by exact key, and if the date slips between the two files, the alternate becomes null without anyone warning. That's the next check that makes sense to write: not \"does this link exist,\" but \"does this post have a sibling.\"\n\n<Terminal\n  commands={[\n    { cmd: '--- symptom: 350 hreflang ignored ---' },\n    { cmd: 'pt->en: 169 | en->pt: 166 | x-default: 15' },\n    { cmd: 'Google: non-mutual pair -> tag discarded' },\n    { cmd: '--- cause: a path premise, not a decision ---' },\n    { cmd: 'canonPathEn = \"/en\" + canonPath' },\n    { cmd: 'translated slug / mono-language page / route alias' },\n    { cmd: '--- cure: the page knows its sibling ---' },\n    { cmd: 'alternatePath?: string | null' },\n    { cmd: 'undefined -> derive | string -> use | null -> no emit' },\n    { cmd: '--- branch order: preview before general ---' },\n    { cmd: '~37 broken links were left in the review panel' },\n    { cmd: '--- icons: never existed in the repo ---' },\n    { cmd: '.gitignore: *.png  ->  !public/icons/*.png' },\n    { cmd: 'lifelog-192.png 192x192 878B | lifelog-512.png 512x512 2828B' },\n    { cmd: '--- post-build guardian ---' },\n    { cmd: 'python3 scripts/check-alternates.py --dist dist' },\n    { cmd: 'walk dist/ + unquote + NFC -> exit 1 if route missing' },\n    { cmd: '114 lines, CI green, 3876 alternates, 0 broken' },\n  ]}\n/>"
  }
];
