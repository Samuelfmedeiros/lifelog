// Gerado automaticamente por scripts/gen-ocultos.mjs
// NÃO editar manualmente

export default [
  {
    "path": "descobertas-as-tres-respostas-do-monitor.mdx",
    "slug": "descobertas-as-tres-respostas-do-monitor",
    "lang": "pt",
    "title": "As três respostas do monitor — por que 'não sei' precisa ser um estado, não um verde",
    "date": "2026-09-12 12:00:00 -03:00",
    "project": "descobertas",
    "content": "import Terminal from '../../components/TerminalWidget.astro'\n\n## O verde que não media nada\n\nO monitor de pipeline de mídia roda a cada quinze minutos, checa meia dúzia de coisas e só fala quando algo está errado. Silêncio é bom: significa que tudo passou.\n\nNuma determinada madrugada, o monitor imprimiu `FIXES APLICADOS` — e não tinha corrigido absolutamente nada. O convidado estava inacessível; nenhum check chegou a medir fosse o que fosse. Dias depois apareceu no log a primeira linha com a palavra INDETERMINADO, e o registro no topo da função explica o que tinha acontecido antes dela existir:\n\n> qualquer check que não devolvesse `True` (inclusive \"não deu pra checar\") virava `FIXED` e entrava em \"FIXES APLICADOS\"\n\nNão era um verde otimista. Era um cinza que se vestia de conserto — a pior das variações, porque \"consertado\" pede revisão zero. Quem lê o log às seis da manhã vê um problema resolvido e volta a dormir.\n\nFui ler o código dos checks. Eles executam um comando no convidado, recebem a saída, e procuram dentro dela a palavra que significa \"existe\". Três caminhos possíveis:\n\n```python\nsaida = convidado(f\"ls -d {alvo} 2>/dev/null && echo EXISTS || echo MISSING\")\nif \"EXISTS\" in saida:\n    return \"ok\", \"caminho presente\"\nif \"MISSING\" in saida:\n    cria_diretorio()\n    return \"fixed\", \"caminho criado (estava faltando)\"\nreturn \"unknown\", f\"o disco não respondeu — nada foi aplicado\"\n```\n\nA lógica parece inofensiva, e era. O problema não estava nela — estava no `convidado()`.\n\n## O exit code que media a coisa errada\n\nO helper que rodava comandos no convidado tinha uma regra simples: devolvia stdout quando o processo terminava com zero, e devolvia string vazia em qualquer outro caso. Um atalho de higiene — comando que falhou não tem saída aproveitável.\n\nSó que existe uma classe inteira de comandos que falham de propósito. São exatamente os comandos de verificação.\n\n`systemctl is-enabled <unidade>` retorna código diferente de zero quando a unidade **não** está habilitada. É o desenho do utilitário: o exit code carrega a resposta. O monitor queria justamente saber se algumas unidades estavam desabilitadas — e o helper descartava a resposta certa, porque a resposta certa tinha chegado com exit code \"errado\".\n\nResultado: check rodando todo dia, devolvendo vazio, rotulando \"não respondeu\". E o mais desconcertante — o serviço estava saudável. O monitor é que estava cego, num ponto específico, de forma permanente e silenciosa.\n\nO conserto tem um caractere e meio:\n\n```python\nconvidado(\"systemctl is-enabled unidade_a unidade_b 2>&1; true\")\n```\n\nForçar zero no final de quem só queria informar. E acrescentar a pergunta que faltava no helper: **exit code diferente de zero é falha da medição ou é o resultado da medição?** São coisas diferentes e o monitor não sabia distinguir.\n\n## O timeout que matava o filho errado\n\nSegunda família de mentira. Esse check depende de um processo externo que depende de um disco que está morrendo — de verdade, com setores lentos e operações de leitura que penduram. E leitura pendurada em disco moribundo entra naquele estado do kernel que nem sinal de morte alcança.\n\nO helper tinha `timeout=` no `subprocess.run`. Que funciona, com uma ressalva que só aparece em produção: ele mata **o filho direto**. O neto — o shell de dentro do convidado, o utilitário que estava preso na leitura do disco — sobrevive. E sobrevive segurando o pipe, o handle, o endereço.\n\nO estrago não ficou restrito ao monitor. Uma tarefa agendada que usou o mesmo padrão ficou travada segurando o lock da própria sessão por horas. Enquanto ela segurava, toda entrega seguinte daquela sessão era descartada por não conseguir o lock — mais de uma centena de entregas no mesmo dia, perdidas uma por uma, sem nenhuma exceção no ar. Nada estava quebrado. Estava tudo esperando.\n\nA cerca precisa ser dupla, e cada metade pega um caso:\n\n```python\ncerco_convidado = f\"timeout -k 5 {limite}\"      # mata lá dentro, onde o I/O está preso\nprocesso = subprocess.Popen(comando, ...)\ntry:\n    saida = processo.communicate(timeout=limite)\nexcept subprocess.TimeoutExpired:\n    subprocess.run([\"taskkill\", \"/T\", \"/F\", \"/PID\", str(processo.pid)])\n    processo.kill()\n```\n\nO `timeout` interno resolve quando o convidado ainda responde. O kill de árvore no host resolve quando o processo pendurado não vai morrer sozinho. Um dos dois sozinho deixa sobrevivente.\n\nA auditoria do que ficou depois foi reveladora: dos mais de oitocentos scripts de automação do ecossistema, pouco mais de uma centena já usava kill de árvore — mas o helper reutilizável com as duas cercas existia em **um** arquivo. Um padrão que funcionava, consertado num lugar só, sem caminho de volta para os outros duzentos que invocavam o mesmo processo externo.\n\n## Quando nem o SIGKILL alcança\n\nTem um detalhe que nenhuma das duas cercas resolve, e que muda o desenho: se o processo está em espera de E/S ininterruptível, ele não morre. Nem com kill forçado, nem com o timeout do convidado. Ele fica ali, zumbi com crédito no nome, até o disco responder — se responder.\n\nA conclusão prática é contraintuitiva: quando o disco é hostil, **o teto de espera tem que ser curto**. Vinte e cinco segundos, um tempo que dói menos do que segurar a entrega por minutos. Rotular \"não sei\" rápido é melhor do que rotular errado devagar.\n\nE outra: contra esse tipo de E/S, retry não é política — é desperdício. Onde um `ls` pode pendurar indefinidamente, insistir três vezes não aumenta a chance de sucesso; multiplica por três o tempo preso. Nesses casos o código faz uma tentativa só.\n\n## A terceira caixa\n\nO erro de desenho não era técnico, era de vocabulário. O monitor tinha duas palavras para um mundo que precisa de três:\n\n| Estado | Significa | Pode alertar? | Pode consertar? |\n|---|---|---|---|\n| OK | medido, passou | não | não |\n| FALHA | medido, reprovado | sim | sim |\n| INDETERMINADO | **não medido** | só na reincidência | nunca |\n\nA regra que saiu daí tem duas metades, e a segunda é a que costuma faltar:\n\n1. **Um INDETERMINADO não vira FALHA.** Uma medição solta não sustenta alerta. O mundo fica flutuando, o convidado oscila, o disco trava — alertar em cada medição perdida é fabricar ruído até alguém ignorar o canal.\n2. **Um INDETERMINADO não vira OK.** Essa é a que dói, porque o verde acumula em silêncio. Monitor que não mede não deve se apresentar como monitor que passou; o painel precisa mostrar o cinza, e o relatório precisa dizer quantos checks foram *poupados* de decidir.\n\nA terceira caixa ainda tem efeito colateral bom: ela obriga a escrever a frase do \"não sei\". E quando a gente escreve, descobre que \"não respondeu\" era mentira — tinha respondido, sim, com exit code que o helper não sabia ler.\n\n## O que o log mostrou depois\n\nDepois das cercas no lugar, o monitor passou a registrar o que não sabe. O arquivo tem 858 execuções acumuladas e 709 checagens no formato atual, que carimba UTC e hora local — foram essas 709 que eu seccionei:\n\n- **27 indeterminações, 3,8% das checagens.** Somando OK, falha, correção e detecção, a taxa de \"não sei\" ficou abaixo de 4% — e foi aí que a média mentiu de novo, pela última vez.\n- Segregando por seção, o 3,8% se desfaz. Das seis checagens, as **quatro que medem no próprio host** somam 472 execuções com **zero** indeterminação. As **duas que cruzam a fronteira** para o convidado respondem por **todas as 27**: 22 em 119 leituras de disco (18,5%) e 5 em 118 consultas de serviço (4,2%).\n- **Uma única correção aplicada** em todo o período, e ela veio de uma medição concreta: nenhum \"consertado\" nasceu de leitura vazia.\n\nO achado não é a taxa, é o endereço. A incerteza não estava distribuída pelo monitor — estava 100% concentrada na linha que atravessa de uma máquina para outra, exatamente onde a espera é imprevisível e onde o timeout que mata o filho errado faz estrago. As partes que medem em casa não têm crise de identidade nenhuma.\n\nIsso muda o que se conserta. A tentação seria afrouxar o timeout de tudo, ou reclamar do convidado. O que os números dizem é mais estreito e mais útil: **toda a sua dúvida mora na fronteira** — então é na fronteira que a sonda barata do lado de fora tem que entrar primeiro, e é ela que decide se vale a pena atravessar.\n\nO número que eu queria, no fim, nem é o 3,8% nem o 18,5%. É o fato de os dois existirem. Antes, essa taxa não era alta nem baixa: era invisível, indistinguível de \"tudo certo\". Um monitor que não sabe quanto não sabe é a versão mais cara de um monitor mudo: custa, engana e ainda dorme tranquilo.\n\n## O que fica\n\n**Exit code é mensagem, não só status.** Todo comando de verificação responde pelo código. Um helper que trata não-zero como \"sem saída\" apaga a resposta de metade das perguntas que o monitor faz.\n\n**Timeout no pai não mata o neto.** Se o que você chama abre um processo que abre outro, o `timeout=` encerra exatamente um deles — e quase nunca o que estava prendendo o recurso. Kill de árvore não é capricho, é o que fecha o buraco.\n\n**Discos morrendo exigem pressa e honestidade.** Onde o E/S pode ficar ininterruptível, o certo é esperar pouco, admitir rápido e não insistir. A pressa aqui não é impaciência: é a garantia de que a tarefa não vira refém.\n\n**O estado desconhecido precisa ter cor.** Se o seu painel só tem vermelho e verde, ele é obrigado a escolher um dos dois quando não sabe nada. Ele vai escolher o verde, porque o verde não gera trabalho — e é assim que a falha fica bonita por semanas.\n\n<Terminal\n  title=\"auditoria-do-monitor\"\n  commands={[\n    { cmd: 'grep -c \"media-streaming-health v1\" monitor.log', output: '858' },\n    { cmd: 'python3 secciona_log.py', output: 'checagens: 709 | ok: 563 | falha: 44 | indeterminado: 27 | corrigido: 1' },\n    { cmd: 'python3 secciona_log.py --por-secao', output: 'socat 5/118 = 4.2% | savepath 22/119 = 18.5%' },\n    { cmd: 'python3 secciona_log.py --host-only', output: '472 checagens no proprio host, 0 indeterminadas' },\n    { cmd: 'grep -rl \"def _run_tree\" scripts/ | wc -l', output: '1' },\n    { cmd: 'grep -rl \"/T\" scripts/ | wc -l', output: '156' },\n    { cmd: 'ls scripts/*.py | wc -l', output: '816' },\n  ]}\n/>"
  },
  {
    "path": "en/descobertas-as-tres-respostas-do-monitor.mdx",
    "slug": "descobertas-as-tres-respostas-do-monitor",
    "lang": "en",
    "title": "The three answers a monitor needs — why 'I don't know' must be a state, not a green",
    "date": "2026-09-12 12:00:00 -03:00",
    "project": "descobertas",
    "content": "import Terminal from '../../../components/TerminalWidget.astro'\n\n## The green that measured nothing\n\nThe media-pipeline watchdog runs every fifteen minutes, checks about half a dozen things, and only speaks when something is wrong. Silence is good: it means everything passed.\n\nOne particular night, the watchdog printed `FIXES APPLIED` — and it had fixed precisely nothing. The guest was unreachable; no check had measured a single thing. Days later the first line containing the word INDETERMINATE appeared in the log, and the comment at the top of the function explains what had happened before that word existed:\n\n> any check that didn't return `True` (including \"couldn't check at all\") became `FIXED` and went into \"FIXES APPLIED\"\n\nThat was not an optimistic green. It was a gray wearing a repair badge — the worst variant, because \"fixed\" demands zero review. Whoever reads the log at six in the morning sees a solved problem and goes back to sleep.\n\nI went to read the checks. They run a command in the guest, take the output, and look inside it for the word that means \"exists\". Three possible paths:\n\n```python\nout = guest(f\"ls -d {target} 2>/dev/null && echo EXISTS || echo MISSING\")\nif \"EXISTS\" in out:\n    return \"ok\", \"path present\"\nif \"MISSING\" in out:\n    create_dir()\n    return \"fixed\", \"path created (it was missing)\"\nreturn \"unknown\", \"the disk did not answer — nothing was applied\"\n```\n\nThe logic looks harmless, and it was. The bug wasn't in it — it was inside `guest()`.\n\n## The exit code that read the wrong thing\n\nThe helper running guest commands had one simple rule: return stdout when the process exits zero, return an empty string in every other case. A hygiene shortcut — a failed command has no usable output.\n\nBut there is an entire class of commands that fail on purpose. They are exactly the verification commands.\n\n`systemctl is-enabled <unit>` returns a non-zero code when the unit is **not** enabled. That is the utility's design: the exit code carries the answer. The watchdog's job was precisely to find out whether a few units were disabled — and the helper threw the correct answer away, because the correct answer had arrived with a \"wrong\" exit code.\n\nThe result: a check running every day, returning empty, labeled \"did not respond\". And the most unsettling part — the service was healthy. The monitor was the blind thing, at one specific spot, permanently and quietly.\n\nThe fix is one and a half characters long:\n\n```python\nguest(\"systemctl is-enabled unit_a unit_b 2>&1; true\")\n```\n\nForcing a zero at the end of something that only wanted to inform. Plus the question the helper was never asked: **is a non-zero exit code a failed measurement, or is it the measurement's result?** Those are different things, and the monitor could not tell them apart.\n\n## The timeout that killed the wrong child\n\nSecond family of lies. That check depends on an external process that depends on a dying disk — genuinely dying, with slow sectors and read operations that hang. And a read hung on a dying disk enters that kernel state that not even a kill signal reaches.\n\nThe helper used `timeout=` on `subprocess.run`. Which works, with one caveat that only surfaces in production: it kills **the direct child**. The grandchild — the shell inside the guest, the utility stuck on the disk read — survives. And it survives holding the pipe, the handle, the address.\n\nThe damage did not stay inside the watchdog. A scheduled task using the same pattern hung for hours holding its own session lock. While it held it, every subsequent delivery for that session was dropped for failing to acquire the lock — over a hundred deliveries in a single day, lost one by one, with no exception anywhere in the air. Nothing was broken. Everything was waiting.\n\nThe fence has to be double, and each half catches a different case:\n\n```python\nguest_fence = f\"timeout -k 5 {limit}\"        # kills in there, where the I/O is stuck\nproc = subprocess.Popen(command, ...)\ntry:\n    out = proc.communicate(timeout=limit)\nexcept subprocess.TimeoutExpired:\n    subprocess.run([\"taskkill\", \"/T\", \"/F\", \"/PID\", str(proc.pid)])\n    proc.kill()\n```\n\nThe inner `timeout` works when the guest still answers. The tree kill on the host works when the hung process has no intention of dying alone. Either one alone leaves a survivor.\n\nThe audit that followed was revealing: of the eight-hundred-plus automation scripts in the ecosystem, just over a hundred already used a tree kill — but the reusable double-fence helper existed in **one** file. A pattern that worked, fixed in one place, with no path back to the two hundred others invoking the same external process.\n\n## When even SIGKILL cannot reach\n\nThere is a detail neither fence solves, and it changes the design: if the process sits in uninterruptible I/O wait, it does not die. Not with a forced kill, not with the guest timeout. It stays there, a zombie with a credit line, until the disk answers — if it answers.\n\nThe practical conclusion is counterintuitive: when the disk is hostile, **the wait ceiling has to be short**. Twenty-five seconds, a number that hurts less than holding a delivery for minutes. Labeling \"I don't know\" fast beats labeling it wrong slowly.\n\nAnd one more: against that kind of I/O, retry is not a policy — it is waste. Where an `ls` may hang indefinitely, insisting three times does not raise the odds of success; it multiplies by three the time spent trapped. For those calls the code makes a single attempt.\n\n## The third box\n\nThe design mistake was not technical, it was vocabulary. The monitor had two words for a world that needs three:\n\n| State | Means | May alert? | May auto-fix? |\n|---|---|---|---|\n| OK | measured, passed | no | no |\n| FAIL | measured, rejected | yes | yes |\n| INDETERMINATE | **not measured** | only on recurrence | never |\n\nThe rule that came out of this has two halves, and the second is the one usually missing:\n\n1. **An INDETERMINATE never becomes a FAIL.** A single measurement does not support an alert. The network flaps, the guest wobbles, the disk stalls — alerting on every lost measurement manufactures noise until somebody mutes the channel.\n2. **An INDETERMINATE never becomes an OK.** This is the one that stings, because green accumulates silently. A monitor that did not measure must not present itself as a monitor that passed; the dashboard has to show the gray, and the report has to state how many checks were *spared* from deciding.\n\nThe third box has a useful side effect: it forces you to write the \"I don't know\" sentence down. And once you write it, you discover that \"did not respond\" was a lie — it had responded, with an exit code the helper did not know how to read.\n\n## What the log showed afterwards\n\nOnce the fences were in place, the monitor started recording what it does not know. The file carries 858 accumulated runs and 709 checks in the current format, which stamps UTC plus local time — those 709 are what I sliced:\n\n- **27 indeterminations, 3.8% of checks.** Add up OK, failure, fix and detection, and the \"I don't know\" rate stayed under 4% — which is exactly where the average lied again, for the last time.\n- Segregated by section, the 3.8% falls apart. Of the six checks, the **four that measure on the host itself** total 472 executions with **zero** indetermination. The **two that cross the boundary** into the guest account for **all 27**: 22 out of 119 disk reads (18.5%) and 5 out of 118 service queries (4.2%).\n- **A single fix applied** across the whole period, and it came from a concrete measurement: no \"fixed\" was ever born from an empty read.\n\nThe finding is not the rate, it is the address. The uncertainty was not spread across the monitor — it was 100% concentrated on the line that crosses from one machine to another, precisely where waiting is unpredictable and where the timeout that kills the wrong child does its damage. The parts that measure at home have no identity crisis at all.\n\nThat changes what you fix. The temptation is to loosen every timeout, or to blame the guest. What the numbers say is narrower and more useful: **all of your doubt lives on the boundary** — so that is where the cheap probe from the outside has to run first, and it is what decides whether crossing is worth it.\n\nThe number I actually wanted is neither the 3.8% nor the 18.5%. It is the fact that both exist. Before, that rate was neither high nor low: it was invisible, indistinguishable from \"all good\". A monitor that does not know how much it does not know is the most expensive version of a silent monitor: it costs, it deceives, and it still sleeps well.\n\n## What stays\n\n**An exit code is a message, not just a status.** Every verification command answers through its code. A helper that treats non-zero as \"no output\" erases the answer to half the questions a monitor asks.\n\n**A timeout on the parent does not kill the grandchild.** If what you call spawns a process that spawns another, `timeout=` ends exactly one of them — almost never the one holding the resource. Tree kill is not a nicety; it is what closes the hole.\n\n**Dying disks demand speed and honesty.** Where I/O can become uninterruptible, the right move is to wait little, admit fast, and not insist. The hurry is not impatience: it is the guarantee that the task never becomes a hostage.\n\n**The unknown state needs a color.** If your dashboard only has red and green, it is forced to pick one of the two when it knows nothing. It will pick green, because green creates no work — and that is how a failure stays pretty for weeks.\n\n<Terminal\n  title=\"watchdog-audit\"\n  commands={[\n    { cmd: 'grep -c \"media-streaming-health v1\" monitor.log', output: '858' },\n    { cmd: 'python3 slice_log.py', output: 'checks: 709 | ok: 563 | fail: 44 | indeterminate: 27 | fixed: 1' },\n    { cmd: 'python3 slice_log.py --by-section', output: 'service query 5/118 = 4.2% | disk read 22/119 = 18.5%' },\n    { cmd: 'python3 slice_log.py --host-only', output: '472 checks on the host itself, 0 indeterminate' },\n    { cmd: 'grep -rl \"def _run_tree\" scripts/ | wc -l', output: '1' },\n    { cmd: 'grep -rl \"/T\" scripts/ | wc -l', output: '156' },\n    { cmd: 'ls scripts/*.py | wc -l', output: '816' },\n  ]}\n/>"
  },
  {
    "path": "en/lifelog-o-ciclo-do-nao.mdx",
    "slug": "lifelog-o-ciclo-do-nao",
    "lang": "en",
    "title": "The no loop — when every reader rejection became a ticket in the pipeline",
    "date": "2026-09-06 08:00:00 -03:00",
    "project": "lifelog",
    "content": "import Terminal from '../../../components/TerminalWidget.astro'\n\n## The day silence ate a request\n\nThis blog publishes daily, and every post is born hidden. I read, decide what goes live, and when something doesn't sit right with me, the system logs my rejection into a file inside the repository. Simple, traceable, quiet. For months this worked — until I discovered that \"working\" and \"being read\" are different things.\n\nThe rejections file grew. Related issues stayed open. Nothing complained, because everything was \"recorded\". But a record without notification is a message in a bottle: the content is intact, the destination is what's missing. A rewrite request sat idle for days while the pipeline kept its rhythm, happy, unaware it owed an answer.\n\n## Why a file isn't enough\n\nA rejection had three possible destinations and the implementation got two right: a traceable comment in the repository and a physical copy under `docs/recusas/`. What was missing was the link that turns a record into a task — someone (or something) that looks at that file and says \"this needs a response\". The blog had the memory of the request, but no system with a mandate to act on it.\n\nThe fix wasn't creating yet another place to write things down. It was closing the loop: now, when a rejection is recorded, the matching pipeline gets notified and the post returns to the review queue — with the original reason attached, intact, in the same place it was born. The file stayed the source of truth; what changed was who reads it and when.\n\n```js\n// Before: rejection recorded, period.\nawait commitNotaFile(slug, nota);        // file in the repo\nawait createIssue(slug, nota);           // traceable issue\n// ...and the pipeline? The pipeline found out when (or if) someone remembered.\n\n// After: rejection recorded + automatic follow-up\nawait commitNotaFile(slug, nota);\nawait createIssue(slug, nota);\nawait notifyPipeline(slug, nota);        // the loop closes itself\n```\n\n## What the loop teaches about feedback\n\nA system that accepts rejection without reacting teaches the user to stop rejecting. Worse: it teaches that rejecting is pointless. The button existed, worked, saved everything — and still the loop was broken, because whoever receives the feedback was never told they had received it.\n\nThe fix reminded me of a rule I apply in code and forget in processes: **every input needs a named consumer**. Not \"someone will see it eventually\", but a specific component, with a specific trigger, reacting to a specific event. Without that, the input is just archiving with good posture.\n\n| | Rejection as a file | Rejection as a ticket |\n|---|---|---|\n| Record | traceable | traceable |\n| Who reads it | whoever remembers | the pipeline, always |\n| Response time | undefined | one cycle |\n| Silence possible | yes, and common | no |\n\n## The takeaway\n\nThe automation I'm proud of isn't the one that publishes posts by itself. It's the one that guarantees **none of my requests disappear**. If I reject a post, that \"no\" has a path: file, issue, notification, rewrite, re-delivery. The blog became a system that listens and answers — and if the loop ever breaks again, there is now a part of the process whose only job is to notice.\n\n<Terminal\n  commands={[\n    { label: 'origin', cmd: 'POST /api/recusar  {\"path\": \"<slug>\", \"nota\": \"...\"}' },\n    { label: 'record', cmd: 'traceable issue + docs/recusas/<slug>.md' },\n    { label: 'follow-up', cmd: 'pipeline notified -> rewrites -> re-delivers hidden' },\n    { label: 'closure', cmd: 'issue closed when the new version enters the flow' },\n  ]}\n/>"
  },
  {
    "path": "en/portfolio-a-fabrica-de-candidaturas-en.mdx",
    "slug": "portfolio-a-fabrica-de-candidaturas-en",
    "lang": "en",
    "title": "The application factory that doesn't exist yet",
    "date": "2026-09-11 18:05:00 -03:00",
    "project": "portfolio",
    "content": "import Terminal from '../../../components/TerminalWidget.astro'\n\n## The manual ritual\n\nApplying to a job today, with the tool already reintroduced, follows a fixed sequence: open the portfolio, click the tailored résumé button, describe the job in the modal, wait for the AI, review the PDF field by field, save, attach it to the company's form. Several clicks — and, worst of all, the whole ritual repeats from zero for every company. The tool exists; the habit is still artisanal.\n\nThis isn't laziness talking. It's the realization that the feature solved the hard part (reformatting the résumé without inventing anything, aligning it with the job) and left the bureaucratic surroundings untouched: collecting the posting, triggering the process, storing the result, remembering who it went to. That surroundings is what the next phase wants to eat.\n\n## The tool that came back different\n\nThe road here already earned a story on this blog: a pre-dawn revert on the 5th took the entire feature out of the tree, and six days later it came back — with a different architecture. Before and after, in two commits:\n\n| | Revert (Saturday, 3:55 AM) | Reintroduction (Friday, 12:18 AM) |\n|---|---|---|\n| Commit | `4dd1a99` | `d247a55` |\n| Files | 20 | 14 |\n| Lines | −4.819 | +3.242 |\n| LLM integration | hardcoded race of free models | provider chain via gateway |\n| LLM down | placeholder PDF with HTTP 200 | explicit 502 |\n| Prompt echo | became a fake PDF | guard + retry + 502 |\n\nThe reintroduction didn't restore — it swapped the model race for a provider chain (the portfolio talks to a gateway, not to a list), made LLM downtime fail loud, and planted a guard that recognizes when the model returns the prompt's own example back as its answer. The rails that hold the tool today were born from that pair of pre-dawn sessions.\n\nBut notice what the table doesn't show: none of these decisions touches the manual ritual. The tool got better at waiting for someone to sit down and operate it.\n\n## Phase 5, written since August\n\nAt the end of August, when the Resume Tailor plan was written, this gap already had a name. The document ends with a phases section, and the last one isn't about code — it's about operation:\n\n```text\n### Phase 5 — Practical Use (Jobs)\n- [ ] Internal script: for each target company, generate a tailored résumé\n- [ ] Semi-automated pipeline: new job posting → prompt → résumé → PDF\n- [ ] Integrate with LifeLog? (post \"I applied to company X\")\n```\n\nThree checkboxes. None checked. The phase that turns the tool into a factory was drawn before the tool even survived its first revert — and that's probably why it's the most honest of the three: it doesn't promise elegance, it promises volume.\n\n## The pipeline design\n\nThe idea that hasn't left paper yet has a deliberately boring sequence:\n\n**The job enters raw.** The posting text, pasted as it came — with badly formatted requirements, a marketing title, whatever. The pipeline doesn't ask for a pretty job posting; demanding one creates friction at the entrance, and friction at the entrance kills pipelines.\n\n**The jobs folder becomes the source of truth.** Each target job becomes an artifact on disk: posting text + metadata (company, role, date). With that, generating stops being an event in an interface and becomes a runnable command — including in batch, for the list of companies already worth keeping separated.\n\n**The prompt is assembled, not written.** The raw text enters the same mold the modal uses today: real résumé as the only source, inventing forbidden, ATS format, brand colors. The advantage of the mold already being in production is that the pipeline inherits the guardrails for free — prompt echo detected, retry with reinforcement; LLM down, 502 and no fake PDF. Fail-loud matters even more in a pipeline than in a feature: a fake PDF in a batch of ten is the kind of error that only surfaces at the interview stage.\n\n**The human points and clicks.** The \"semi\" in semi-automated is deliberate. The AI prepares the draft; the decision to send, with a real name under it, belongs to whoever signs. Automating the sending would save the one part nobody should save.\n\n**Applications become records.** The plan's last checkbox wires the pipeline to the blog: every generated application can become a public line on LifeLog. Not for vanity — for memory. \"I applied to X on such date\" is the antidote to the ghost application, the one you forget you sent and get surprised when they answer.\n\n## Today vs. what's still coming\n\n| | Today (manual) | Phase 5 (pipeline) |\n|---|---|---|\n| Input | posting pasted into the modal, one at a time | raw posting on disk, in batch |\n| Execution | full ritual per company | one command per company |\n| Guardrails | identical | inherited from the same mold |\n| Records | none | every application becomes a blog line |\n| Who sends | you | you — the AI only prepares |\n\n## What an empty checkbox teaches\n\nFeatures are born twice: first as a tool, then as a habit. Friday's reintroduction delivered the tool — and it's good, with fail-loud errors and an echo guard. The habit is still pending, because habituating someone into a ten-click manual ritual is wasting the tool that was built to replace it.\n\nThe part that makes me want to write about something that doesn't exist yet: a plan with empty checkboxes is a promise with an open expiration date. Phase 5 was written before the tool was ready, survived the revert alongside it, and is still there — three lines waiting for the next pre-dawn block. When the checkboxes become code, this post is the \"before\". And if they never do, the post becomes the photo of a good plan that stayed on paper — which is also an honest record.\n\n<Terminal\n  client:visible\n  title=\"samuel@dev:~/portifolio\"\n  commands={[\n    { cmd: 'git show d247a55 --stat | tail -1' },\n    { cmd: '# 14 files changed, 3242 insertions(+) — the tool is back' },\n    { cmd: 'grep -A4 \"Fase 5\" docs/plans/2026-08-30-resume-tailor-ai.md' },\n    { cmd: '# - [ ] Internal script: for each target company...' },\n    { cmd: '# - [ ] Semi-automated pipeline: new job → prompt → résumé → PDF' },\n    { cmd: '# - [ ] Integrate with LifeLog? (post \"I applied to company X\")' },\n    { cmd: 'echo \"3 empty checkboxes. For now.\"' },\n  ]}\n/>"
  },
  {
    "path": "lifelog-o-ciclo-do-nao.mdx",
    "slug": "lifelog-o-ciclo-do-nao",
    "lang": "pt",
    "title": "O ciclo do não — quando cada recusa do leitor virou um ticket no pipeline",
    "date": "2026-09-06 08:00:00 -03:00",
    "project": "lifelog",
    "content": "import Terminal from '../../components/TerminalWidget.astro'\n\n## O dia em que o silêncio comeu um pedido\n\nO blog publica todos os dias, e cada post nasce escondido. Eu leio, decido o que vai pro ar, e quando algo não me agrada, o sistema anota minha recusa num arquivo dentro do repositório. Simples, rastreável, quieto. Por meses isso funcionou — até descobrir que \"funcionar\" e \"ser lido\" são coisas diferentes.\n\nO arquivo de recusas crescia. Issues relacionadas ficavam abertas. Nada reclamava, porque tudo estava \"registrado\". Só que registro sem notificação é mensagem numa garrafa: o conteúdo está intacto, o destino é que não existe. Um pedido de reescrita ficou dias parado enquanto o pipeline seguia seu ritmo, feliz, sem saber que devia algo.\n\n## Por que um arquivo não basta\n\nA recusa tinha três destinos possíveis e a implementação acertou dois: um comentário rastreável no repositório e uma cópia física em `docs/recusas/`. O que faltava era o elo que transforma registro em tarefa — alguém (ou algo) que olhe aquele arquivo e diga \"isto precisa de resposta\". O blog tinha a memória do pedido, mas nenhum sistema com mandato de agir sobre ela.\n\nA correção não foi criar mais um lugar pra escrever. Foi fechar o ciclo: agora, quando uma recusa é registrada, o pipeline correspondente é notificado e o post volta pro fluxo de revisão — com o motivo original anexado, intacto, no mesmo lugar onde nasceu. O arquivo continuou sendo a fonte de verdade; o que mudou foi quem o lê e quando.\n\n```js\n// Antes: recusa registrada, e só.\nawait commitNotaFile(slug, nota);        // arquivo no repo\nawait createIssue(slug, nota);           // issue rastreável\n// ...e o pipeline? O pipeline descobria quando (ou se) alguém lembrava.\n\n// Depois: recusa registrada + cobrança automática\nawait commitNotaFile(slug, nota);\nawait createIssue(slug, nota);\nawait notifyPipeline(slug, nota);        // o ciclo fecha sozinho\n```\n\n## O que o ciclo ensina sobre feedback\n\nUm sistema que aceita rejeição sem reagir ensina o usuário a parar de rejeitar. Pior: ensina que recusar é inútil. O botão existia, funcionava, salvava tudo — e ainda assim o loop estava quebrado, porque quem recebe o feedback nunca foi avisado de que tinha recebido.\n\nO conserto me lembrou de uma regra que aplico no código e esqueço nos processos: **toda entrada precisa de um consumidor nomeado**. Não \"alguém vai ver eventualmente\", mas um componente específico, com gatilho específico, reagindo a um evento específico. Sem isso, a entrada é só arquivamento com passo firme.\n\n| | Recusa como arquivo | Recusa como ticket |\n|---|---|---|\n| Registro | rastreável | rastreável |\n| Quem lê | quem lembrar | o pipeline, sempre |\n| Tempo de resposta | indefinido | um ciclo |\n| Silêncio possível | sim, e comum | não |\n\n## O aprendizado\n\nA automação que me orgulho não é a que publica posts sozinha. É a que garante que **nenhum pedido meu desaparece**. Se eu recuso um post, aquele \"não\" tem um caminho: arquivo, issue, notificação, reescrita, re-entrega. O blog virou um sistema que ouve e responde — e se um dia o ciclo quebrar de novo, agora existe uma parte do processo cujo único trabalho é perceber isso.\n\n<Terminal\n  commands={[\n    { label: 'origem', cmd: 'POST /api/recusar  {\"path\": \"<slug>\", \"nota\": \"...\"}' },\n    { label: 'registro', cmd: 'issue rastreável + docs/recusas/<slug>.md' },\n    { label: 'cobrança', cmd: 'pipeline notificado -> reescreve -> re-entrega oculta' },\n    { label: 'fechamento', cmd: 'issue fechada quando a nova versão entra no fluxo' },\n  ]}\n/>"
  },
  {
    "path": "portfolio-a-fabrica-de-candidaturas.mdx",
    "slug": "portfolio-a-fabrica-de-candidaturas",
    "lang": "pt",
    "title": "A fábrica de candidaturas que ainda não existe",
    "date": "2026-09-11 18:05:00 -03:00",
    "project": "portfolio",
    "content": "import Terminal from '../../components/TerminalWidget.astro'\n\n## O ritual manual\n\nAplicar a uma vaga hoje, com a ferramenta já reintroduzida, tem uma sequência fixa: abrir o portfólio, clicar no botão de currículo personalizado, descrever a vaga no modal, esperar a IA, revisar o PDF campo por campo, salvar, anexar no formulário da empresa. São vários cliques e — o pior — é um ritual que se repete inteiro para cada empresa. A ferramenta existe; o hábito continua artesanal.\n\nNão é reclamação de preguiça. É a percepção de que a feature resolveu a parte difícil (reformatar o currículo sem inventar nada, alinhar com a vaga) e deixou intocado o entorno burocrático: coletar a vaga, disparar o processo, guardar o resultado, lembrar pra quem foi. Essa entorno é que a próxima fase quer comer.\n\n## A ferramenta que voltou diferente\n\nO caminho até aqui rendeu uma história que já foi contada neste blog: um revert de madrugada no dia 5 tirou a feature inteira da árvore, e seis dias depois ela voltou — com outra arquitetura. O antes e o depois, em dois commits:\n\n| | Revert (sábado, 03h55) | Reintrodução (sexta, 00h18) |\n|---|---|---|\n| Commit | `4dd1a99` | `d247a55` |\n| Arquivos | 20 | 14 |\n| Linhas | −4.819 | +3.242 |\n| Integração LLM | corrida de modelos free hardcoded | cadeia de providers via gateway |\n| LLM fora do ar | PDF placeholder com HTTP 200 | erro explícito 502 |\n| Echo de prompt | virava PDF de mentira | guard + retry + 502 |\n\nA reintrodução não restaurou — ela trocou a corrida de modelos por uma cadeia de providers (o portfólio fala com um gateway, não com uma lista), fez o LLM indisponível falhar alto e cravou um guard que reconhece quando o modelo devolve o próprio exemplo do prompt de volta. Os trilhos que hoje sustentam a ferramenta nasceram desse par de madrugadas.\n\nMas repare no que a tabela não mostra: nenhuma dessas decisões mexe no ritual manual. A ferramenta ficou melhor esperando alguém sentar e operar.\n\n## A Fase 5, escrita desde agosto\n\nNo fim de agosto, quando o plano do Resume Tailor foi escrito, essa lacuna já tinha nome. O documento termina com uma seção de fases, e a última delas não fala de código — fala de operação:\n\n```text\n### Fase 5 — Uso Prático (Vagas)\n- [ ] Script interno: para cada empresa-alvo, gerar currículo personalizado\n- [ ] Pipeline semi-automático: nova vaga → prompt → currículo → PDF\n- [ ] Integrar com LifeLog? (postar \"Candidatei para X empresa\")\n```\n\nTrês checkboxes. Nenhum marcado. A fase que transforma a ferramenta em fábrica foi desenhada antes de a ferramenta sequer sobreviver ao primeiro revert — e é provavelmente por isso que ela é a mais honesta das três: não promete eleganteza, promete volume.\n\n## O desenho do pipeline\n\nA ideia que ainda vai sair do papel tem uma sequência propositalmente sem graça:\n\n**A vaga entra crua.** Texto do anúncio, colado como veio — com requisito mal formatado, título marketing, o que for. O pipeline não pede que a vaga seja bonita; pedir isso é criar atrito na entrada, e atrito na entrada mata pipeline.\n\n**A pasta de vagas vira fonte de verdade.** Cada vaga-alvo vira um artefato em disco: texto do anúncio + metadados (empresa, cargo, data). Com isso, gerar deixa de ser um evento na interface e vira um comando executável — inclusive em lote, para a lista de empresas que já vale a pena ter separadas.\n\n**O prompt é montado, não escrito.** O texto cru da vaga entra no mesmo molde que hoje o modal usa: currículo real como única fonte, proibido inventar, formato ATS, cores da marca. A vantagem de o molde já existir em produção é que o pipeline herda os guardrails de graça — echo de prompt detectado, repete com reforço; LLM fora, 502 e nenhum PDF de mentira. Fail-loud em pipeline é ainda mais importante do que em feature: um PDF falso num lote de dez é o tipo de erro que só aparece na entrevista.\n\n**O humano aponta e clica.** O \"semi\" de semi-automático é deliberado. A IA prepara o rascunho; a decisão de enviar, com o nome de verdade embaixo, é de quem assina. Automatizar o envio seria economizar a única parte que ninguém deveria economizar.\n\n**Candidatura vira registro.** O último checkbox do plano liga o pipeline ao blog: cada candidatura gerada pode virar uma linha pública no LifeLog. Não por vaidade — por memória. \"Candidatei para X em tal data\" é o antídoto contra a candidatura fantasma, aquela que você esquece que enviou e se surpreende quando respondem.\n\n## Hoje vs. o que ainda vem\n\n| | Hoje (manual) | Fase 5 (pipeline) |\n|---|---|---|\n| Entrada | vaga colada no modal, uma por vez | vaga crua em disco, em lote |\n| Execução | ritual inteiro por empresa | um comando por empresa |\n| Guardrails | idênticos | herdados do mesmo molde |\n| Registro | nenhum | cada candidatura vira linha no blog |\n| Quem envia | você | você — a IA só prepara |\n\n## O que um checkbox vazio ensina\n\nFeatures nascem duas vezes: primeiro como ferramenta, depois como hábito. A reintrodução da sexta-feira entregou a ferramenta — e ela é boa, com falha alta e guard de echo. O hábito continua pendente porque habituar alguém a um ritual manual de dez cliques é desperdiçar a ferramenta que o substitui.\n\nA parte que me faz querer escrever sobre algo que ainda não existe: um plano com checkboxes vazios é uma promessa com data de validade aberta. A Fase 5 foi escrita antes da ferramenta estar pronta, sobreviveu ao revert junto com ela, e continua lá — três linhas esperando o próximo bloco de madrugada. Quando os checkboxes virarem código, este post é o \"antes\". E se nunca virarem, o post vira a foto do plano que era bom e ficou no papel — o que também é um registro honesto.\n\n<Terminal\n  client:visible\n  title=\"samuel@dev:~/portifolio\"\n  commands={[\n    { cmd: 'git show d247a55 --stat | tail -1' },\n    { cmd: '# 14 files changed, 3242 insertions(+) — a ferramenta de volta' },\n    { cmd: 'grep -A4 \"Fase 5\" docs/plans/2026-08-30-resume-tailor-ai.md' },\n    { cmd: '# - [ ] Script interno: para cada empresa-alvo...' },\n    { cmd: '# - [ ] Pipeline semi-automático: nova vaga → prompt → currículo → PDF' },\n    { cmd: '# - [ ] Integrar com LifeLog? (postar \"Candidatei para X empresa\")' },\n    { cmd: 'echo \"3 checkboxes vazios. Por enquanto.\"' },\n  ]}\n/>"
  }
];
