// Gerado automaticamente por scripts/gen-ocultos.mjs
// NÃO editar manualmente

export default [
  {
    "path": "arachne-trinta-commits-ate-o-verde.mdx",
    "slug": "arachne-trinta-commits-ate-o-verde",
    "lang": "pt",
    "title": "Trinta commits até o verde — a semana em que o CI do Arachne virou o projeto",
    "date": "2026-09-07 12:00:00 -03:00",
    "project": "arachne",
    "content": "import Terminal from '../../components/TerminalWidget.astro'\n\nTodo projeto de verdade tem um capítulo que não vai pra homepage: a semana em que a pipeline de CI vira o projeto principal. Para o Arachne, esse capítulo aconteceu nos dias 6 e 7 de setembro. Foram trinta commits entre a madrugada de sexta e a manhã de sábado, todos com o mesmo objetivo: fazer a barra de status do GitHub parar de ser vermelha.\n\nA pipeline roda num runner self-hosted e divide a execução em duas etapas de banco de dados: a suíte completa contra SQLite e de novo contra PostgreSQL. São mais de três mil testes declarados no diretório `api/tests/`, dos quais cerca de 2.850 rodam na pipeline — o resto fica atrás de ignores e markers por rodar engine real de scraping ou depender de modelo com embeddings de verdade. Com esse tamanho, qualquer falso vermelho custa caro: ele esconde o vermelho que importa.\n\n## O comentário que engoliu o comando\n\nO primeiro suspeito era quase cômico. O step de testes unitários do workflow tinha uma linha de comentário **dentro** do bloco `run:` multilinha, no meio da lista de argumentos do pytest:\n\n```yaml\n- name: Run unit tests (fast)\n  run: |\n    pytest api/tests/ \\\n      --ignore=api/tests/test_e2e.py \\\n      --ignore=api/tests/test_ingest*.py \\\n      --ignore=api/tests/test_workflow.py \\\n      # cap 4 workers: -n auto (12) x deps pesadas = worker death\n      -n 4 \\\n      -q \\\n      --tb=short\n```\n\nParece inofensivo. Não é. O YAML entrega esse bloco inteiro ao shell como um único script, e em shell um `#` no meio da linha corta o restante dela — incluindo a barra invertida de continuação. O resultado: o pytest recebia o comando truncado bem antes do `-n 4`. Metade das flags nunca era aplicada, e o comportamento real da pipeline não era o que estava escrito no arquivo. O comentário foi movido pra uma linha própria **acima** da instrução `run:` (o lugar correto pra documentar decisão no YAML), e o comando voltou a ser o que dizia ser.\n\nA ironia: alguém tinha adicionado aquele comentário justamente pra *explicar* por que o `-n 4` existia. A intenção era boa; o lugar, fatal.\n\n## A fixture que nascia com FK ligada\n\nAbaixo da camada engraçada, o bug sério. A fixture que prepara o banco RAG nos testes de crossref desligava a foreign key enforcement do SQLite com um listener de evento `connect` — a forma canônica de fazer isso com SQLAlchemy. Só que listeners `connect` disparam **apenas em conexões novas**. O engine em questão já tinha conexões vivas no pool, criadas antes pelo setup de testes com as pragmas padrão do provider (FK **ON**). O listener nunca rodava pra elas.\n\nResultado: os INSERTs da fixture pegavam uma conexão \"velha\" do pool com `PRAGMA foreign_keys = ON` e explodiam com `FOREIGN KEY constraint failed` — um registro de teste apontando pra um workspace que não existia. O diagnóstico só fechou quando ficou claro que PRAGMA no SQLite é **por conexão**, não por banco.\n\nO conserto cabe em uma linha: `engine.dispose()` após registrar o listener. O pool é descartado, as próximas conexões nascem novas, o listener roda em todas elas, e a fixture passa a operar com FK desligada como previsto. Uma linha no meio de um bug de dois dias.\n\n## Doze workers, zero sobreviventes\n\nCom os testes de verdade rodando de novo, apareceu o problema de escala. O runner self-hosted é a própria máquina de desenvolvimento — e a suíte carrega deps pesadas (parsers de documento, stack científica). Com `-n auto`, o pytest-xdist subia doze workers simultâneos e a máquina simplesmente não tinha memória: workers morriam no meio da execução e o xdist entrava em deadlock esperando resultados que nunca chegariam.\n\nA resposta foram três mudanças cumulativas:\n\n1. **Cap explícito em `-n 4`** — menos paralelismo, mais previsibilidade.\n2. **`pytest-timeout` global de 300s** — nenhum teste pode pendurar o job por 90 minutos de novo; se pendura, morre com stack trace em 5.\n3. **Bordas claras nos testes** — o que depende de rede real ou crawl de verdade virou marker `integration`; o que exige embeddings reais ganhou `skipif` condicional (com o embedder dummy do CI, teste semântico sem semântica é só ruído vermelho).\n\nCom isso, veio à tona o último falso positivo: em execução serial, testes que tocam o mesmo estado de banco brigavam entre si — um flaky de estado FK/SQL que o isolamento por worker do xdist resolveu naturalmente. O que parecia \"teste quebrado\" era \"teste compartilhando estado sem querer\".\n\n## O tabuleiro final\n\n| Pergunta | Resposta | Como descobri |\n|----------|----------|---------------|\n| Por que o pytest rodava sem as flags? | Comentário dentro do `run:` engolia a continuação do shell | Git show do commit que \"arrumava\" o step |\n| Por que FK falhava na fixture? | Pool já tinha conexões com PRAGMA ON; listener só roda em conexão nova | `engine.dispose()` após o listener — e o erro sumiu |\n| Por que workers morriam? | `-n auto` = 12 workers × deps pesadas na máquina de dev | Cap em 4 + timeout global |\n| Por que testes brigavam no serial? | Estado FK/SQL compartilhado entre testes | Isolamento por worker do xdist |\n\n## Aprendizados\n\n1. **Comentário dentro de bloco `run:` multilinha é código, não documentação.** No YAML de CI, o `run:` vira shell — e shell engole `#` junto com a continuação da linha. Decisão de arquitetura se documenta em linha própria acima do step.\n2. **Listener de conexão não é retroativo.** Configuração por-conexão (PRAGMA, `SET`, search_path) precisa garantir *quais* conexões recebem a configuração. Pool existente = conexões com o estado antigo. `dispose()` é mais barato que dois dias de debugging.\n3. **Runner self-hosted herda os limites da máquina.** `-n auto` é uma promessa que a máquina precisa cumprir. Cap explícito + timeout global é o que separa pipeline de aposta.\n4. **Verde instável é pior que vermelho honesto.** A suíte que flakava ensinou mais que a que passava: marker de integration, skip condicional e isolamento de estado agora dizem exatamente o que cada teste verifica.\n\nQuando o último commit entrou e a barra ficou verde nas duas etapas (SQLite e PostgreSQL), o silêncio do GitHub Actions durou só o tempo de eu ler a notificação. Verde chato assim — que ninguém comenta porque não tem nada a corrigir — é o objetivo final de qualquer pipeline.\n\n<Terminal commands={[\n  { cmd: 'git log --since=\"2026-09-06\" --oneline | wc -l', output: '30 commits na campanha (ci/fix/test/build)' },\n  { cmd: 'grep -c \"def test_\" api/tests/ -r | awk -F: \"{s+=\\\\$2} END {print s}\"', output: '3061 testes · ~2858 rodam na pipeline' },\n  { cmd: 'python -m pytest api/tests/ -n 4 -q --tb=short', output: 'SQLite: OK · PostgreSQL (:5433): OK' },\n  { cmd: 'grep -A3 \"Run unit tests\" .github/workflows/ci.yml | head -4', output: '# cap 4 workers — comentario ACIMA do run:' },\n  { cmd: 'grep -c \"pytest-timeout\" pyproject.toml', output: '1 — timeout global 300s anti-penduramento' },\n]} />"
  },
  {
    "path": "descobertas-a-capa-que-nao-mudava.mdx",
    "slug": "descobertas-a-capa-que-nao-mudava",
    "lang": "pt",
    "title": "A capa que não mudava — cache immutable e o fix que foi renomear o arquivo",
    "date": "2026-09-08 08:00:00 -03:00",
    "project": "descobertas",
    "content": "import Terminal from '../../components/TerminalWidget.astro'\n\n## O defeito que ninguém via — menos quem importa\n\nA capa de um post foi recusada. Justo: tinha elementos que pareciam texto — uma fileira de medalhões que o olho lia como escrita. Refiz a capa com um prompt que bania explicitamente qualquer coisa que sugerisse tipografia, validei com OCR em quatro modos de segmentação e três escalas de renderização: zero caracteres detectados. Commit, push, CI verde, deploy concluído.\n\nE a resposta veio curta: \"tem erro sim na capa\".\n\nNão era teimosia. O navegador carregava a imagem **antiga** — a que tinha pseudo-texto. O servidor já servia os bytes novos. Os dois tinham razão ao mesmo tempo, e essa é a pior classe de bug.\n\n## A matemática de um ano\n\nUma capa de blog é o candidato perfeito a cache agressivo: o arquivo muda raramente, o nome é estável, e regenerar o mesmo binário a cada visita seria desperdício. A configuração do deploy refletia isso com um header quase poético:\n\n```\nCache-Control: public, max-age=31536000, immutable\n```\n\nTraduzindo o `31536000`: são 31.536.000 segundos — um ano inteiro. E o `immutable` é a parte radical: ele diz ao navegador \"nem se dê ao trabalho de revalidar\". Sem ele, o browser ainda perguntaria ao servidor se o arquivo mudou. Com ele, a pergunta nunca acontece. O arquivo entra no cache e vira pedra.\n\nO header não estava errado — era a decisão certa *para arquivos que nunca mudam*. O erro foi mudar o conteúdo de um arquivo que tinha prometido não mudar.\n\n## O deploy servia a verdade; o cliente guardava a mentira\n\nConfirmei o divórcio entre servidor e cliente com dois comandos. O histórico do git mostrava o path reescrito duas vezes no mesmo dia — cada reescrita congelou uma versão diferente em caches diferentes:\n\n```bash\ngit log --oneline -- public/covers/capa-do-post.webp\n# a8f64bf  refazer(#36): capa nova (7.2KB, abstrata)\n# df3f67e  refazer(#34): variante sem texto (47.8KB)\n```\n\nE a resposta do servidor confirmava que os bytes novos estavam no ar — o cabeçalho de cache acompanhava cada resposta, prometendo imutabilidade justamente da imagem que acabara de mudar.\n\nO cliente que já tinha visitado o post antes nunca re- requisitaria aquele URL. O `Age` da resposta era baixo porque o *edge* tinha a versão nova — mas o cache do meu navegador nem perguntava ao edge. Duas camadas de cache, e o contrato de imutabilidade valendo nas duas.\n\n## O fix que é um rename\n\nA solução não foi lutar contra o cache — foi respeitar o contrato dele. Asset imutável que muda de conteúdo precisa mudar de nome:\n\n```bash\ngit mv public/covers/capa-do-post.webp public/covers/capa-do-post-v2.webp\n```\n\nMais uma linha no frontmatter de cada versão do post apontando pro novo caminho — e só isso. O build regenerou as páginas, o deploy subiu, e quem abre o post hoje busca um URL que **nunca existiu antes**: zero chance de cache velho, em qualquer navegador, em qualquer rede.\n\nA URL antiga continua respondendo 200 com o conteúdo antigo — e vai continuar por um ano, imutável como prometido. Não é lixo: é o preço do contrato. Nada mais a aponta, então ninguém mais a busca.\n\n## Quando isso mora (e quando não mora)\n\nA regra prática que ficou, em três linhas:\n\n1. **Conteúdo novo no mesmo nome é invisível** pra quem já cacheou — não importa quantos deploys façam.\n2. **Renomear é o cache-bust universal** — funciona em qualquer CDN, sem invalidação manual, sem esperar propagação.\n3. **No frontmatter, o nome da capa é código** — mudou o arquivo, mudam PT e EN juntos, senão o post EN continua mostrando o fantasma.\n\nO caso onde não precisa: conteúdo *realmente* imutável. Bundles com hash no nome (`app.a3f9c2.js`), imagens de posts já publicados e estáveis. Aliás, o hash no nome do bundle é exatamente esse padrão industrializado — ferramentas de build renomeiam sozinhas a cada mudança, porque os engenheiros que escreveram elas também foram mordidos por um arquivo que mudou de conteúdo sem mudar de nome.\n\n## Epílogo: dois bugs que se disfarçam\n\nO que tornou esse bug difícil de aceitar foi a assimetria de evidência: eu via a capa nova (cache limpo, browser sem histórico), Samuel via a velha (cache de um ano, com direito a `immutable`). Nenhum dos dois estava errado — e nenhum print resolvia, porque cada print provava apenas o cache de quem tirou.\n\nCache stale é o irmão gêmeo do estado congelado que já narrei por aqui: nos dois casos, o sistema serve uma foto do passado com a confiança de quem descreve o presente. A diferença é que o cache ao menos assina o contrato — e cabe a quem publica respeitar a cláusula que ele mesmo escreveu.\n\n<Terminal\n  commands={[\n    { label: 'o contrato', cmd: 'curl -sI /covers/capa.webp | grep -i cache' },\n    { label: 'cache-control', cmd: 'public, max-age=31536000, immutable' },\n    { label: 'o fix', cmd: 'git mv capa.webp capa-v2.webp  # URL nova = cache novo' },\n    { label: 'a regra', cmd: 'rework de asset => rename + frontmatter PT/EN' },\n    { label: 'a prova', cmd: 'git log --oneline -- <capa>  # path reescrito 2x = 2 caches congelados' },\n  ]}\n/>"
  },
  {
    "path": "dogwalk-o-app-que-entra-na-tela-de-casa.mdx",
    "slug": "dogwalk-o-app-que-entra-na-tela-de-casa",
    "lang": "pt",
    "title": "O app que entra na tela de casa",
    "date": "2026-09-08 12:00:00 -03:00",
    "project": "dogwalk",
    "content": "import Terminal from '../../components/TerminalWidget.astro'\n\n## A prova que veio no elevador\n\nExiste um teste que nenhum ambiente de staging reproduz: o elevador do prédio.\nO PataPass já tinha service worker, já tinha cache de tiles do mapa em\nIndexedDB, já tinha push notification. No laptop, sempre funcionava. Mas\n\"funcionar\" no laptop é o cenário mais generoso que existe: rede estável, app\naberto na aba, cache quente.\n\nO teste real é o passeador descendo no elevador, sem sinal, abrindo o app pra\nver os horários da semana. Aí o navegador responde do jeito dele: tela de\n\"dinosaur\" de sem conexão, e o usuário fecha. No celular o site ainda por cima\nparece um visitante — abre na aba do Chrome, sem ícone na tela inicial, sem\npresença. Um site que o usuário nunca instala não vira hábito.\n\nA Fase 4F (commit `978f4e75`, 05/09) existiu para resolver esses dois\nproblemas juntos: **fazer o app instalável de verdade** e **dar um destino\ndigno pra navegação offline**.\n\n## Antes de desenhar tela: o que o próprio navegador oferece\n\nA tentação é construir um \"adicionar à tela de início\" artesanal: um modal\nbonito com instruções por plataforma (iOS pra um lado, Android pra outro).\nAlguém já resolvia isso — o critério era só usar o que o padrão oferece.\n\nO mecanismo do Chromium é o evento `beforeinstallprompt`. O navegador emite\nesse evento quando ele **decide** que o site é instalável (manifest decente,\nservice worker registrado, ícones em ordem). Antes disso, nada de modal: o\ncomponente fica dormindo. Essa ordem importa — se o modal aparece sem o evento,\nvocê está pedindo pro usuário instalar algo que o navegador ainda nem considera\ninstalável.\n\n```jsx\n// O banner não decide nada sozinho: só desperta quando o browser autoriza\nuseEffect(() => {\n  if (localStorage.getItem(DISMISS_KEY) === '1') return undefined;\n\n  const onPrompt = (e) => {\n    e.preventDefault(); // impede o mini-infobar nativo\n    setDeferredPrompt(e);\n    setVisible(true);\n  };\n  const onInstalled = () => {\n    setVisible(false);\n    setDeferredPrompt(null);\n    localStorage.setItem(DISMISS_KEY, '1');\n  };\n  window.addEventListener('beforeinstallprompt', onPrompt);\n  window.addEventListener('appinstalled', onInstalled);\n  return () => {\n    window.removeEventListener('beforeinstallprompt', onPrompt);\n    window.removeEventListener('appinstalled', onInstalled);\n  };\n}, []);\n```\n\nDetalhe do `e.preventDefault()`: sem ele, o navegador mostra o mini-infobar\ndele (aquele balãozinho \"adicionar à tela inicial\") por cima do nosso banner.\nDois convites competindo na mesma tela é pior que nenhum.\n\nE o `deferredPrompt` guardado no estado não é decorativo. É ele quem permite\nque o diálogo nativo de instalação dispare a partir do **nosso** botão, com a\nnossa copy, no nosso momento:\n\n```jsx\nconst install = async () => {\n  deferredPrompt.prompt();\n  try {\n    const { outcome } = await deferredPrompt.userChoice;\n    if (outcome === 'accepted') localStorage.setItem(DISMISS_KEY, '1');\n  } catch {\n    // usuário fechou o dialog nativo — nada a fazer\n  }\n  setVisible(false);\n  setDeferredPrompt(null);\n};\n```\n\nO `userChoice` que volta do diálogo (\"accepted\" ou \"dismissed\") define o\ndestino do banner: aceitou, nunca mais aparece; fechou o diálogo nativo, o\nbanner vai embora mas volta na próxima visita. Recusar o banner com o X persiste\na decisão no `localStorage` — o teste de regressão foi exatamente esse: \"não\nreaparece quando já foi dismissado antes\".\n\n## O offline.html e o pecado de cachear HTML\n\nA página offline em si é banal de escrever: um HTML estático de 31 linhas com\num botão \"Tentar novamente\" e dois listeners que recarregam quando a conexão\nvolta. O difícil foi a decisão ao redor dela.\n\nO Workbox facilita demais cachear tudo, e a receita mais comum de PWA\n(\"navigateFallback: /index.html\") resolve o offline devolvendo o shell do app.\nProblema: isso transforma o cache numa máquina do tempo. Depois de um deploy, o\nusuário que navegou offline recebe o `index.html` da **versão antiga**, que\ncarrega os assets da versão nova — quebrando em runtime. Numa plataforma com\ncadência de deploy alta, não é bug raro, é bug garantido.\n\nA decisão no `vite.config.js` foi radical e consciente:\n\n```js\nworkbox: {\n  maximumFileSizeToCacheInBytes: 5 * 1024 * 1024,\n  // NUNCA cachear HTML — causa página antiga após deploy\n  // offline.html no precache: entrada LITERAL (não **/*.html — isso cachearia\n  // index.html, o que navigateFallback=null evita por design p/ pós-deploy).\n  globPatterns: ['**/*.{js,css,ico,png,svg,ttf,woff2}', 'offline.html'],\n  navigateFallback: null,\n  skipWaiting: true,\n  clientsClaim: true,\n}\n```\n\nRepara no detalhe: `offline.html` entra no `globPatterns` **como entrada\nliteral**, nunca por um `**/*.html`. Um glob de HTML pegaria o `index.html`\njunto — exatamente o que a política proíbe. Literal = só o arquivo que a gente\nquer, revisionado pelo Workbox como qualquer asset.\n\nE se o HTML do app nunca é cacheado, quem segura a navegação offline é o\n`setCatchHandler` lá no service worker:\n\n```js\n// Navegação que falha → serve /offline.html (precacheado)\nsetCatchHandler(({ event }) => {\n  if (event.request.mode === 'navigate') {\n    return caches\n      .match('/offline.html', { ignoreSearch: true })\n      .then((resp) => resp || Response.error());\n  }\n  return Response.error();\n});\n```\n\nO `mode === 'navigate'` é o filtro que separa as águas: navegação de documento\nsem rede recebe a página offline; requisição de API, tile ou asset que falha\nsegue o caminho próprio (os tiles, por exemplo, têm a estratégia própria em\nIndexedDB com fallback pra grade cinza). Erro de rede não é mais um abismo\nbranco — cada tipo de recurso tem um plano B.\n\n## Testes: banner é componente, e componente mente menos sob teste\n\nBanner que insiste em aparecer é a pior experiência possível — pior que não ter\nbanner. Então a barra de garantia foi pesada: 5 testes cobrindo os estados que\nimportam.\n\n```bash\nPASS  src/components/UI/__tests__/InstallPromptBanner.test.jsx\n  InstallPromptBanner\n    - nao renderiza sem beforeinstallprompt\n    - renderiza apos o evento e install chama prompt()\n    - dismiss esconde e persiste no localStorage\n    - nao reaparece quando ja foi dismissado antes\n    - esconde quando o app e instalado (appinstalled)\n\n# Fase 4 inteira, mesmo commit:\n#  backend 6/6 (test_profile_work_schedule) + front 5/5 (banner)\n#  + regressao hooks 21/21 + TypeCheck 0 erros\n```\n\nO quinto teste fecha o ciclo: quando o evento `appinstalled` chega (o usuário\ninstalou por outro caminho — pela barra de endereço, por exemplo), o banner\ndesaparece e marca a decisão. Nenhum caminho deixa o convite pendurado na tela\nde quem já entrou.\n\n## O que ficou\n\nA Fase 4F entregou no mesmo commit a trindade PWA: **instalação** (banner\ncomportado que respeita recusa), **offline** (página dedicada com auto-reload\nquando a rede volta) e **integridade de deploy** (navegação offline nunca serve\ncódigo velho). O teste do elevador agora termina diferente: sem sinal, o app\ninstalado abre o shell, a navegação cai na página offline explicando o estado, e\no reload é automático quando o sinal volta. Ninguém vê dinosaur de navegador\nnem página quebrada de versão misturada.\n\nO aprendizado que ficou: PWA não é uma feature, é um contrato. Instalar é o\nusuário dizendo \"me trata como app daqui pra frente\" — e app que abre offline\nmostrando código velho quebrou o contrato antes de qualquer tela carregar.\n\n<Terminal\n  client:load\n  commands={[\n    { cmd: 'git show 978f4e75 --stat | tail -5' },\n    { cmd: '12 files changed, 443 insertions(+), 6 deletions(-)' },\n    { cmd: 'grep -n \"navigateFallback\" vite.config.js' },\n    { cmd: 'navigateFallback: null,   # NUNCA cachear HTML' },\n  ]}\n/>"
  },
  {
    "path": "en/arachne-trinta-commits-ate-o-verde.mdx",
    "slug": "arachne-trinta-commits-ate-o-verde",
    "lang": "en",
    "title": "Thirty commits to green — the week the Arachne CI became the project",
    "date": "2026-09-07 12:00:00 -03:00",
    "project": "arachne",
    "content": "import Terminal from '../../../components/TerminalWidget.astro'\n\nEvery real project has a chapter that never makes the homepage: the week when the CI pipeline becomes the main project. For Arachne, that chapter happened on September 6th and 7th. Thirty commits between Friday's small hours and Saturday morning, all with one goal: making the GitHub status bar stop being red.\n\nThe pipeline runs on a self-hosted runner and splits execution into two database stages: the full suite against SQLite, then again against PostgreSQL. There are over three thousand declared tests in `api/tests/`, of which roughly 2,850 run in the pipeline — the rest sit behind ignores and markers for spawning real scraping engines or requiring a model with actual embeddings. At this scale, any false red is expensive: it hides the red that matters.\n\n## The comment that ate the command\n\nThe first suspect was almost comedic. The unit test step in the workflow had a comment line **inside** the multiline `run:` block, in the middle of the pytest arguments:\n\n```yaml\n- name: Run unit tests (fast)\n  run: |\n    pytest api/tests/ \\\n      --ignore=api/tests/test_e2e.py \\\n      --ignore=api/tests/test_ingest*.py \\\n      --ignore=api/tests/test_workflow.py \\\n      # cap 4 workers: -n auto (12) x heavy deps = worker death\n      -n 4 \\\n      -q \\\n      --tb=short\n```\n\nLooks harmless. It isn't. YAML hands that entire block to the shell as a single script, and in shell a `#` mid-line truncates the rest of the line — including the backslash continuation. The result: pytest received the command truncated well before `-n 4`. Half the flags were never applied, and what the pipeline actually ran was not what the file said. The comment was moved to its own line **above** the `run:` instruction (the right place to document decisions in YAML), and the command became what it claimed to be.\n\nThe irony: someone had added that comment precisely to *explain* why `-n 4` existed. Good intention, fatal location.\n\n## The fixture born with FK enabled\n\nBelow the funny layer sat the serious bug. The fixture that prepares the RAG database in the crossref tests disabled SQLite's foreign key enforcement with a `connect` event listener — the canonical way to do it with SQLAlchemy. But `connect` listeners fire **only on new connections**. The engine in question already had live pooled connections, created earlier by the test setup with the provider's default pragmas (FK **ON**). The listener never ran for them.\n\nThe result: the fixture's INSERTs grabbed a \"stale\" pooled connection with `PRAGMA foreign_keys = ON` and blew up with `FOREIGN KEY constraint failed` — a test record pointing at a workspace that didn't exist. The diagnosis only clicked once it was clear that PRAGMA in SQLite is **per connection**, not per database.\n\nThe fix fits in one line: `engine.dispose()` after registering the listener. The pool is discarded, the next connections are born fresh, the listener fires on every one of them, and the fixture operates with FK disabled as intended. One line in the middle of a two-day bug.\n\n## Twelve workers, zero survivors\n\nWith the real tests running again, the scaling problem surfaced. The self-hosted runner is the dev machine itself — and the suite pulls heavy dependencies (document parsers, the scientific stack). With `-n auto`, pytest-xdist spawned twelve simultaneous workers and the machine simply didn't have the memory: workers died mid-run and xdist deadlocked waiting for results that would never arrive.\n\nThe answer came as three cumulative changes:\n\n1. **Explicit cap at `-n 4`** — less parallelism, more predictability.\n2. **Global `pytest-timeout` of 300s** — no test can hang the job for 90 minutes again; if it hangs, it dies with a stack trace in 5.\n3. **Clear test boundaries** — anything depending on real network or actual crawling became an `integration` marker; anything requiring real embeddings got a conditional `skipif` (with the CI's dummy embedder, a semantic test without semantics is just red noise).\n\nThat flushed out the last false positive: in serial execution, tests touching the same database state fought each other — a FK/SQL state flakiness that xdist's per-worker isolation naturally resolved. What looked like a \"broken test\" was \"tests sharing state by accident\".\n\n## The final board\n\n| Question | Answer | How I found out |\n|----------|----------|---------------|\n| Why did pytest run without the flags? | A comment inside `run:` ate the shell continuation | Git show of the commit \"fixing\" the step |\n| Why did FK fail in the fixture? | The pool already had connections with PRAGMA ON; listeners only run on new connections | `engine.dispose()` after the listener — and the error vanished |\n| Why did workers die? | `-n auto` = 12 workers × heavy deps on the dev machine | Cap at 4 + global timeout |\n| Why did tests fight in serial? | Shared FK/SQL state between tests | xdist per-worker isolation |\n\n## Lessons\n\n1. **A comment inside a multiline `run:` block is code, not documentation.** In CI YAML, `run:` becomes shell — and shell swallows `#` along with the line continuation. Architectural decisions go on their own line above the step.\n2. **Connection listeners are not retroactive.** Per-connection configuration (PRAGMA, `SET`, search_path) needs to guarantee *which* connections receive it. An existing pool = connections with the old state. `dispose()` is cheaper than two days of debugging.\n3. **Self-hosted runners inherit the machine's limits.** `-n auto` is a promise the machine has to keep. An explicit cap plus a global timeout is what separates a pipeline from a gamble.\n4. **Unstable green is worse than honest red.** The flaky suite taught more than the passing one: integration markers, conditional skips, and state isolation now state exactly what each test verifies.\n\nWhen the last commit landed and the bar turned green on both stages (SQLite and PostgreSQL), the silence from GitHub Actions lasted just long enough for me to read the notification. Boring green like that — the kind nobody comments on because there's nothing to fix — is the end goal of any pipeline.\n\n<Terminal commands={[\n  { cmd: 'git log --since=\"2026-09-06\" --oneline | wc -l', output: '30 commits in the campaign (ci/fix/test/build)' },\n  { cmd: 'grep -c \"def test_\" api/tests/ -r | awk -F: \"{s+=\\\\$2} END {print s}\"', output: '3061 tests · ~2858 run in the pipeline' },\n  { cmd: 'python -m pytest api/tests/ -n 4 -q --tb=short', output: 'SQLite: OK · PostgreSQL (:5433): OK' },\n  { cmd: 'grep -A3 \"Run unit tests\" .github/workflows/ci.yml | head -4', output: '# cap 4 workers — comment ABOVE the run:' },\n  { cmd: 'grep -c \"pytest-timeout\" pyproject.toml', output: '1 — global 300s timeout, no more hangings' },\n]} />"
  },
  {
    "path": "en/descobertas-a-capa-que-nao-mudava.mdx",
    "slug": "descobertas-a-capa-que-nao-mudava",
    "lang": "en",
    "title": "The cover that wouldn't change — immutable cache and the fix that was renaming the file",
    "date": "2026-09-08 08:00:00 -03:00",
    "project": "descobertas",
    "content": "import Terminal from '../../../components/TerminalWidget.astro'\n\n## The defect nobody could see — except the person who mattered\n\nA post cover got rejected. Fair enough: it had elements that looked like text — a row of medallions the eye read as lettering. I remade the cover with a prompt that explicitly banned anything suggesting typography, validated it with OCR across four segmentation modes and three rendering scales: zero characters detected. Commit, push, CI green, deployment done.\n\nAnd the reply was short: \"there is indeed an error in the cover\".\n\nThat wasn't stubbornness. The browser was loading the **old** image — the one with the pseudo-text. The server was already serving the new bytes. Both were right at the same time, and that's the worst class of bug.\n\n## The math of a year\n\nA blog cover is the perfect candidate for aggressive caching: the file rarely changes, the name is stable, and regenerating the same binary on every visit would be waste. The deployment config reflected that with an almost poetic header:\n\n```\nCache-Control: 31536000 seconds, immutable, public\n```\n\nTranslating `31536000`: that's 31,536,000 seconds — a full year. And `immutable` is the radical part: it tells the browser \"don't even bother revalidating\". Without it, the browser would still ask the server whether the file changed. With it, the question never happens. The file enters the cache and turns to stone.\n\nThe header wasn't wrong — it was the right decision *for files that never change*. The mistake was changing the contents of a file that had promised not to change.\n\n## The deployment served the truth; the client kept the lie\n\nI confirmed the divorce between server and client with two commands. The git history showed the path rewritten twice on the same day — each rewrite froze a different version in different caches:\n\n```bash\ngit log --oneline -- public/covers/capa-do-post.webp\n# a8f64bf  refazer(#36): new cover (7.2KB, abstract)\n# df3f67e  refazer(#34): text-free variant (47.8KB)\n```\n\nAnd the server response confirmed the new bytes were live — the cache header rode along with every response, promising immutability precisely for the image that had just changed.\n\nA client that had visited the post before would never re-request that URL. The response's `Age` was low because the *edge* had the new version — but my browser's cache wasn't even asking the edge. Two cache layers, and the immutability contract holding in both.\n\n## The fix is a rename\n\nThe solution wasn't to fight the cache — it was to honor its contract. An immutable asset whose contents change needs to change its name:\n\n```bash\ngit mv public/covers/capa-do-post.webp public/covers/capa-do-post-v2.webp\n```\n\nPlus one line in each language version's frontmatter pointing to the new path — and that's it. The build regenerated the pages, the deploy went up, and anyone opening the post today fetches a URL that has **never existed before**: zero chance of stale cache, in any browser, on any network.\n\nThe old URL keeps answering 200 with the old content — and will keep doing so for a year, immutable as promised. It's not garbage: it's the price of the contract. Nothing else points at it anymore, so nobody fetches it again.\n\n## Where this lives (and where it doesn't)\n\nThe practical rule that stuck, in three lines:\n\n1. **New content under the same name is invisible** to anyone who already cached it — no matter how many deployments you make.\n2. **Renaming is the universal cache-bust** — works on any CDN, no manual invalidation, no propagation wait.\n3. **In the frontmatter, the cover filename is code** — changed the file, change PT and EN together, or the EN post keeps showing the ghost.\n\nThe case where you don't need it: *genuinely* immutable content. Bundles with a hash in the name (`app.a3f9c2.js`), images from already-published, stable posts. Actually, the hash in the bundle name is exactly this pattern industrialized — build tools rename automatically on every change because the engineers who wrote them were also bitten by a file that changed contents without changing names.\n\n## Epilogue: two bugs in disguise\n\nWhat made this bug hard to accept was the asymmetry of evidence: I saw the new cover (clean cache, no browsing history), Samuel saw the old one (year-long cache, `immutable` included). Neither was wrong — and no screenshot could settle it, because every screenshot proves only the cache of whoever took it.\n\nStale cache is the twin brother of the frozen state I've narrated here before: in both cases, the system serves a photo of the past with the confidence of someone describing the present. The difference is that the cache at least signs a contract — and it's up to whoever publishes to honor the clause it wrote itself.\n\n<Terminal\n  commands={[\n    { label: 'the contract', cmd: 'curl -sI /covers/capa.webp | grep -i cache' },\n    { label: 'cache-control', cmd: 'public, max-age=31536000, immutable' },\n    { label: 'the fix', cmd: 'git mv capa.webp capa-v2.webp  # new URL = new cache' },\n    { label: 'the rule', cmd: 'asset rework => rename + frontmatter PT/EN' },\n    { label: 'the proof', cmd: 'git log --oneline -- <capa>  # path rewritten 2x = 2 frozen caches' },\n  ]}\n/>"
  },
  {
    "path": "en/dogwalk-o-app-que-entra-na-tela-de-casa.mdx",
    "slug": "dogwalk-o-app-que-entra-na-tela-de-casa",
    "lang": "en",
    "title": "The app that makes it to the home screen",
    "date": "2026-09-08 12:00:00 -03:00",
    "project": "dogwalk",
    "content": "import Terminal from '../../../components/TerminalWidget.astro'\n\n## The proof test arrived in an elevator\n\nThere's a test no staging environment can reproduce: the elevator ride. PataPass\nalready had a service worker, tile caching for the map in IndexedDB, push\nnotifications. On a laptop, everything worked. But \"working\" on a laptop is the\nmost generous scenario there is: stable network, app open in a tab, warm cache.\n\nThe real test is a walker in the elevator, no signal, opening the app to check\nnext week's schedule. And the browser answers the way it always does: the\n\"dinosaur\" no-connection screen, and the user closes the tab. On mobile the site\nwas still just a visitor — it opened in a Chrome tab, with no icon on the home\nscreen, no presence. A site the user never installs never becomes a habit.\n\nPhase 4F (commit `978f4e75`, Sep 5) existed to solve both problems at once:\n**make the app genuinely installable** and **give offline navigation a decent\nplace to land**.\n\n## Before designing any screen: use what the browser already offers\n\nThe temptation is to build a hand-rolled \"add to home screen\" flow: a pretty\nmodal with per-platform instructions (iOS on one side, Android on the other).\nSomeone had already solved this — the only criterion was to use what the\nstandard provides.\n\nIn Chromium the mechanism is the `beforeinstallprompt` event. The browser fires\nit when **it** decides the site is installable (a decent manifest, a registered\nservice worker, icons in place). Before that happens, no modal: the component\nstays asleep. That order matters — if the modal shows up without the event,\nyou're asking the user to install something the browser doesn't even consider\ninstallable yet.\n\n```jsx\n// The banner decides nothing on its own: it only wakes up when the browser authorizes it\nuseEffect(() => {\n  if (localStorage.getItem(DISMISS_KEY) === '1') return undefined;\n\n  const onPrompt = (e) => {\n    e.preventDefault(); // suppress the native mini-infobar\n    setDeferredPrompt(e);\n    setVisible(true);\n  };\n  const onInstalled = () => {\n    setVisible(false);\n    setDeferredPrompt(null);\n    localStorage.setItem(DISMISS_KEY, '1');\n  };\n  window.addEventListener('beforeinstallprompt', onPrompt);\n  window.addEventListener('appinstalled', onInstalled);\n  return () => {\n    window.removeEventListener('beforeinstallprompt', onPrompt);\n    window.removeEventListener('appinstalled', onInstalled);\n  };\n}, []);\n```\n\nA note on `e.preventDefault()`: without it, the browser shows its own\nmini-infobar (that little \"add to home screen\" bubble) on top of our banner.\nTwo invitations competing on the same screen is worse than none.\n\nAnd the `deferredPrompt` we stash in state isn't decorative. It's what allows\nthe native install dialog to fire from **our** button, with our copy, on our\nschedule:\n\n```jsx\nconst install = async () => {\n  deferredPrompt.prompt();\n  try {\n    const { outcome } = await deferredPrompt.userChoice;\n    if (outcome === 'accepted') localStorage.setItem(DISMISS_KEY, '1');\n  } catch {\n    // user closed the native dialog — nothing to do\n  }\n  setVisible(false);\n  setDeferredPrompt(null);\n};\n```\n\nThe `userChoice` that comes back (\"accepted\" or \"dismissed\") decides the\nbanner's fate: accepted, it never shows again; native dialog closed, the banner\ngoes away but returns on the next visit. Dismissing via the X persists the\nchoice in `localStorage` — the regression test covers exactly that case: \"does\nnot reappear once dismissed\".\n\n## The offline.html and the sin of caching HTML\n\nThe offline page itself is trivial: 31 lines of static HTML with a \"Try again\"\nbutton and two listeners that reload once connectivity returns. The hard part\nwas the decision around it.\n\nWorkbox makes it far too easy to cache everything, and the most common PWA\nrecipe (\"navigateFallback: /index.html\") solves offline by serving the app\nshell. The problem: that turns the cache into a time machine. After a deploy,\nthe user who navigated offline gets the **old** `index.html`, which loads the\nnew version's assets — breaking at runtime. On a platform with a high deploy\ncadence this isn't a rare bug, it's a guaranteed one.\n\nThe decision in `vite.config.js` was radical and deliberate:\n\n```js\nworkbox: {\n  maximumFileSizeToCacheInBytes: 5 * 1024 * 1024,\n  // NEVER cache HTML — it causes a stale page after deploy\n  // offline.html goes into precache as a LITERAL entry (not **/*.html — that\n  // would cache index.html, which navigateFallback=null avoids by design).\n  globPatterns: ['**/*.{js,css,ico,png,svg,ttf,woff2}', 'offline.html'],\n  navigateFallback: null,\n  skipWaiting: true,\n  clientsClaim: true,\n}\n```\n\nNote the detail: `offline.html` lands in `globPatterns` **as a literal entry**,\nnever through an `**/*.html` glob. An HTML glob would grab `index.html` too —\nexactly what the policy forbids. Literal means only the file we want, revisioned\nby Workbox like any other asset.\n\nAnd since the app's HTML is never cached, what holds offline navigation is the\n`setCatchHandler` over in the service worker:\n\n```js\n// Failed navigation → serve /offline.html (precached)\nsetCatchHandler(({ event }) => {\n  if (event.request.mode === 'navigate') {\n    return caches\n      .match('/offline.html', { ignoreSearch: true })\n      .then((resp) => resp || Response.error());\n  }\n  return Response.error();\n});\n```\n\n`mode === 'navigate'` is the filter that splits the waters: a document\nnavigation with no network gets the offline page; a failing API request, tile\nor asset follows its own path (tiles, for instance, have their own IndexedDB\nstrategy with a gray-grid fallback). A network error is no longer a white\nabyss — every resource type has a plan B.\n\n## Tests: a banner is a component, and components lie less under test\n\nA banner that insists on showing up is the worst possible experience — worse\nthan having no banner. So the guarantee bar was set high: 5 tests covering the\nstates that matter.\n\n```bash\nPASS  src/components/UI/__tests__/InstallPromptBanner.test.jsx\n  InstallPromptBanner\n    - does not render without beforeinstallprompt\n    - renders after the event and install calls prompt()\n    - dismiss hides it and persists to localStorage\n    - does not reappear once dismissed before\n    - hides when the app is installed (appinstalled)\n\n# The whole of Phase 4, same commit:\n#  backend 6/6 (test_profile_work_schedule) + front 5/5 (banner)\n#  + hooks regression 21/21 + TypeCheck 0 errors\n```\n\nThe fifth test closes the loop: when the `appinstalled` event arrives (the user\ninstalled through another path — the address bar, say), the banner disappears\nand marks the decision. No code path leaves the invitation hanging on the\nscreen of someone who's already in.\n\n## What remains\n\nPhase 4F shipped the PWA trinity in a single commit: **installation** (a\nwell-behaved banner that respects refusal), **offline** (a dedicated page that\nauto-reloads when the network returns) and **deploy integrity** (offline\nnavigation never serves stale code). The elevator test now ends differently: no\nsignal, the installed app opens its shell, navigation lands on the offline page\nexplaining the situation, and the reload happens automatically once the signal\nis back. Nobody sees the browser's dinosaur, and nobody gets a broken page made\nof mixed versions.\n\nThe lesson that stuck: a PWA isn't a feature, it's a contract. Installing is\nthe user saying \"treat me as an app from now on\" — and an app that opens\noffline showing stale code broke the contract before any screen could load.\n\n<Terminal\n  client:load\n  commands={[\n    { cmd: 'git show 978f4e75 --stat | tail -5' },\n    { cmd: '12 files changed, 443 insertions(+), 6 deletions(-)' },\n    { cmd: 'grep -n \"navigateFallback\" vite.config.js' },\n    { cmd: 'navigateFallback: null,   # NEVER cache HTML' },\n  ]}\n/>"
  },
  {
    "path": "en/en-lifelog-o-ciclo-do-nao.mdx",
    "slug": "en-lifelog-o-ciclo-do-nao",
    "lang": "en",
    "title": "The no loop — when every reader rejection became a ticket in the pipeline",
    "date": "2026-09-06 08:00:00 -03:00",
    "project": "lifelog",
    "content": "import Terminal from '../../../components/TerminalWidget.astro'\n\n## The day silence ate a request\n\nThis blog publishes daily, and every post is born hidden. I read, decide what goes live, and when something doesn't sit right with me, the system logs my rejection into a file inside the repository. Simple, traceable, quiet. For months this worked — until I discovered that \"working\" and \"being read\" are different things.\n\nThe rejections file grew. Related issues stayed open. Nothing complained, because everything was \"recorded\". But a record without notification is a message in a bottle: the content is intact, the destination is what's missing. A rewrite request sat idle for days while the pipeline kept its rhythm, happy, unaware it owed an answer.\n\n## Why a file isn't enough\n\nA rejection had three possible destinations and the implementation got two right: a traceable comment in the repository and a physical copy under `docs/recusas/`. What was missing was the link that turns a record into a task — someone (or something) that looks at that file and says \"this needs a response\". The blog had the memory of the request, but no system with a mandate to act on it.\n\nThe fix wasn't creating yet another place to write things down. It was closing the loop: now, when a rejection is recorded, the matching pipeline gets notified and the post returns to the review queue — with the original reason attached, intact, in the same place it was born. The file stayed the source of truth; what changed was who reads it and when.\n\n```js\n// Before: rejection recorded, period.\nawait commitNotaFile(slug, nota);        // file in the repo\nawait createIssue(slug, nota);           // traceable issue\n// ...and the pipeline? The pipeline found out when (or if) someone remembered.\n\n// After: rejection recorded + automatic follow-up\nawait commitNotaFile(slug, nota);\nawait createIssue(slug, nota);\nawait notifyPipeline(slug, nota);        // the loop closes itself\n```\n\n## What the loop teaches about feedback\n\nA system that accepts rejection without reacting teaches the user to stop rejecting. Worse: it teaches that rejecting is pointless. The button existed, worked, saved everything — and still the loop was broken, because whoever receives the feedback was never told they had received it.\n\nThe fix reminded me of a rule I apply in code and forget in processes: **every input needs a named consumer**. Not \"someone will see it eventually\", but a specific component, with a specific trigger, reacting to a specific event. Without that, the input is just archiving with good posture.\n\n| | Rejection as a file | Rejection as a ticket |\n|---|---|---|\n| Record | traceable | traceable |\n| Who reads it | whoever remembers | the pipeline, always |\n| Response time | undefined | one cycle |\n| Silence possible | yes, and common | no |\n\n## The takeaway\n\nThe automation I'm proud of isn't the one that publishes posts by itself. It's the one that guarantees **none of my requests disappear**. If I reject a post, that \"no\" has a path: file, issue, notification, rewrite, re-delivery. The blog became a system that listens and answers — and if the loop ever breaks again, there is now a part of the process whose only job is to notice.\n\n<Terminal\n  commands={[\n    { label: 'origin', cmd: 'POST /api/recusar  {\"path\": \"<slug>\", \"nota\": \"...\"}' },\n    { label: 'record', cmd: 'traceable issue + docs/recusas/<slug>.md' },\n    { label: 'follow-up', cmd: 'pipeline notified -> rewrites -> re-delivers hidden' },\n    { label: 'closure', cmd: 'issue closed when the new version enters the flow' },\n  ]}\n/>"
  },
  {
    "path": "lifelog-o-ciclo-do-nao.mdx",
    "slug": "lifelog-o-ciclo-do-nao",
    "lang": "pt",
    "title": "O ciclo do não — quando cada recusa do leitor virou um ticket no pipeline",
    "date": "2026-09-06 08:00:00 -03:00",
    "project": "lifelog",
    "content": "import Terminal from '../../components/TerminalWidget.astro'\n\n## O dia em que o silêncio comeu um pedido\n\nO blog publica todos os dias, e cada post nasce escondido. Eu leio, decido o que vai pro ar, e quando algo não me agrada, o sistema anota minha recusa num arquivo dentro do repositório. Simples, rastreável, quieto. Por meses isso funcionou — até descobrir que \"funcionar\" e \"ser lido\" são coisas diferentes.\n\nO arquivo de recusas crescia. Issues relacionadas ficavam abertas. Nada reclamava, porque tudo estava \"registrado\". Só que registro sem notificação é mensagem numa garrafa: o conteúdo está intacto, o destino é que não existe. Um pedido de reescrita ficou dias parado enquanto o pipeline seguia seu ritmo, feliz, sem saber que devia algo.\n\n## Por que um arquivo não basta\n\nA recusa tinha três destinos possíveis e a implementação acertou dois: um comentário rastreável no repositório e uma cópia física em `docs/recusas/`. O que faltava era o elo que transforma registro em tarefa — alguém (ou algo) que olhe aquele arquivo e diga \"isto precisa de resposta\". O blog tinha a memória do pedido, mas nenhum sistema com mandato de agir sobre ela.\n\nA correção não foi criar mais um lugar pra escrever. Foi fechar o ciclo: agora, quando uma recusa é registrada, o pipeline correspondente é notificado e o post volta pro fluxo de revisão — com o motivo original anexado, intacto, no mesmo lugar onde nasceu. O arquivo continuou sendo a fonte de verdade; o que mudou foi quem o lê e quando.\n\n```js\n// Antes: recusa registrada, e só.\nawait commitNotaFile(slug, nota);        // arquivo no repo\nawait createIssue(slug, nota);           // issue rastreável\n// ...e o pipeline? O pipeline descobria quando (ou se) alguém lembrava.\n\n// Depois: recusa registrada + cobrança automática\nawait commitNotaFile(slug, nota);\nawait createIssue(slug, nota);\nawait notifyPipeline(slug, nota);        // o ciclo fecha sozinho\n```\n\n## O que o ciclo ensina sobre feedback\n\nUm sistema que aceita rejeição sem reagir ensina o usuário a parar de rejeitar. Pior: ensina que recusar é inútil. O botão existia, funcionava, salvava tudo — e ainda assim o loop estava quebrado, porque quem recebe o feedback nunca foi avisado de que tinha recebido.\n\nO conserto me lembrou de uma regra que aplico no código e esqueço nos processos: **toda entrada precisa de um consumidor nomeado**. Não \"alguém vai ver eventualmente\", mas um componente específico, com gatilho específico, reagindo a um evento específico. Sem isso, a entrada é só arquivamento com passo firme.\n\n| | Recusa como arquivo | Recusa como ticket |\n|---|---|---|\n| Registro | rastreável | rastreável |\n| Quem lê | quem lembrar | o pipeline, sempre |\n| Tempo de resposta | indefinido | um ciclo |\n| Silêncio possível | sim, e comum | não |\n\n## O aprendizado\n\nA automação que me orgulho não é a que publica posts sozinha. É a que garante que **nenhum pedido meu desaparece**. Se eu recuso um post, aquele \"não\" tem um caminho: arquivo, issue, notificação, reescrita, re-entrega. O blog virou um sistema que ouve e responde — e se um dia o ciclo quebrar de novo, agora existe uma parte do processo cujo único trabalho é perceber isso.\n\n<Terminal\n  commands={[\n    { label: 'origem', cmd: 'POST /api/recusar  {\"path\": \"<slug>\", \"nota\": \"...\"}' },\n    { label: 'registro', cmd: 'issue rastreável + docs/recusas/<slug>.md' },\n    { label: 'cobrança', cmd: 'pipeline notificado -> reescreve -> re-entrega oculta' },\n    { label: 'fechamento', cmd: 'issue fechada quando a nova versão entra no fluxo' },\n  ]}\n/>"
  }
];
