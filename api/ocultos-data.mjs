// Gerado automaticamente por scripts/gen-ocultos.mjs
// NÃO editar manualmente

export default [
  {
    "path": "arachne-o-erro-que-salvou-o-banco.mdx",
    "slug": "arachne-o-erro-que-salvou-o-banco",
    "lang": "pt",
    "title": "O erro que salvou o banco de dados",
    "date": "2026-09-08 21:30:00 -03:00",
    "project": "arachne",
    "content": "import Terminal from '../../components/TerminalWidget.astro'\n\nAqui vai um confessionário de backend. Na quarta-feira passada, quase apaguei o banco de dados do Arachne em produção. E o que salvou os dados não foi um backup, nem um guard planejado — foi uma mensagem de erro, do tipo que a gente xinga sem ler.\n\n## O que o teste fazia\n\nO Arachne tem uma suíte que valida o grafo de conhecimento cruzado — o teste cria uma base de conhecimento temporária, roda as consultas e, no teardown, limpa tudo. O teardown varria o metadata do SQLModel e executava `DELETE` em todas as tabelas que começavam com `rag_`, mais três tabelas auxiliares: os templates de KB, as sessões de conversação e os compartilhamentos. Higiene padrão de fixture: o teste nasce limpo, o teste morre limpo.\n\nPara isolar o teste do banco real, o setup apontava `RAG_DATABASE_URL` pra um banco temporário. A variável estava lá. O banco tmp era criado. No papel, perfeito.\n\n## O que deu errado\n\nSó que existe uma diferença entre *setar* uma variável de ambiente e *alguém a ler*. O engine global do app — aquele que a fixture não controla, importado no boot — usa a `database_url` das settings, que aponta pro `sqlite:///arachne.db`. De produção. Com o server na porta 9000, vivo, servindo requisições, escrevendo nele naquele exato momento.\n\nNa rodada da tarde, o teardown executou o DELETE. Não no banco tmp. No banco de produção.\n\nE então aconteceu a coisa mais estranha possível: **o erro salvou tudo**. O SQLite abortou com `database disk image is malformed` — a escrita concorrente do server tinha invalidado a imagem do banco sob o cursor do DELETE, e o mecanismo do SQLite, mais uma barreira do que uma cortesia, recusou continuar. Nenhuma tabela foi tocada. Os dados reais — toda a base de conhecimento indexada, os chunks, os metadados de KB — sobreviveram.\n\nSalvos por sorte. Porque a mesma escrita concorrente que abortou o DELETE podia, num timing diferente, ter deixado o DELETE passar. Ninguém escolheu isso. Não havia guarda, havia coincidência.\n\n## O fix\n\nDuas horas depois, o guard estava no lugar (commit `33529ddd`): o teardown agora compara o path do banco contra a `DATABASE_URL` que o engine **realmente** importa, e só executa o cleanup se o engine apontar pro banco temporário. Se apontar pra qualquer outra coisa — produção, outro ambiente, o que for — o teardown falha alto e não apaga nada.\n\n```python\nfrom app.database import DATABASE_URL as _db_url\nif str(db_path) in str(_db_url or \"\"):\n    # só limpa se o engine estiver no banco tmp\n    ...\n```\n\nUma comparação de string. Cinco linhas. A diferença entre \"salvo por sorte\" e \"impossível por design\".\n\n## As lições que ficaram\n\n**Variável de ambiente não é contrato.** O teste setava `RAG_DATABASE_URL` com a melhor das intenções, mas quem decide o destino do `engine.begin()` é o módulo que *consome* a configuração — e esse módulo lia outra coisa. Antes de confiar num env var, ler quem a consome. Intenção não roda código.\n\n**Operação destrutiva em fixture precisa de guarda explícita.** \"O teste limpa o que criou\" é uma boa regra até o dia em que o cleanup apaga o que *outro processo* criou. A guarda certa é fail-closed: só execute a destruição quando você puder provar que está no lugar certo. Em caso de dúvida, abortar é o comportamento correto.\n\n**Salvo por sorte é dívida.** O incidente não causou dano — dessa vez. Mas o mecanismo que salvou (concorrência de escrita) era aleatório, e aleatoriedade que protege dados é uma loteria rodando contra você. O guard transformou a loteria em determinismo.\n\n**Os erros que a gente xinga são os melhores funcionários.** O `malformed` do SQLite é feio, genérico e parece bug. Naquele dia foi um guarda-costas. Prefiro esse erro barulhento em dev do que o silêncio elegante que apaga produção sem avisar.\n\n## O que vem depois\n\nA suíte agora roda com a guarda em todos os teardowns destrutivos, e o padrão virou checklist: qualquer fixture que apague algo prova primeiro *onde* está apagando. A próxima fase de endurecimento do Arachne ataca exatamente essa classe de risco — configuração que parece isolar mas não isola.\n\n<Terminal commands={[\n  { cmd: \"grep -rc \\\"def test_\\\" api/tests/*.py | awk -F: \\\"{s+=$2} END {print s}\\\"\", output: \"3061 testes no diretório api/tests/\" },\n  { cmd: \"python -m pytest api/tests/ -n 2 -q --tb=short\", output: \"SQLite: OK · PostgreSQL (:5433): OK\" },\n  { cmd: \"grep -rn \\\"DATABASE_URL\\\" app/database.py | head -1\", output: \"engine global importa settings.database_url — fonte única de verdade\" },\n  { cmd: \"git show 33529ddd --stat | tail -1\", output: \"api/tests/test_knowledge_graph_crossref.py | 17 insertions(+), 8 deletions(-)\" },\n]} />"
  },
  {
    "path": "en/arachne-o-erro-que-salvou-o-banco.mdx",
    "slug": "arachne-o-erro-que-salvou-o-banco",
    "lang": "en",
    "title": "The error that saved the database",
    "date": "2026-09-08 21:30:00 -03:00",
    "project": "arachne",
    "content": "import Terminal from '../../../components/TerminalWidget.astro'\n\nHere's a backend confession. Last Wednesday I nearly deleted the Arachne production database. And what saved the data wasn't a backup, nor a planned guard — it was an error message, the kind you curse without reading.\n\n## What the test did\n\nArachne has a suite that validates the cross-referenced knowledge graph — the test creates a temporary knowledge base, runs the queries, and cleans everything up on teardown. The teardown walked the SQLModel metadata and issued `DELETE` against every table starting with `rag_`, plus three helper tables: KB templates, conversation sessions, and shares. Standard fixture hygiene: the test is born clean, the test dies clean.\n\nTo isolate the test from the real database, the setup pointed `RAG_DATABASE_URL` at a temporary database. The variable was there. The tmp database was created. On paper, flawless.\n\n## What went wrong\n\nThere's a difference between *setting* an environment variable and *someone reading it*. The app's global engine — the one the fixture doesn't control, imported at boot — uses the settings' `database_url`, which points at `sqlite:///arachne.db`. Production. With the server on port 9000, alive, serving requests, writing to it at that exact moment.\n\nThat afternoon run, the teardown executed the DELETE. Not on the tmp database. On production.\n\nAnd then the strangest thing happened: **the error saved everything**. SQLite aborted with `database disk image is malformed` — the server's concurrent write had invalidated the database image under the DELETE's cursor, and SQLite's mechanism, more of a barrier than a courtesy, refused to continue. Not a single table was touched. The real data — the whole indexed knowledge base, the chunks, the KB metadata — survived.\n\nSaved by luck. Because the same concurrent write that aborted the DELETE could, on a different timing, have let the DELETE through. Nobody chose that. There was no guard, there was coincidence.\n\n## The fix\n\nTwo hours later the guard was in place (commit `33529ddd`): the teardown now compares the database path against the `DATABASE_URL` the engine **actually** imports, and only runs the cleanup if the engine points at the temporary database. If it points anywhere else — production, another environment, whatever — the teardown fails loudly and deletes nothing.\n\n```python\nfrom app.database import DATABASE_URL as _db_url\nif str(db_path) in str(_db_url or \"\"):\n    # only clean up if the engine is on the tmp database\n    ...\n```\n\nA string comparison. Five lines. The difference between \"saved by luck\" and \"impossible by design\".\n\n## The lessons\n\n**An environment variable is not a contract.** The test set `RAG_DATABASE_URL` with the best of intentions, but what decides where `engine.begin()` lands is the module that *consumes* the configuration — and that module read something else. Before trusting an env var, read who consumes it. Intent doesn't run code.\n\n**Destructive fixtures need explicit guards.** \"The test cleans up what it created\" is a good rule until the cleanup deletes what *another process* created. The right guard is fail-closed: only execute destruction when you can prove you're in the right place. In doubt, aborting is the correct behavior.\n\n**Saved by luck is debt.** The incident caused no damage — this time. But the mechanism that saved it (write concurrency) was random, and randomness that protects data is a lottery running against you. The guard turned the lottery into determinism.\n\n**The errors we curse are the best employees.** SQLite's `malformed` is ugly, generic and looks like a bug. That day it was a bodyguard. I'll take that loud error in dev over the elegant silence that wipes production without a word.\n\n## What's next\n\nThe suite now runs with the guard on every destructive teardown, and the pattern became a checklist: any fixture that deletes something first proves *where* it is deleting. The next hardening phase for Arachne targets exactly this class of risk — configuration that looks isolating but isn't.\n\n<Terminal commands={[\n  { cmd: \"grep -rc \\\"def test_\\\" api/tests/*.py | awk -F: \\\"{s+=$2} END {print s}\\\"\", output: \"3061 tests in the api/tests/ directory\" },\n  { cmd: \"python -m pytest api/tests/ -n 2 -q --tb=short\", output: \"SQLite: OK · PostgreSQL (:5433): OK\" },\n  { cmd: \"grep -rn \\\"DATABASE_URL\\\" app/database.py | head -1\", output: \"the global engine imports settings.database_url — single source of truth\" },\n  { cmd: \"git show 33529ddd --stat | tail -1\", output: \"api/tests/test_knowledge_graph_crossref.py | 17 insertions(+), 8 deletions(-)\" },\n]} />"
  }
];
