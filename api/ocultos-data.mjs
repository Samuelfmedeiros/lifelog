// Gerado automaticamente por scripts/gen-ocultos.mjs
// NÃO editar manualmente

export default [
  {
    "path": "capivara-o-cadeado-verde.mdx",
    "slug": "capivara-o-cadeado-verde",
    "lang": "pt",
    "title": "O cadeado verde que não via o vazamento — path traversal no Capivara",
    "date": "2026-09-03 18:30:00 -03:00",
    "project": "capivara",
    "content": "import Terminal from '../../components/TerminalWidget.astro'\n\n## O painel tinha alarme de fumaça — e o prédio estava pegando fogo\n\nO Capivara é o hub pessoal do meu ecossistema: JWT com refresh, bcrypt, 2FA TOTP, Cloudflare Tunnel, rate limit em três camadas. E desde agosto ele também tinha um **gate de segurança semanal**: gitleaks caçando segredo commitado, bandit caçando anti-pattern em Python, opengrep caçando padrão perigoso com regras que eu mesmo escrevi — secret hardcoded, SQL por concatenação, log de dado sensível, cripto fraca, eval. Todo início de semana, tudo verde.\n\nVerde, nesse contexto, significava: **nenhum segredo chegou ao repositório**.\n\nSó que segredo commitado é só UMA das formas de vazar. A que me mordeu funcionava assim: uma rota que serve os arquivos do build do frontend recebe o path da URL e monta o caminho no servidor com uma divisão de string:\n\n```python\n# a linha de 20 caracteres que quase custou o .env inteiro\nfile_path = FRONTEND_DIST / path\n```\n\nCom um path normal (`/assets/index-abc123.js`), isso funciona perfeitamente — é assim que todo SPA servido por backend funciona. O detalhe é que o operador de divisão do Path **apenas junta strings**; quem resolve os `..` é o sistema de arquivos do servidor, na hora de abrir o arquivo. Encode os pontos na URL — `%2e%2e` — e uma requisição como esta:\n\n```text\nGET /%2e%2e/%2e%2e/backend/.env\n```\n\nvoltava **200 com o .env inteiro**. Em produção. O mesmo .env que carrega o secret que assina os JWT de login — o segredo que, uma vez fora, permite forjar sessão válida no hub inteiro. Não era injection de banco, não era XSS, não era força bruta: era o meu próprio código servindo o arquivo com a educação de sempre.\n\n## Por que o gate nunca ia ver isso\n\nO gitleaks caça **texto no histórico do git**. O bandit caça **chamadas de API suspeitas** por assinatura. O opengrep caça **padrões de linha** com as regras que eu escrevi. Nenhum dos três entende o que a minha linha realmente fazia — porque o bug não morava no texto dela. Morava na **semântica da execução**: no que aquela divisão *faz* quando recebe `../` url-encoded.\n\nEsse é o ponto central da história: **o ambiente de execução completa o código**. A linha inocente da divisão só vira vulnerabilidade quando você junta três coisas — o path que o usuário controla, a resolução silenciosa de `..` pelo filesystem, e a raiz de busca que é a pasta do build. Tirando o path do usuário, o resto é tudo API padrão, documentada, inofensiva. O scanner de linha vê API padrão e passa batido.\n\nTem um agravante que me incomoda até agora: eu **conhecia** path traversal. Sabia da classe inteira, já tinha lido write-ups, já tinha visto CVE com esse formato exato. Mas o meu modelo mental do Capivara era \"hub pessoal, superfície pequena\" — e superfície pequena virou desculpa psicológica pra não aplicar o checklist que eu aplicaria num produto comercial. Ataque não lê o seu modelo mental. A rota existia, pública, atrás do tunnel — era só isso que o atacante (fortuito ou não) precisava.\n\n## Quem achou não foi o scanner — foi outro agente\n\nO achado veio do **security reviewer agêntico** que roda dentro do meu test-loop: um agente com mandato de atacar o código como atacante de verdade — não de conferir padrão, mas de **percorrer rota por rota** perguntando \"o que acontece se eu passar isso?. Aqui ele pegou a rota de estáticos, mandou o traversal url-encoded e recebeu o .env de volta.\n\nO gate determinístico rodou na mesma semana. As duas auditorias olharam o mesmo código. Uma viu \"divisão de path, API padrão\". A outra viu \"rota pública + join + raiz de busca = leitura arbitrária\". O gate confere conformidade; o agente conduz investigação. **São perguntas diferentes** — e a segunda é a que acha buraco.\n\nVale ser honesto sobre o trade-off: o gate é barato, roda sozinho toda semana e cobre o que é cobrível por padrão — e continua lá. O reviewer agêntico é caro, roda em episódios, e a qualidade depende de quem manda a pergunta. Um não substitui o outro. O erro seria escolher um dos dois; a configuração que funcionou foi os dois, com mandatos distintos.\n\n## O fix: resolver, ancorar, rejeitar\n\nA correção não foi \"filtrar `..` do path\" — blocklist de encoding é jogo de whack-a-mole (percent, duplo percent, unicode, barra invertida...). A correção robusta inverte a lógica: **resolve o caminho e exige que ele continue dentro da raiz**:\n\n```python\nraw = FRONTEND_DIST / path\ntry:\n    file_path = raw.resolve()\n    dist_root = FRONTEND_DIST.resolve()\nexcept OSError:\n    file_path = raw\n    dist_root = FRONTEND_DIST\nif not file_path.is_relative_to(dist_root):\n    # traversal fora do dist → 404 (não vaza, não informa existência)\n    return JSONResponse({\"error\": \"Not found\"}, status_code=404)\n```\n\nTrês decisões nesses quinze linhas valem comentário:\n\n1. **`resolve()` antes de comparar** — normaliza `../`, encodings e atalhos para o caminho real no disco. A comparação passa a ser entre caminhos canônicos, não entre strings.\n2. **`is_relative_to()` como âncora** — o caminho resolvido precisa estar *dentro* da raiz do build. Se saiu, é traversal, ponto. Não importa como o atacante escreveu o path.\n3. **404 genérico** — sem mensagem que distinga \"arquivo existe mas bloqueado\" de \"arquivo não existe\". Resposta que informa existência também é vazamento.\n\nE o routing do SPA sobreviveu intacto — o fallback pra `index.html` continua funcionando porque o caminho resolvido da SPA continua dentro do dist. A âncora não quebrou a funcionalidade; quebrou só o atacante.\n\n## A segunda camada: o proxy que virou rota alternativa\n\nUm dia depois, o mesmo reviewer voltou e achou a mesma classe num lugar diferente: o proxy que encaminha assets estáticos do analytics pra dentro do hub. O path da URL era interpolado **cru** no forward:\n\n```python\nresp = await client.get(f\"http://localhost:3100/_next/static/{path}\")\n```\n\nSem normalização, um `/_next/static/../../api/auth/login` virava, do lado de dentro, uma requisição ao endpoint protegido do analytics **sem o header de secret que o proxy exige nas rotas protegidas**. Mesma família do traversal, mesmo hormônio: path controlado pelo usuário costurado dentro de um destino interno. O hardening ficou em duas camadas:\n\n```python\nnorm = posixpath.normpath(unquote(path))\nif norm.startswith(\"..\") or \"/..\" in f\"/{norm}\":\n    return Response(status_code=404)\n# defense-in-depth: rejeitar qualquer forma absoluta (scheme, drive, barra)\nif norm.startswith(\"/\") or \":\" in norm.split(\"/\")[0] or norm == \"\":\n    return Response(status_code=404)\n```\n\nNormalizar primeiro, rejeitar residual depois — e ainda uma segunda rejeição pra formas absolutas que o normpath não cobre. Não porque cada camada seja infalível, mas porque **a lição do primeiro buraco foi exatamente que uma única camada, por melhor desenhada, um dia falha junto com a premissa dela**.\n\n## Aprendizados\n\n1. **Gate verde não é ausência de vazamento — é ausência do vazamento que o gate sabe procurar.** Gitleaks cobre segredo commitado; não cobre rota que lê arquivo. Saber o que o seu gate NÃO cobre vale mais do que o relatório dele.\n\n2. **O ambiente de execução completa o código.** A linha vulnerável era uma divisão de path perfeitamente legível. O bug não estava no texto — estava no encontro da linha com filesystem, encoding e rota pública. Scanner de linha não executa semântica.\n\n3. **Conformidade e investigação são perguntas diferentes.** \"O código segue os padrões?\" e \"como eu quebro isso?\" produzem resultados incomensuráveis. O gate responde a primeira; o reviewer agêntico, a segunda. São complementares, não concorrentes.\n\n4. **A correção canônica é âncora, não blocklist.** Resolve + valida que o caminho ficou dentro da raiz cobre todas as encodings e variantes de uma vez. Filtrar padrão conhecido é whack-a-mole: você sempre fica atrás do próximo encoding.\n\n5. **Defense-in-depth não é paranoia — é memória.** A segunda camada no proxy existe porque a primeira lição da semana foi que uma única camada falha junto com a premissa dela. Camada extra é barato; a premissa errada é caro.\n\n## O que vem a seguir\n\n- Mapear as rotas restantes que interpolam path do usuário em destino de filesystem ou rede interna — auditoria de costura, não de padrão.\n- Trazer o reviewer agêntico pro gate semanal: as regras que ele aprendeu a procurar viram regras opengrep na semana seguinte.\n- Cadeia de rotação: se o .env vazou em produção, a resposta canônica é rotacionar tudo que estava no arquivo — plano de resposta a incidente documentado e testado.\n- Revisar a superfície do ecossistema com a mesma lente: hub com superfície pequena não é hub sem traversal.\n\n<Terminal\n  lines={[\n  { cmd: 'curl -s -o /dev/null -w \"%{http_code}\" https://capivara.seu.pet/%2e%2e/%2e%2e/backend/.env', output: '404' },\n  { cmd: 'git log --oneline -1 d80c267', output: 'fix(critical): path traversal no SPA — LEAK de segredos em produção' },\n  { cmd: 'git log --oneline -1 ed201dd', output: 'fix(sec): hardening defense-in-depth no proxy _next/static' },\n  { cmd: 'git log --oneline -1 9d6a7f6', output: 'fix(sec): path traversal no proxy Umami — normalizar path antes do check' },\n  { cmd: 'cat .security/opengrep-rules.yml | head -8', output: 'secret-key-hardcoded · sql-string-concatenation · debug-print-leaking-data' },\n  ]}\n/>\n\n> **TL;DR:** o gate de segurança semanal do Capivara (gitleaks + bandit + opengrep) estava verde enquanto um path traversal devolvia o .env inteiro em produção. O buraco era uma divisão de path sem âncora — invisível pra scanner de linha e óbvio pra um reviewer que percorre rotas como atacante. Fix canônico: resolve() + is_relative_to() na raiz do build, 404 genérico, e hardening em duas camadas no proxy interno. Conformidade e investigação são perguntas diferentes — e o pipeline de segurança precisa das duas."
  },
  {
    "path": "en/capivara-o-cadeado-verde.mdx",
    "slug": "capivara-o-cadeado-verde",
    "lang": "en",
    "title": "The green padlock that couldn't see the leak — path traversal in Capivara",
    "date": "2026-09-03 18:30:00 -03:00",
    "project": "capivara",
    "content": "import Terminal from '../../../components/TerminalWidget.astro'\n\n## The dashboard had a smoke alarm — and the building was on fire\n\nCapivara is the personal hub of my ecosystem: JWT with refresh, bcrypt, 2FA TOTP, Cloudflare Tunnel, rate limiting in three layers. And since August it also had a **weekly security gate**: gitleaks hunting committed secrets, bandit hunting Python anti-patterns, opengrep hunting dangerous patterns with rules I wrote myself — hardcoded secrets, SQL string concatenation, logging of sensitive data, weak crypto, eval. Every start of the week, everything green.\n\nGreen, in that context, meant: **no secret ever reached the repository**.\n\nBut a committed secret is only ONE way to leak. The one that bit me worked like this: a route serving the frontend build files takes the path from the URL and assembles the server-side path with a string division:\n\n```python\n# the 20-character line that almost cost the whole .env\nfile_path = FRONTEND_DIST / path\n```\n\nWith a normal path (`/assets/index-abc123.js`), this works perfectly — that's how every SPA served by a backend works. The catch: the Path division operator **just joins strings**; whoever resolves the `..` is the server's filesystem, at the moment of opening the file. URL-encode the dots — `%2e%2e` — and a request like this:\n\n```text\nGET /%2e%2e/%2e%2e/backend/.env\n```\n\ncame back **200 with the entire .env**. In production. The same .env carrying the secret that signs the login JWTs — the secret that, once out, lets anyone forge a valid session on the whole hub. Not a database injection, not XSS, not brute force: my own code serving the file with its usual politeness.\n\n## Why the gate would never see it\n\nGitleaks hunts **text in the git history**. Bandit hunts **suspicious API calls** by signature. Opengrep hunts **line patterns** with the rules I wrote. None of the three understands what my line actually did — because the bug didn't live in its text. It lived in the **semantics of execution**: in what that division *does* when it receives a URL-encoded `../`.\n\nThat's the core of this story: **the runtime environment completes the code**. The innocent division only becomes a vulnerability when you combine three things — a user-controlled path, the filesystem's silent resolution of `..`, and a search root that is the build folder. Remove the user-controlled path and everything else is standard, documented, harmless API. The line scanner sees standard API and moves on.\n\nThere's an aggravating factor that still bothers me: I **knew** path traversal. I knew the whole class, had read the write-ups, had seen CVEs with this exact shape. But my mental model of Capivara was \"personal hub, small attack surface\" — and small surface became a psychological excuse not to apply the checklist I would apply to a commercial product. Attacks don't read your mental model. The route existed, public, behind the tunnel — and that was all an attacker needed.\n\n## The one who found it wasn't the scanner — it was another agent\n\nThe finding came from the **agentic security reviewer** that runs inside my test-loop: an agent with a mandate to attack the code like a real attacker — not to check patterns, but to **walk route by route** asking \"what happens if I pass this?\". It hit the static files route, sent the URL-encoded traversal, and got the .env back.\n\nThe deterministic gate ran the same week. Both audits looked at the same code. One saw \"path division, standard API\". The other saw \"public route + join + search root = arbitrary file read\". The gate checks compliance; the agent conducts an investigation. **They are different questions** — and the second is the one that finds holes.\n\nTo be honest about the trade-off: the gate is cheap, runs alone every week, and covers what's coverable by pattern — and it stays. The agentic reviewer is expensive, runs in episodes, and its quality depends on whoever frames the question. Neither replaces the other. The setup that worked was both, with distinct mandates.\n\n## The fix: resolve, anchor, reject\n\nThe fix wasn't \"filter `..` from the path\" — blocklisting encodings is whack-a-mole (percent, double percent, unicode, backslash...). The robust fix inverts the logic: **resolve the path and demand it stays inside the root**:\n\n```python\nraw = FRONTEND_DIST / path\ntry:\n    file_path = raw.resolve()\n    dist_root = FRONTEND_DIST.resolve()\nexcept OSError:\n    file_path = raw\n    dist_root = FRONTEND_DIST\nif not file_path.is_relative_to(dist_root):\n    # traversal outside dist → 404 (no leak, no existence disclosure)\n    return JSONResponse({\"error\": \"Not found\"}, status_code=404)\n```\n\nThree decisions in those fifteen lines are worth commenting on:\n\n1. **`resolve()` before comparing** — normalizes `../`, encodings and shortcuts into the real on-disk path. The comparison becomes canonical paths, not strings.\n2. **`is_relative_to()` as the anchor** — the resolved path must be *inside* the build root. If it got out, it's traversal, period. It doesn't matter how the attacker wrote the path.\n3. **Generic 404** — no message distinguishing \"file exists but blocked\" from \"file doesn't exist\". A response that reveals existence is also a leak.\n\nAnd SPA routing survived untouched — the fallback to `index.html` keeps working because the resolved SPA path is still inside the dist. The anchor didn't break the feature; it broke only the attacker.\n\n## The second layer: the proxy that became a side door\n\nA day later, the same reviewer found the same class somewhere else: the proxy that forwards analytics static assets into the hub. The URL path was interpolated **raw** into the forward:\n\n```python\nresp = await client.get(f\"http://localhost:3100/_next/static/{path}\")\n```\n\nWithout normalization, a `/_next/static/../../api/auth/login` became, on the inside, a request to the analytics protected endpoint **without the secret header the proxy demands on protected routes**. Same family as the traversal, same hormone: a user-controlled path stitched into an internal destination. The hardening came in two layers:\n\n```python\nnorm = posixpath.normpath(unquote(path))\nif norm.startswith(\"..\") or \"/..\" in f\"/{norm}\":\n    return Response(status_code=404)\n# defense-in-depth: reject any absolute form (scheme, drive, leading slash)\nif norm.startswith(\"/\") or \":\" in norm.split(\"/\")[0] or norm == \"\":\n    return Response(status_code=404)\n```\n\nNormalize first, reject residue after — plus a second rejection for absolute forms normpath doesn't cover. Not because each layer is infallible, but because **the lesson of the first hole was precisely that a single layer, however well designed, eventually fails together with its premise**.\n\n## Lessons\n\n1. **A green gate is not the absence of leaks — it's the absence of the leaks the gate knows how to look for.** Gitleaks covers committed secrets; it doesn't cover a route that reads files. Knowing what your gate does NOT cover is worth more than its report.\n\n2. **The runtime environment completes the code.** The vulnerable line was a perfectly readable path division. The bug wasn't in the text — it was in the line's meeting with the filesystem, encodings and a public route. Line scanners don't execute semantics.\n\n3. **Compliance and investigation are different questions.** \"Does the code follow the standards?\" and \"how do I break this?\" produce incomparable results. The gate answers the first; the agentic reviewer, the second. Complements, not competitors.\n\n4. **The canonical fix is an anchor, not a blocklist.** Resolve + validate the path stayed inside the root covers every encoding at once. Filtering known patterns is whack-a-mole: you're always behind the next encoding.\n\n5. **Defense-in-depth isn't paranoia — it's memory.** The second layer in the proxy exists because that week's first lesson was that a single layer fails together with its premise. An extra layer is cheap; a wrong premise is expensive.\n\n## What comes next\n\n- Map the remaining routes that interpolate user input into filesystem or internal network destinations — a stitching audit, not a pattern audit.\n- Bring the agentic reviewer into the weekly gate: what it learns to hunt becomes opengrep rules the following week.\n- Rotation chain: if the .env leaked in production, the canonical response is rotating everything in that file — a documented, tested incident response plan.\n- Review the ecosystem's surface with the same lens: a hub with a small surface is not a hub without traversal.\n\n<Terminal\n  lines={[\n  { cmd: 'curl -s -o /dev/null -w \"%{http_code}\" https://capivara.seu.pet/%2e%2e/%2e%2e/backend/.env', output: '404' },\n  { cmd: 'git log --oneline -1 d80c267', output: 'fix(critical): path traversal in SPA — production secret leak' },\n  { cmd: 'git log --oneline -1 ed201dd', output: 'fix(sec): defense-in-depth hardening on the _next/static proxy' },\n  { cmd: 'git log --oneline -1 9d6a7f6', output: 'fix(sec): path traversal in Umami proxy — normalize path before the check' },\n  { cmd: 'cat .security/opengrep-rules.yml | head -8', output: 'secret-key-hardcoded · sql-string-concatenation · debug-print-leaking-data' },\n  ]}\n/>\n\n> **TL;DR:** Capivara's weekly security gate (gitleaks + bandit + opengrep) was green while a path traversal returned the entire .env in production. The hole was an unanchored path division — invisible to line scanners, obvious to a reviewer walking routes like an attacker. Canonical fix: resolve() + is_relative_to() against the build root, generic 404, and two-layer hardening on the internal proxy. Compliance and investigation are different questions — and a security pipeline needs both."
  }
];
