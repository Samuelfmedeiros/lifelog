// Gerado automaticamente por scripts/gen-ocultos.mjs
// NÃO editar manualmente

export default [
  {
    "path": "en/estudos-a-coluna-que-nunca-existiu.mdx",
    "slug": "estudos-a-coluna-que-nunca-existiu",
    "lang": "en",
    "title": "The column that never existed — when the test agrees with the bug",
    "date": "2026-09-14 08:00:00 -03:00",
    "project": "estudos",
    "content": "import Terminal from '../../../components/TerminalWidget.astro'\n\n## Nine days without collecting a single link\n\nThere is a piece in my ecosystem whose job is simple to describe: it reads the agent's conversation database, picks up Telegram messages containing a URL, downloads the page, extracts title and body, and writes the result into vector memory. A bridge between what I talk about and what I can look up later.\n\nIt ran every thirty minutes. For nine days. Not one of those runs collected a single link.\n\nThe gap isn't the worst part. The worst part is that every one of those runs finished with exit code zero — which is precisely what a scheduler consults when deciding whether to bother anybody.\n\n## What the query asked for\n\nThe message selection joins `messages` with `sessions` and brings the chat name along with the content — because every memory written needs to know where it came from. That detail is where this lives. The query asked:\n\n```sql\nSELECT m.id, s.chat_id, s.chat_title, m.content\nFROM messages m\nJOIN sessions s ON s.id = m.session_id\n```\n\nThat `chat_title` column doesn't exist. Not a migration drift, not a renamed column that disappeared from my database one day. It **never existed** in the production schema. The real name, the one the agent always wrote, is `display_name`.\n\nSQLite raises `no such column: s.chat_title` as soon as the statement is prepared — loud, unmistakable, impossible to read as success. And this is where it gets interesting: the broad `except` wrapping the whole block turned that exception into a JSON with `\"status\": \"erro\"`. An honest status. It just came along with a zero exit code.\n\nA pipeline that reports failure while exiting as success is a pipeline nobody will look at. That is what kept it silent for nine days.\n\n## The part that stings: I had already fixed that name\n\nOn the seventh — six days before the discovery — I worked on the other consumer of the same database: the watcher that captures conversations in real time. Its queries asked for `chat_title` too. I corrected it to `display_name` and ran a backfill that patched 2,906 old points left without a chat name.\n\nSo: I knew the right column name. I had written it in a commit message five days earlier. And the link bridge kept asking for the wrong one until the thirteenth.\n\nBecause the bridge had tests. Green tests. And a green test is the most expensive thing there is when it's a false one.\n\n## The test agreed with the bug\n\nThe test fixture built its own database by hand. And its `CREATE TABLE` carried the exact line I should have checked:\n\n```sql\n-- fixture (wrong, mirroring the query)\nCREATE TABLE sessions (id INTEGER PRIMARY KEY, chat_id TEXT, chat_title TEXT, source TEXT)\n```\n\nThe test then ran a query asking for a column that existed in a table it had invented itself. It passed. Every run, every cycle, every integration suite. It wasn't testing the bridge against the database — it was testing the bridge against my memory of the database, and my memory was wrong in the same way the code was.\n\nThe first line of the fix wasn't the query. It was the fixture:\n\n```sql\n-- fixture (mirroring the real schema)\nCREATE TABLE sessions (id INTEGER PRIMARY KEY, chat_id TEXT, display_name TEXT, source TEXT)\n```\n\nWithout that, every new test written afterwards would keep proving the same false thing. Two lines corrected across three fixtures — and only then did the rest of the fix mean anything.\n\n## The cure in three layers\n\n**One: the query stops picking columns in the dark.** Instead of swapping one name for another — which would only hold until the next rename — resolution now happens against the live database, at run time. A small function, closed candidate list:\n\n```python\n@staticmethod\ndef _session_title_col(con) -> str:\n    \"\"\"Resolve the chat-name column against the REAL state.db schema.\"\"\"\n    cols = {r[1] for r in con.execute(\"PRAGMA table_info(sessions)\")}\n    for cand in (\"chat_title\", \"display_name\", \"title\"):\n        if cand in cols:\n            return f\"s.{cand}\"\n    return \"''\"\n```\n\nThe order is deliberate: if the legacy name ever comes back in a fork it wins; today's name follows; `title` is a legitimate last resort. And if none exist, it returns an empty string — the scan keeps collecting links without a chat name instead of taking everything down. A chat name is enrichment; the link is the job.\n\n**Two: regression against the real schema.** Two new tests, one of them exactly the scenario that died in production — a database with `display_name` and no `chat_title`, asserting real ingestion with the chat name propagated into metadata. The other covers the total absence of a name column, which is the case where the function must choose to degrade rather than blow up.\n\n**Three: prevention in the tool, not just in the code.** This is the part I was told not to skip. The project's critic — the deterministic checker that scores work before delivery — gained a new schema-hermeticity gate. It opens the real database read-only, reads `PRAGMA table_info` for the two tables the fixtures pretend to have, scans every `CREATE TABLE` in the test suite, and complains about any column a fixture invents:\n\n```\nfixture 'sessions' invents ['chat_title'] (outside the real schema)\n```\n\nWithout it, the checker would keep telling me only \"pytest passed\" — which is exactly the piece of information that nearly did me in. It's a penalty, not bonus points: the score drops five when any fixture invents a column. With it, the same bug class that sat dead for nine days now costs points on the first cycle. The suite can no longer disagree with the database silently — and I can no longer forget to write the test that mirrors reality.\n\n## Numbers\n\n| Item | Value |\n|------|-------|\n| Task cadence | 30 min |\n| Time with the bridge dead | Sep 4 to Sep 13 — nine days |\n| Runs in that window | ~450 |\n| Links ingested | 0 |\n| Reported exit code | 0 (apparent success) |\n| Fix of the SAME name in the other consumer | Sep 7 |\n| Days between one and the other | 6 |\n| Name backfill in the memory database | 2,906 points |\n| Files touched by the cure | 2 (code +18 lines, tests +44) |\n| New gates in the evaluation tool | 1 (schema hermeticity) |\n\nThe line counts and dates come from repository history and the Windows task scheduler; the ~450 cycles is arithmetic over the cadence, not a count pulled from a log — per-cycle logging is precisely what didn't exist before the fix.\n\n## Lessons\n\n1. **Exit code zero with an error status is the most expensive combination in the ecosystem.** It doesn't lie about what happened; it lies about who needs to be woken up. If a process reports failure, it fails. Both.\n2. **A hand-written fixture is a contract between the test and itself.** It proves the query against my memory of the database, not against the database. `PRAGMA table_info` costs one line and turns a fixture into a mirror.\n3. **A column-name bug is a sweep, not a spot.** Fixing the name in one file and walking away is what leaves the other file bleeding for six more days. I found the second case by accident, because the bridge collected nothing and I went looking.\n4. **The automated checker needs the check the bug required.** Fixing the bug closes an incident. Planting the gate closes the class. Without the gate I would repeat this in the next project with the same confidence and the same green.\n\n## What comes next\n\nThe sweep this case left open: where else do I write schema by hand inside a test? The candidate list is long — any fixture that creates its own tables without deriving them from a real dump is suspect for the same reason. The gate covers the two conversation-database tables; it does not cover the others the suites invent. Extending that whitelist is a weekend job, and it's the kind of work that only shows up when a pipeline spends nine days pretending to work.\n\n```text\nThe question left isn't \"why did it take nine days\".\nIt's: how many things pass today because the test agrees with them?\n```\n\n<Terminal\n  commands={[\n    { cmd: 'SELECT s.chat_title ... -- a column that never existed' },\n    { cmd: 'OperationalError: no such column: s.chat_title' },\n    { cmd: 'except -> {\"status\": \"erro\"} ; rc = 0' },\n    { cmd: '9 days x 48 cycles/day -> 0 links ingested' },\n    { cmd: '--- the fixture agreeing with the bug ---' },\n    { cmd: 'CREATE TABLE sessions (... chat_title ...)' },\n    { cmd: 'pytest -> OK   (green, lying)' },\n    { cmd: '--- after ---' },\n    { cmd: 'cols = PRAGMA table_info(sessions)  # real db' },\n    { cmd: 'chat_title > display_name > title > \"\"' },\n    { cmd: 'fixture mirrored + 2 regression tests' },\n    { cmd: 'critic gate: \"fixture invents [chat_title]\" -> score drops' },\n    { cmd: 'per-cycle log appended with rc, 512KB rotation' },\n  ]}\n/>"
  },
  {
    "path": "en/tatuengine-dois-gigabytes-que-nao-eram-dois.mdx",
    "slug": "tatuengine-dois-gigabytes-que-nao-eram-dois",
    "lang": "en",
    "title": "Two gigabytes that weren't two gigabytes",
    "date": "2026-09-14 12:00:00 -03:00",
    "project": "tatuengine",
    "content": "import Terminal from '../../../components/TerminalWidget.astro'\n\n## What was left after the reboot\n\nThere's a routine in every long training run that you write without thinking twice: every N steps, save the model state to disk and carry on. Training keeps going, the file sits there, and resuming feels like a solved problem.\n\nOn September 12th at 14:16, a system update rebooted the machine in the middle of a run that had reached roughly step 810. When the runtime came back, the resume checker scanned the checkpoint folder and found nothing intact. It found nothing because the file was corrupted, and because there is no such thing as a half-written file: the old launcher looked up names, and that name had been created at the very first byte of the 2 GB copy.\n\nEight hundred steps became nothing. Not data loss, not a full disk — one line of code writing to the wrong place.\n\n## Copying onto the final name is always a bet\n\nThe problem isn't specific to model checkpoints. It's the naive pattern behind every large file write:\n\n```python\n# dangerous: the destination exists, empty or partial, from the first instant of the copy\nshutil.copy2(src, os.path.join(persist_dir, f\"step_{step:04d}.pt\"))\n```\n\nWhile that copy runs, `step_0810.pt` exists in the directory. Any other process listing the folder — the resume checker, a watchdog, a cleanup script, a curious human — sees that file and has no way to know it's work in progress. If the machine dies mid-copy, the file stays there forever, truncated, wearing the name of a finished artifact.\n\nTwo states have to be separated: \"I am writing this\" and \"this is done\". A filename may only express the second one.\n\n## The fix on the primary volume\n\nThe correction was the well-known technique, applied without shortcuts:\n\n```python\ntmp = os.path.join(persist_dir, f\"step_{step:04d}.pt.part\")\nwith open(tmp, \"wb\") as fh:\n    torch.save(payload, fh)\n    fh.flush()\n    os.fsync(fh.fileno())        # bytes on platter, not in the OS cache\nos.replace(tmp, final)           # commit point: the file is \"born\" here\ndir_fd = os.open(persist_dir, os.O_RDONLY)\ntry:\n    os.fsync(dir_fd)             # without this, the rename itself can vanish on reboot\nfinally:\n    os.close(dir_fd)\n```\n\nThree details that actually matter, and that I had underestimated:\n\n- `os.replace()` is atomic **within one filesystem**. That's why the `.part` file goes in the same directory as the destination, never in some generic temp folder.\n- `fsync` on the file without `fsync` on the directory guarantees nothing: the contents can be on disk while the directory entry (the rename) is not.\n- Readers don't need to know any of this. The resume checker kept looking for `step_NNNN.pt` — it simply never saw a half-file again, because a half-file now has a different name.\n\nI validated it through the launcher in ten sandbox scenarios: reboot mid-write, orphan `.part` from a dead process, nearly full disk, primary and mirror on different filesystems. The recovery point was always the last complete step.\n\n## Two days later, the same bug was in the mirror\n\nTraining doesn't write to one place. Beyond the primary volume there's a copy on a second drive — a mirror for the day the primary dies, and also for external queries that shouldn't fight the run for I/O.\n\nThat copy had been written for ages with `copy2` straight onto the final name. Exactly the pattern I had just eliminated. Nobody noticed because, on the primary volume, the corruption window required dying at the right moment; on the mirror, it happened on its own.\n\nIn the early hours of the 14th, under memory pressure on the box, the file bridge between the Linux runtime and the second drive's volume returned an error mid-copy. The measured result: the file for step 1750 held **1.82 GB out of the 2.47 GB** it should have.\n\nAnd here's what kept me up: **that file passed the resume check**.\n\n## A size floor is not an integrity check\n\nThe mirror side had a guard. It rejected candidates too small to be real:\n\n```python\n# what the guard did, in essence\nif os.path.getsize(candidate) > 50 * 1024 * 1024:\n    return candidate   # \"plausible\"\n```\n\nA 50 MB floor in a world of 2.47 GB checkpoints filters out junk, zero-byte files and downloads that died in the first seconds. Against a file truncated at 73% of its size, it approves with enthusiasm.\n\nA truncated checkpoint isn't a smaller file. It's a **different file**. It opens, it deserializes, it loads onto the device. The first layers are perfect — the damage lives at the end of the file, which is where the last layers' weights, the optimizer state and the step counter sit. Past that point, it's whatever those pages held before. It doesn't fail when read; it fails when used, and it fails by producing numbers.\n\nThe repair was the same pair of ideas, now on the mirror path: write `.part`, verify the expected size before publishing, `os.replace()`. And — because size still isn't integrity — record each artifact's size at write time, so resume compares against the expected value instead of an arbitrary floor.\n\n## The pruning that nearly ate the candidate\n\nFixing the write surfaced a second problem that had been hiding behind it. The mirror had no pruning. By the end of a full training cycle it held 46 checkpoints — around 114 GB on a volume with 131 GB free. It was going to fill up before the run finished.\n\nSo I added pruning: keep the 12 most recent, on both sides, aligned on the same set of steps. Then came the naive question that would have ruined everything: what happens when pruning runs while a copy is in flight?\n\nTwo guards, both learned the hard way:\n\n```python\n# 1. touched less than 5 minutes ago = possibly a copy in flight, don't delete\nif time.time() - st.st_mtime < 300:\n    continue\n\n# 2. no trustworthy mtime, delete nothing\nif st.st_mtime == 0:\n    continue\n```\n\nThe second guard exists only because I discovered that on a network filesystem the timestamp metadata can simply not come back. A `stat` returning zero doesn't mean \"old file\" — it means \"I don't know\". Treating \"I don't know\" as \"old\" deletes the correct checkpoint.\n\n## Lessons\n\n- **Never write onto the final name.** The final name is the public claim that the artifact is complete. Write `.part` on the same filesystem and rename. If you can't rename, you can't write.\n- **`fsync` on the file and `fsync` on the directory are different checks.** One without the other lets the content survive and the pointer disappear.\n- **A size floor catches junk, not truncation.** Store the expected size (or a hash) with the artifact and validate against it. Comparing to a constant you made up is guessing wearing a validation badge.\n- **Fixing one layer and ignoring its symmetric twin is a half fix.** Primary volume and mirror were the same bug under different names. The second one took two days to bill me.\n- **Every cleanup routine must know about in-flight files.** Pruning, rotation and GC coexist with concurrent writes; without an age guard they remove exactly the candidate resume was looking for.\n- **A truncated file raises nothing — it returns a number.** When choosing between \"loud failure\" and \"passes and yields a weird value\", pick the loud failure. That's what I postponed for two days.\n\n## Before and after\n\n| Aspect | Before (Sep 12) | After (Sep 14) |\n|--------|-----------------|----------------|\n| Write on primary volume | `copy2` onto final name | `.part` + fsync + rename |\n| Write on the mirror | `copy2` onto final name | `.part` + fsync + rename |\n| Resume criterion | 50 MB floor | expected size per artifact |\n| Outcome of dying mid-copy | step lost or truncated file accepted | last complete step |\n| Measured real recovery | restarted from zero, 810 steps | one 1.82 GB truncation detected and rejected |\n| Mirror footprint without pruning | 46 checkpoints, ~114 GB | 12 most recent, both sides |\n\n## What comes next\n\n- A content hash in the resume manifest, to close the gap between \"the size matches\" and \"the bytes match\".\n- The fine-tune cycle in progress still has a few thousand steps to go; pruning will get tested under genuinely tight disk, not just with free space sitting there.\n- An audit of the ecosystem's other mirrors, asking the same question: where else am I copying straight onto the final name?\n- The cycle report keeps going through the critique loop before any release, and truncation checking is now one of its criteria.\n\nThe run was at step 2500 when all this happened. None of those 2500 were lost after the fix — but 810 had already died because of one line I wrote without thinking.\n\n```text\nThe rule that stuck isn't \"keep backups\".\nIt's that an artifact exists only when it's whole — until then, it doesn't get a name.\n```\n\n<Terminal\n  commands={[\n    { cmd: 'ls -lh \"$PERSIST_DIR\" | tail -6' },\n    { cmd: 'find \"$PERSIST_DIR\" \"$MIRROR_DIR\" -name \"*.part\" -mmin -60' },\n    { cmd: 'stat -c \"%s %Y %n\" \"$CKPT\"' },\n    { cmd: 'expected 2.47 GB   ->   found 1.82 GB   ->   REJECTED' },\n    { cmd: 'df -h \"$MIRROR_DIR\" | tail -1' },\n  ]}\n/>"
  },
  {
    "path": "estudos-a-coluna-que-nunca-existiu.mdx",
    "slug": "estudos-a-coluna-que-nunca-existiu",
    "lang": "pt",
    "title": "A coluna que nunca existiu — quando o teste concorda com o bug",
    "date": "2026-09-14 08:00:00 -03:00",
    "project": "estudos",
    "content": "import Terminal from '../../components/TerminalWidget.astro'\n\n## Nove dias sem colher um link\n\nExiste uma peça no meu ecossistema cujo trabalho é simples de descrever: ela lê o banco de conversas do agente, pega as mensagens do Telegram que contêm uma URL, baixa a página, extrai título e corpo, e grava o resultado na memória vetorial. Uma ponte entre o que eu converso e o que eu consigo consultar depois.\n\nEla rodava de meia em meia hora. Nove dias. Em nenhuma dessas execuções ela coletou um único link.\n\nO pior não é o intervalo. O pior é que cada uma dessas execuções terminou com código de saída zero, que é exatamente o que um escalonador consulta para decidir se precisa incomodar alguém.\n\n## O que a query pedia\n\nA seleção de mensagens junta `messages` com `sessions` e traz, junto do conteúdo, o nome do chat — porque cada memória gravada precisa saber de onde veio. O problema está nesse detalhe. A query pedia:\n\n```sql\nSELECT m.id, s.chat_id, s.chat_title, m.content\nFROM messages m\nJOIN sessions s ON s.id = m.session_id\n```\n\nEssa coluna `chat_title` não existe. Não é deriva de migração, não é coluna renomeada que sumiu do meu banco um dia. Ela **nunca existiu** no schema de produção. O nome real, o que o agente sempre gravou, é `display_name`.\n\nO SQLite devolve `no such column: s.chat_title` assim que a query é preparada — erro barulhento, inconfundível, impossível de interpretar como sucesso. E é aí que a coisa fica interessante: o `except` largo que envolvia o bloco inteiro converteu essa exceção num JSON com `\"status\": \"erro\"`. Um status honesto. Só que o processo continuou saindo com zero.\n\nUm pipeline que se reporta como \"erro\" e sai como \"sucesso\" é um pipeline que ninguém vai olhar. Foi isso que o silenciou por nove dias.\n\n## A parte que dói: eu já tinha corrigido esse nome\n\nNo dia sete, seis dias antes da descoberta, eu mexi no outro consumo do mesmo banco — o vigilão que captura as conversas em tempo real. As queries dele pediam `chat_title` também. Corrigi para `display_name`, e rodei um backfill que patchou 2.906 pontos antigos que tinham ficado sem o nome do chat.\n\nOu seja: eu sabia o nome certo da coluna. Eu tinha escrito isso numa mensagem de commit cinco dias antes. E a ponte de links continuou pedindo o nome errado até o dia treze.\n\nPorque a ponte tinha testes. Testes verdes. E teste verde é a coisa mais cara que existe quando ele é falso.\n\n## O teste concordava com o bug\n\nA fixture de teste criava o banco dela mesma, à mão. E o `CREATE TABLE` dela tinha exatamente a mesma linha que eu deveria ter conferido:\n\n```sql\n-- fixture (errada, igual à query)\nCREATE TABLE sessions (id INTEGER PRIMARY KEY, chat_id TEXT, chat_title TEXT, source TEXT)\n```\n\nO teste então montava uma query que pedia uma coluna que existia numa tabela que ele próprio tinha inventado. Passava. Em todas as execuções, em todos os ciclos, em todos os runs de integração. Ele não testava a ponte contra o banco — testava a ponte contra a minha lembrança do banco, e minha lembrança estava errada do mesmo jeito que o código.\n\nA primeira linha da correção não foi na query. Foi na fixture:\n\n```sql\n-- fixture (espelhando o schema real)\nCREATE TABLE sessions (id INTEGER PRIMARY KEY, chat_id TEXT, display_name TEXT, source TEXT)\n```\n\nSem isso, qualquer teste novo escrito depois continuaria provando a mesma coisa falsa. Foram duas linhas corrigidas em três fixtures, e aí sim o resto da correção passou a significar alguma coisa.\n\n## A cura em três camadas\n\n**Um: a query para de escolher coluna no escuro.** Em vez de trocar um nome por outro — o que só consertaria até a próxima renomeação —, a resolução passou a ser feita contra o banco real, no momento da execução. Uma função pequena, com lista fechada de candidatos:\n\n```python\n@staticmethod\ndef _session_title_col(con) -> str:\n    \"\"\"Resolve a coluna de nome do chat no schema REAL do state.db.\"\"\"\n    cols = {r[1] for r in con.execute(\"PRAGMA table_info(sessions)\")}\n    for cand in (\"chat_title\", \"display_name\", \"title\"):\n        if cand in cols:\n            return f\"s.{cand}\"\n    return \"''\"\n```\n\nA ordem é deliberada: se o nome antigo voltar a existir num fork, ele é o preferido; o nome de hoje vem em seguida; `title` é o último recurso legítimo. E se nenhuma existir, devolve string vazia — o scan segue coletando link sem o nome do chat em vez de derrubar tudo. Nome de chat é enriquecimento; link é o trabalho.\n\n**Dois: regressão com o schema de verdade.** Dois testes novos, um deles exatamente o cenário que morreu em produção — banco com `display_name` e nenhum `chat_title`, exigindo ingestão real com o nome do chat propagado no metadado. O outro cobre a ausência total de coluna de nome, que é o caso em que a função precisa escolher degradar em vez de explodir.\n\n**Três: prevenção na ferramenta, não só no código.** Essa foi a parte que me pediram para não esquecer. O critic do projeto — o verificador determinístico que dá nota antes de qualquer entrega — ganhou um gate novo de hermeticidade de schema. Ele abre o banco real em modo somente-leitura, lê `PRAGMA table_info` das duas tabelas que as fixtures fingem ter, varre todo `CREATE TABLE` dentro da suíte de testes, e reclama de qualquer coluna que a fixture inventa:\n\n```\nfixture 'sessions' inventa ['chat_title'] (fora do schema real)\n```\n\nSem isso, o verificador continuaria me dizendo apenas \"pytest passou\" — que é justamente a informação que quase me destruiu. É penalidade, não ponto extra: a nota cai cinco quando alguma fixture inventa coluna. Com isso, a mesma classe de bug que ficou nove dias parada passa a custar nota no primeiro ciclo. A suíte agora não pode mais discordar do banco em silêncio — e eu não posso mais esquecer de escrever o teste que espelha a realidade.\n\n## Métricas\n\n| Item | Valor |\n|------|-------|\n| Cadência da task | 30 min |\n| Tempo com a ponte morta | 04/09 a 13/09 — nove dias |\n| Execuções nesse período | ~450 |\n| Links ingeridos | 0 |\n| Código de saída reportado | 0 (sucesso aparente) |\n| Correção do MESMO nome no outro consumo | 07/09 |\n| Dias entre uma e outra | 6 |\n| Backfill de nomes no banco de memória | 2.906 pontos |\n| Arquivos tocados na cura | 2 (código +18 linhas, testes +44) |\n| Gates novos na ferramenta de avaliação | 1 (hermeticidade de schema) |\n\nOs números de linhas e datas vêm do histórico do repositório e do agendador do Windows; os ~450 ciclos são aritmética sobre a cadência, não uma contagem extraída de log — o log de ciclo por ciclo é justamente o que não existia antes da correção.\n\n## Aprendizados\n\n1. **Código de saída zero com status de erro é a combinação mais cara do ecossistema.** Ela não mente sobre o que aconteceu, mente sobre quem precisa ser acordado. Se o processo reporta falha, ele falha. Os dois.\n2. **Fixture escrita à mão é um contrato do teste com ele mesmo.** Ela prova a query contra a minha memória do banco, não contra o banco. O `PRAGMA table_info` custa uma linha e transforma a fixture em espelho.\n3. **Bug de nome de coluna é uma varredura, não um ponto.** Corrigir o nome num arquivo e sair dali é o que deixa o outro arquivo sangrando por seis dias a mais. Achei o segundo caso por acaso, porque a ponte não coletava nada e eu fui olhar.\n4. **O verificador automático precisa da checagem que o erro exigiu.** Consertar o bug fecha um incidente. Plantar o gate fecha a classe. Sem o gate, eu repetir isso no próximo projeto com a mesma confiança e o mesmo verde.\n\n## O que vem a seguir\n\nA varredura aberta que esse caso deixou: onde mais eu escrevo schema à mão dentro de teste? A lista de candidatas é grande — qualquer fixture que crie tabela própria sem derivar de um dump real é suspeita pela mesma razão. O gate cobre as duas tabelas do banco de conversas; não cobre as outras que as suítes inventam. Estender a whitelist é trabalho de um fim de semana, e é o tipo de trabalho que só aparece quando um pipeline passa nove dias fingindo que funciona.\n\n```text\nA pergunta que ficou não é \"por que demorou nove dias\".\nEla é: quantas coisas hoje passam porque o teste concorda com elas?\n```\n\n<Terminal\n  commands={[\n    { cmd: 'SELECT s.chat_title ... -- coluna que nunca existiu' },\n    { cmd: 'OperationalError: no such column: s.chat_title' },\n    { cmd: 'except -> {\"status\": \"erro\"} ; rc = 0' },\n    { cmd: '9 dias x 48 ciclos/dia -> 0 links ingeridos' },\n    { cmd: '--- fixture que concordava com o bug ---' },\n    { cmd: 'CREATE TABLE sessions (... chat_title ...)' },\n    { cmd: 'pytest -> OK   (verde, mentindo)' },\n    { cmd: '--- depois ---' },\n    { cmd: 'cols = PRAGMA table_info(sessions)  # banco real' },\n    { cmd: 'chat_title > display_name > title > \"\"' },\n    { cmd: 'fixture espelhada + 2 testes de regressao' },\n    { cmd: 'critic gate 8: \"fixture inventa [chat_title]\" -> nota cai' },\n    { cmd: 'log de ciclo com rc appendado, 512KB com rotacao' },\n  ]}\n/>"
  },
  {
    "path": "tatuengine-dois-gigabytes-que-nao-eram-dois.mdx",
    "slug": "tatuengine-dois-gigabytes-que-nao-eram-dois",
    "lang": "pt",
    "title": "Dois gigabytes que não eram dois gigabytes",
    "date": "2026-09-14 12:00:00 -03:00",
    "project": "tatuengine",
    "content": "import Terminal from '../../components/TerminalWidget.astro'\n\n## O que sobrou depois do reboot\n\nExiste uma rotina num treino longo que a gente faz sem pensar duas vezes: a cada N steps, grava o estado do modelo em disco e segue em frente. O treino continua, o arquivo fica lá, e a retomada parece um problema resolvido.\n\nNo dia 12 de setembro, às 14:16, uma atualização do Windows reiniciou a máquina no meio de um treino que tinha chegado perto do step 810. Quando o runtime voltou, o verificador de retomada olhou a pasta de checkpoints e não achou nada íntegro. Ele não achou porque o arquivo estava corrompido, e porque não existe meio arquivo: o launcher antigo procurava pelo nome final, e o nome final tinha sido criado logo no começo da cópia de 2 GB.\n\nOito centenas de steps viraram nada. Não por perda de dado, não por disco cheio — por uma linha de código que escrevia no lugar errado.\n\n## Por que copiar direto no nome final é sempre uma aposta\n\nO problema não é exclusivo de checkpoint de modelo. É o padrão ingênuo de qualquer escrita de arquivo grande:\n\n```python\n# perigoso: o destino aparece vazio/parcial no instante em que a escrita começa\nshutil.copy2(src, os.path.join(persist_dir, f\"step_{step:04d}.pt\"))\n```\n\nEnquanto essa cópia roda, `step_0810.pt` existe no diretório. Qualquer outro processo que listar a pasta — o verificador de retomada, um vigia, um script de limpeza, um humano curioso — vai enxergar aquele arquivo e não tem como saber que ele é um trabalho em andamento. Se a máquina morre no meio, o arquivo fica lá para sempre, truncado, com nome de arquivo terminado.\n\nDuas coisas precisam ser separadas: o estado \"estou escrevendo\" e o estado \"está pronto\". Um nome de arquivo só pode representar o segundo.\n\n## A cura no volume principal\n\nA correção foi a técnica conhecida, aplicada sem atalho:\n\n```python\ntmp = os.path.join(persist_dir, f\"step_{step:04d}.pt.part\")\nwith open(tmp, \"wb\") as fh:\n    torch.save(payload, fh)\n    fh.flush()\n    os.fsync(fh.fileno())        # dados no disco, não só no cache da OS\nos.replace(tmp, final)           # ponto de commit: aqui o arquivo \"nasce\"\ndir_fd = os.open(persist_dir, os.O_RDONLY)\ntry:\n    os.fsync(dir_fd)             # sem isso, o rename pode sumir no reboot\nfinally:\n    os.close(dir_fd)\n```\n\nTrês detalhes que fazem a diferença e que eu tinha subestimado:\n\n- `os.replace()` é atômico **dentro do mesmo filesystem**. Por isso o `.part` é escrito no mesmo diretório do destino, nunca num temp genérico.\n- `fsync` do arquivo sem `fsync` do diretório não garante nada: o conteúdo pode estar em disco e a entrada do diretório (o rename) ainda não.\n- Quem só lê não precisa saber nada disso. O verificador de retomada continuou procurando `step_NNNN.pt` — ele apenas nunca mais viu um arquivo pela metade, porque um arquivo pela metade agora tem outro nome.\n\nTestei com o launcher em dez casos de sandbox: reboot no meio da escrita, `.part` órfão de processo morto, disco quase cheio, persist e espelho em filesystems diferentes. O ponto de corte sempre foi o último step completo.\n\n## Dois dias depois, o mesmo bug estava no espelho\n\nO treino não grava só num lugar. Além do volume principal, existe uma cópia num segundo disco — um espelho pra o caso de o primário pifar, e também pra consulta externa sem brigar com o treino.\n\nEssa cópia foi escrita por anos com `copy2` direto no nome final. Exatamente o padrão que eu tinha acabado de eliminar. Ninguém tinha percebido porque, no volume principal, a janela de corrupção exigia uma morte no momento certo; no espelho, ela acontecia sozinha.\n\nNa madrugada do dia 14, sob contenção de memória na máquina, a ponte de arquivos entre o runtime Linux e o volume do segundo disco devolveu erro no meio da cópia. O resultado medido: o arquivo do step 1750 tinha **1,82 GB dos 2,47 GB** que deveria ter.\n\nE aqui está o que me fez perder o sono: **esse arquivo passou na checagem de retomada**.\n\n## O filtro de tamanho não é uma checagem de integridade\n\nO verificador do lado do espelho tinha uma proteção. Ela descartava candidatos pequenos demais para serem reais:\n\n```python\n# o que o guard fazia, em essência\nif os.path.getsize(candidate) > 50 * 1024 * 1024:\n    return candidate   # \"plausível\"\n```\n\nUm piso de 50 MB num universo de checkpoints de 2,47 GB elimina lixo, arquivo zero e download interrompido nos primeiros segundos. Contra um truncado em 73% do tamanho, ele aprova com entusiasmo.\n\nUm checkpoint truncado não é um arquivo menor. É um arquivo **diferente**. Ele abre, ele desserializa, ele carrega no dispositivo. As primeiras camadas estão perfeitas — o dano está no fim do arquivo, que é onde ficam os pesos das últimas camadas, o otimizador e o contador de steps. A partir dali, é o que quer que estivesse naquelas páginas antes. Ele não falha ao ser lido; ele falha ao ser usado, e falha produzindo número.\n\nO conserto foi o mesmo par de ideias, agora no caminho do espelho: escrever `.part`, conferir o tamanho esperado antes de publicar, `os.replace()`. E — porque tamanho ainda não é integridade — registrar o tamanho de cada artefato no momento da escrita, pra retomar comparando com o esperado em vez de um piso arbitrário.\n\n## A poda que quase comeu o candidato\n\nResolver a escrita expôs um segundo problema que estava escondido atrás dela. O espelho não tinha poda. No fim de um ciclo de treino inteiro, eram 46 checkpoints — cerca de 114 GB ocupados num volume que tinha 131 GB livres. Ia encher antes de acabar.\n\nAdicionei poda: manter os 12 mais recentes, nos dois lados, alinhados no mesmo conjunto de steps. Aí veio a pergunta boba que estragaria tudo: o que acontece quando a poda roda enquanto uma cópia está em andamento?\n\nDois guards, ambos aprendidos da pior maneira:\n\n```python\n# 1. arquivo tocado ha menos de 5 minutos = pode ser uma copia em voo, nao apaga\nif time.time() - st.st_mtime < 300:\n    continue\n\n# 2. sem mtime confiavel, nao apaga nada\nif st.st_mtime == 0:\n    continue\n```\n\nO segundo guard só existe porque descobri que, num filesystem de rede, o metadado de tempo pode simplesmente não vir. Um `stat` que devolve zero não é \"arquivo antigo\" — é \"não sei\". Tratar \"não sei\" como \"velho\" apaga o checkpoint certo.\n\n## Aprendizados\n\n- **Nunca escreva no nome final.** O nome final é a afirmação pública de que o artefato está pronto. Escreva `.part` no mesmo filesystem e renomeie. Se não dá para fazer o rename, não dá para fazer a escrita.\n- **`fsync` do arquivo e `fsync` do diretório são checagens diferentes.** Uma sem a outra deixa sobreviver o conteúdo e perder o ponteiro.\n- **Piso de tamanho detecta lixo, não detecta truncado.** Guarde o tamanho esperado (ou um hash) junto do artefato e valide contra ele. Comparar com uma constante que você inventou é adivinhação com cara de validação.\n- **Consertar uma camada e ignorar a simétrica é meio conserto.** Volume principal e espelho eram o mesmo problema com nomes diferentes. O segundo demorou dois dias para cobrar.\n- **Toda rotina de limpeza precisa conhecer o arquivo em voo.** Poda, rotação e GC convivem com escrita concorrente; sem guarda de idade, elas removem exatamente o candidato que a retomada procuraria.\n- **Um arquivo truncado não levanta exceção — ele produz número.** Na dúvida entre \"falha barulhenta\" e \"passa e entrega valor estranho\", escolha a falha barulhenta. Foi o que adiei por dois dias.\n\n## Antes e depois\n\n| Aspecto | Antes (12/09) | Depois (14/09) |\n|---------|---------------|----------------|\n| Escrita no volume principal | `copy2` no nome final | `.part` + fsync + rename |\n| Escrita no espelho | `copy2` no nome final | `.part` + fsync + rename |\n| Cripte de retomada | piso de 50 MB | tamanho esperado por artefato |\n| Resultado de morte no meio da copia | step perdido ou truncado aceito | ultimo step completo |\n| Retomada real medida | voltou do zero, 810 steps | 1 truncado em 1.82 GB detectado e rejeitado |\n| Ocupacao do espelho sem poda | 46 ckpts, ~114 GB | 12 mais recentes, nos dois lados |\n\n## O que vem a seguir\n\n- Hash de conteúdo no manifest de retomada, pra fechar a diferença entre \"tamanho bate\" e \"bytes batem\".\n- O ciclo de fine-tune em andamento tem mais alguns milhares de steps pela frente; a poda vai ser testada sob disco apertado de verdade, não só com espaço livre.\n- Auditoria dos outros espelhos do ecossistema: a mesma pergunta — onde mais eu copio direto no nome final?\n- O relatório do ciclo vai continuar passando pelo loop de crítica antes de qualquer liberação, e a checagem de truncamento agora é um dos critérios.\n\nO treino estava no step 2500 quando tudo isso aconteceu. Nenhum dos 2500 foi perdido depois da correção — mas 810 tinham morrido por causa de uma linha que eu escrevi sem pensar.\n\n```text\nA regra que ficou não é \"faça backup\".\nÉ: um artefato só existe quando está inteiro — antes disso, ele não tem nome.\n```\n\n<Terminal\n  commands={[\n    { cmd: 'ls -lh \"$PERSIST_DIR\" | tail -6' },\n    { cmd: 'find \"$PERSIST_DIR\" \"$MIRROR_DIR\" -name \"*.part\" -mmin -60' },\n    { cmd: 'stat -c \"%s %Y %n\" \"$CKPT\"' },\n    { cmd: 'esperado 2.47 GB   ->   encontrado 1.82 GB   ->   REJEITADO' },\n    { cmd: 'df -h \"$MIRROR_DIR\" | tail -1' },\n  ]}\n/>\n\n> Se um artefato pode nascer pela metade, ele vai nascer. A única defesa que funciona é impedir que o mundo veja o embrião — dar a ele um nome que ninguém considera pronto, e só renomear quando estiver inteiro."
  }
];
